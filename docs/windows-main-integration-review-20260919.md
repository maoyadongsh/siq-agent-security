# Windows 开发条线阶段性整合复核（2026-09-19）

## 本次交付范围

维护者已授权尽量合并可验收的 Windows 开发分支。整合基线为 `main@c089aed`，包含已合并的 #80、#81、#82，以及修复不可变记录短元数据重试的 `52bbe4e`。本批整合 #83 的 `3d8eb2d`，保留原开发者提交和历史证据，不把历史候选的实机结果转记到新候选。

纳入：Windows 显式文件系统权限解释与身份复验、状态协议读写版本保护、三宿主管理接线、WorkBuddy 用户/项目 Skill 安装更新移除、SEC 管理页面、受管钩子响应预算、用户任务安装升级回滚、通知及配套合同、测试、原始阶段证据。

## 冲突处理

1. 客户端安装同时保留 Linux 的“可信清单验证后、暂存前建立持久身份”和 Windows 的 Writer/已有身份复用约束。生产入口统一拒绝仅由安装器环境提供的临时 seed；Windows 使用自身身份准备流程，不能退化为直接创建密钥。
2. 审批恢复保留主线的当前 SEC 与原调用归属逐字段复验，同时使用来自已验证 Intent 的 Windows 资源解释；不能用新资源支持覆盖主线的归属安全修复。
3. 内嵌 UI 从合并后的前端源码重新构建，不选择任一分支的旧散列产物。
4. 规格同时保留 Linux 首次身份修复说明和 Windows 安装/任务归属扩展。

## 单独保留的发行变更

`skills/siq-agent-security/scripts/verify_manifest.py` 的 Windows junction 检查改变了已签名 Skill 包内容。旧正式清单绑定内容摘要 `e69b1486…`；上游修改后摘要为 `d815fc4a…`，不能靠替换摘要文字或关闭断言修复。

本机未配置官方发布种子，本批采用源代码整合与正式发行分开交付：

- 主线保留原已签名 Skill 包的字节及既有验签测试；不改签名或信任根。
- junction 校验脚本修改，以及对应 Python/Go 跨平台逃逸测试调整，共四个路径，完整保留至 `codex/windows-bootstrap-signing-pending-20260919` 待签分支。
- 本次不声称旧 Python 引导器已具备新增 junction 防护；Windows 新版 Skill 引导安装的正式发行仍被 #87 阻塞。运行时原生 Go 路径/身份防护与此发行包是不同范围。
- 待签分支必须使用批准的新版本、重新构建的四目标制品和实际下载前缀签发，再通过内置信任根验签和原有发行内容一致性检查。禁止直接复用旧二进制摘要或把开发构建标作正式发行。

此拆分不是把失败测试改成通过：当前签名包保持原样，尚未签署的新包及其新测试保留为独立未完成交付。原始上游失败证据继续保留。

## 历史分支核对

以下开发条线的内容已被后续整合，不应再强制应用旧实现：

| 条线 | 复核方式 |
| --- | --- |
| `windows-client-install-20260918` | 两项独有提交与 #83 为等价补丁 |
| `windows-migration-done-barrier-20260918` | 两项独有提交与 #83 为等价补丁 |
| `windows-task-upgrade-20260918` | 实现、nonce 和原生证据为等价补丁；初版合同由后续版本替代 |
| `windows-resource-binding-20260916` | 规范化实现为等价补丁；初版合同由完整接线版本替代 |
| `windows-filesystem-evidence-20260914` | 51 个相关文件在整合树中逐字节一致 |
| `windows-openclaw-session-evidence-20260918` | 会话材料及最终换行/摘要校正版本一致；中间材料由该分支最后的校正提交替代 |

其余远端 `codex/windows-*` 分支已是整合前 `main` 的祖先。分支引用是否仍存在，不等于其内容未交付；本批不删除协作者分支。

## 验证与证据边界

整合期间执行 Go 全量回归、vet、关键状态/审批组件 race、四目标 CGO=0 构建、Control API Python 回归与 Ruff、前端测试及两种构建、OpenClaw 适配器脚本和 Hermes 适配器测试。

首次 Go 全量运行仅 `internal/skillmanifest` 的三个既有清单一致性断言失败，准确复现 #87；其余包通过。拆分待签交付后，重新执行受影响的清单、安装与审批包，保留其余已通过结果，最终远端 CI 仍须通过才合并。

历史证据日志 `workbuddy-scoped-skills-20260918/logs/web/full-regression-r1/stdout.log` 和 `vitest-r2.log` 含文件尾空行。本批保留原字节，不为消除 whitespace 提示改写证据；生产代码与新增复核文档须通过 whitespace 检查。

本机是 Linux；本批组件测试和交叉编译不替代 Windows 原生实测。OpenClaw 的既有 Windows 路线证据来自 WSL Agent；Hermes 是原生 CLI；WorkBuddy 有原生最小 Skill 读写证据，但完整审批恢复、桌面更新及重复稳定性仍未完成。不能由本次合并关闭所有 Windows Issue，也不能将不同候选的测试合并宣称一个发行候选全部通过。

#68 为用户明确排除的 Cursor 云环境，本批不合并、不关闭。
