# 监督进程退出验收结果与阶段交接

日期：2026-09-24。结论：本轮实际测试已经结束，整体未通过；测试资源已清理，不继续开启下一轮测试。客户端及企业端固定候选保持不变，原标杆目标仍未完成。

## 实际结果

v2 经真实业务 HTTP → Hermes/OpenShell/Qwen 收到模型输出后，使用 pidfd 终止与原请求绑定的监督进程，业务 API 保持存活且未重启。SSE 共一个 run、两个 delta、一个 error，没有成功 done。原执行环境已回收，子身份已撤销，旧子凭据返回 401，父身份在诊断撤销前仍有效。

整轮证据 `passed=false`，唯一为假的检查为 `supervisor_exit_business_failed_without_success`：数据库终态不符合诊断预期的 `failed`。现有脱敏产物没有保存实际终态值，因此不能推断是哪一个终态，也不能据此直接断言生产逻辑错误或修改断言使其通过。后续应先核对运行合同与终态证据，再决定修复实现还是诊断。

API finalizer 已在诊断清理前完成释放；临时数据库已删除、隔离 18083/47811 监听关闭，原服务与预览进程身份未变。模型桥开始响应 200，故障后的终端状态为 502，不属于正常生成成功。

v1 因诊断调用仅可清理句柄禁止使用的执行断言而失败，未注入预定信号；已修正诊断只读观察方式，遗留资源经原执行绑定补偿回收。保留 v1 失败及补偿记录，不记作自动故障恢复成功。

## 交接边界

本次只记录实际结果，没有修改生产代码、重启主服务、改写历史证据或重跑已经通过的检查。监督退出验收仍是待闭环项，不能据清理成功将整轮改为通过。

后续工程缺口为监督退出终态核对、正式报告发布与当前授权下读回链路。生产接入、固定模型质量/性能、原生平台验收及签发另按[收尾清单](flagship-closeout-20260924.md)执行；外部安全检查仍在后续任务清单。现有候选包、预览入口及限制见[统一交付状态](flagship-delivery-status-20260923.md)。

[本批脱敏证据](../evidence/flagship-optimization-20260921/business-supervisor-exit-result-20260924.json)由候选清单（本机私有路径：`var/flagship/supervisor-exit-result-20260924/candidate.json`）绑定。不包含私有日志、模型正文或凭据。
