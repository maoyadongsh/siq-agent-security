# 企业安全控制台候选部署完成（2026-09-24）

现有 SIQ 平台中的企业安全 API 和 Web 已从 9 月 6 日的镜像更新到当前功能候选。入口仍为 **http://192.168.2.121:10082/agent-security/**，沿用原 Gateway、IAM、PostgreSQL 和既有配置；没有新增对外端口或使用开发身份。

这完成了企业控制台的代码部署及部署恢复检查，**不等于真实业务账号、独立审批人和跨系统业务导航已经验收**。研究业务入口仍为本机 15173，企业导航 origin 尚未配置；不能把远端用户浏览器的 loopback 当成服务器业务地址。

## 来源和运行身份

从 E164 固定提交 `4a4bcd0b3695dfa56e73c24d8f42a547fe94be31` 导出隔离源码，加入已通过验证的导航与报告合同增量。共 697 个文件，13 个增量文件逐一核对原证据摘要；使用仓库 Dockerfile 的固定基础镜像及锁文件构建，没有把工作区 `.env`、虚拟环境或私有数据送入构建上下文。

| 服务 | 本批最终镜像 |
| --- | --- |
| API | `sha256:24a03ceb4b484b77e3efffa8e2b83f30f2761d046c3e8761de0137889c8eb254` |
| Web | `sha256:6adc727f15d7dffcb0a6fb1ec2f2931315e17e6841545cea22378ad752eaeb4b` |

当前容器分别为 `a845413e8a735ba2c1216526038befb3db49d12060e94a4bcc22503352d704aa` 和 `b11518574304b7bb50553756df9a707c5728773f16bed51061ec2681c3b54555`。容器身份会在后续重建时改变，操作前必须读回。

API 以 `appuser` 运行，现有生产模式、数据库运行账户和 IAM 配置保持；`X-Dev-*` 不能授权。两服务沿用原 restart policy，其他平台服务没有被本批 Compose 操作重建或更换镜像。

## 数据库与恢复

原实际安全数据库为 0013。已先备份到私有目录，在独立内网和临时 PostgreSQL 中完整恢复，再以新镜像升级到 0017。19 张既有表原字段的数据摘要一致；重复迁移无变化，新 API 可使用运行账户启动，旧 API 也能读取升级后的库。演练容器和网络已清理。

实际切换前正常停止原安全 API，再次备份，然后使用原迁移账户执行 0013→0017。实际备份 SHA-256：`4ffddf79fe2e2322eacba843e2df4b35d2ecf5bece1d634b621541ea626f4277`，65,573 字节。备份仅包含本服务数据库，未查询或迁移其他服务数据库。

已执行新镜像→原旧镜像→新镜像的真实切换，每次均从原 Gateway 检查页面、健康和匿名拒绝。回退保留 0017 表及数据，没有降级删除审计或部署记录；切回旧代码前确认没有新的 deployment submission。

## 检查结果及失败记录

- 隔离恢复/迁移六项检查通过，覆盖旧字段保留、重复迁移、新 API 权限拒绝及旧 API 启动。
- 实际数据库达到 0017，API 运行账户具有新表必要权限，既有 IAM JWKS 可读取；这仅证明连接与运行权限，未使用真实用户登录。
- Gateway 的页面/健康可访问，新接口匿名请求及伪造开发身份均被拒绝。
- 容器内 146 个 API 源码/迁移文件与隔离清单逐项匹配，Gateway 的 index、主脚本和样式与 Web 镜像一致。
- Chromium 桌面登录字段、375px 手机无横向溢出、无页面 JavaScript 异常三项通过；未提交登录表单，没有尝试未知账号或默认密码。

首次部署脚本在新旧切换和 JWKS 检查通过后，因要求所有其他服务的启动时间不变而失败，并恢复了旧安全镜像。核查发现原 `flow-worker` 容器持续自动重启，累计超过 25,000 次。后续记录显示同一旧容器在本批前后继续重启，并未被本批重建。修正诊断为核对其他容器 ID/镜像不变、单列观察到的自动重启后，已恢复新版并通过；不据此声称整个平台全部健康。Flow 故障保留为独立环境问题，本轮没有修改 Flow。

首轮浏览器检查被本机代理 7897 返回 502；同头直连为 200。v2 只为测试浏览器显式设置直连，确认连接到实际 Gateway 后完成检查，没有修改宿主全局代理。系统 Python 没有 Playwright，改用已有依赖的 Python 执行；没有安装新依赖。

## 后续运维

本批材料位于 `var/flagship/enterprise-runtime-delivery-20260924/`。`compose-new.private.json` 与 `compose-rollback.private.json` 是仅包含安全 API/Web 的精确配置，复用原外部 Docker 网络；配置包含既有受控环境值，权限 0600，不提交或对外分发。源码清单和脱敏结果可单独审阅。

需要重新启动本批准确候选时，在核对清单、镜像及未决操作后执行：

```bash
docker compose -p siq-platform \
  -f /home/maoyd/siq/siq-agent-security/var/flagship/enterprise-runtime-delivery-20260924/compose-new.private.json \
  up -d --no-deps --no-build --pull never agent-security-api agent-security-web
```

受支持回退使用同目录的 `compose-rollback.private.json`，保留数据库 0017；先关闭部署执行入口并核对未决操作。该配置只声明两个服务，因此不能加 `--remove-orphans`，否则可能删除同项目的其他业务服务。

一次性 `deploy.py`、`resume.py` 已执行，不要再次运行。原平台源码配置及启动脚本没有更改；全平台 `start_all.sh` 会构建并切换整套验收配置，不是本批两服务的恢复命令。后续重新构建须重新绑定源码与镜像，不能把本批验收直接套给另一次构建。

## 剩余结束条件

企业代码部署从待办中移除。真实组织/账号、独立审批、业务系统的可访问 origin、跨站结果访问及事件出口仍需联调；报告执行权限仍按既有变更单等待决定。客户端正式候选签发/安装升级及原定模型与原生平台验收继续保留。

本批[脱敏证据](../evidence/flagship-optimization-20260921/enterprise-runtime-delivery-20260924.json)由同批 `candidate.json` 引用。历史报告的“企业端未部署”只描述当时状态，以本页为当前部署事实。
