# E163 客户端开发收尾与发行交接

**本轮客户端代码已冻结，停止新增功能。正式签发尚未完成。** 用户要求尽快收尾，本记录把已交付代码、待签发事项和原项目后续任务分开。

## 已完成

- 本地独立候选分支：`codex/flagship-client-0.4.0-rc.1-e163`。
- 最终源码提交：`fd02384d6de5f96a849f9d04bbfbfaff38cf6148`，独立工作树干净。共 277 项文件变更，包含客户端、对应合同、两框架适配器、实现规格与共享测试样例；没有合并、推送或发布。
- 主工作树仍在原分支与原 HEAD，保留全部已有改动；仅顺手移除一个新增 TS 文件的行尾空格，不改变行为。
- 发布构建工具从固定提交导出源码，隔离安装锁定依赖并重建前端，与内嵌页面逐字节一致；四目标发行构建通过。版本 `0.4.0-rc.1` 仅为本地候选，不代表已建立发布标签。
- 独立源码 Go 全量 **44 包通过**；发行工具 **15 项通过**。最终 Linux ARM64 程序通过新状态启动、配对、控制台与停止，以及真实 systemd setup 专项；无正式签名的 bootstrap 保持拒绝。

[本机可用体验包及说明](ux-discovery-recovery-e162-validation-20260923.md)仍有效。新的四平台固定源码构建包供发行审核，未签名 bootstrap 会按设计拒绝，不能将其当作正式安装包。

## 收尾时修正的材料问题

首次暂存完整新增文件时发现 `apps/web/src/api/onboarding.ts` 一处行尾空格，已清理；先前普通 diff 检查未覆盖未跟踪文件。首次独立候选 Go 测试还发现遗漏 `testdata/openshell-status-version.v1.json`，已追加提交补齐并重跑全量通过。初始快照、初始提交和失败日志保留，不混入最终通过结论。

初始提交 `7b6e650de58825a168bc4fb1c7ecb276074d59d1` 的产品构建内容与最终提交相同，后一提交只增加测试样例。最终候选仍重新走固定提交打包工具；实际二进制摘要以本批 evidence/candidate 引用的最终资产为准。

## 正式签发尚缺的输入

已检查当前进程：`SIQ_AGENT_SECURITY_RELEASE_SEED` 和兼容变量均未配置。未搜索磁盘中的私钥，未读取或输出密钥正文，未更换产品信任根。

用户已选择“提供现有受控签发环境”，具体脚本路径、密钥引用或执行入口尚待提供。收到入口后，按原发行流程对本候选签发、验签并执行安装/升级验收；若构建或身份漂移须停止核对，不复制历史签名。对外发布另按授权处理，不能因为本地签发就自动上传。

## 本轮与总目标的边界

本轮客户端代码及构建准备已收尾，剩余的**发行步骤**是受控签发和签名包安装/升级验收。签发不是“还要继续增加功能”的理由。

原标杆总目标仍保留未完成项：企业/研究业务端在线部署、生产 IAM 与真实业务授权、业务运行故障恢复、最终原生 CI 等；SEC-F01–F10 仍按用户要求列为后续。这些不能随着客户端冻结被标为完成，也不再追加为本轮体验候选的新功能。

[本轮证据](../evidence/flagship-optimization-20260921/client-release-freeze-e163.json) · 候选文件（本机私有路径：`var/flagship/ux-e163/candidate.json`）
