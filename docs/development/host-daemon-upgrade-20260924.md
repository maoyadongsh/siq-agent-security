# 宿主 AgentShield 候选升级与收尾状态（2026-09-24）

宿主 `127.0.0.1:47611` 已切换到已验证的新候选，原运行身份与授权继续可用。本次修复的是部署版本和 Hermes 路径配置，没有修改产品源码、扩大权限或执行业务任务。

## 原因与实际修复

旧进程缺少 `/v1/runtime-identity/self`，请求落到前端页面并返回 HTTP 200；研究端严格检查 JSON 合同后拒绝继续。现有运行身份绑定的是 `data/hermes/home/profiles/siq_analysis`，旧进程又没有携带 `HERMES_HOME`。因此只换二进制仍不足以恢复身份校验。

新版沿用原状态目录、端口、凭据与 grant，并明确设置上述已存在且与原实例 ID 匹配的 Hermes 路径。没有创建新的身份，没有替换签名密钥，没有重新签发 grant。

| 对象 | 本次读回 |
| --- | --- |
| 新进程 | PID `171047`；原进程 `3748715` 已正常退出并释放 Writer |
| 新程序 | `var/flagship/report-generation-20260924/agentshield-linux-arm64` |
| 新程序 SHA-256 | `334367d16defb46cfc7f6bebe233048f87574c9e55ecd81e2f17dfbcbb6eafea` |
| 原程序 SHA-256 | `5ec356cbdf1f01d164da26381290803621cf4fdd74b03587883a93f87f43ca58` |
| 状态 | `.tmp/flagship/as01-state-v2-20260921`；目录身份未改变 |
| 历史链 | 31 条回执，head_seq 30，验签通过；链头与升级前完全一致 |
| 当前绑定 | 默认绑定及两份 scope 绑定均 HTTP 200，身份、实例、agent、grant 匹配；三份绑定引用同一个运行身份 |
| 拒绝行为 | 匿名、无效凭据均 HTTP 401 |
| 消费端 | 研究端实际 `load` + `SelfIdentityClient.inspect` 三次严格校验通过 |

原有文件逐项 SHA-256 对比通过，只有运行 Writer 标记因进程更替改变。`runtime_state=unverified` 仍按合同返回；身份有效不等于已经证明钩子加载或完成业务执行。

## 演练与失败记录

先备份状态，在禁网、非 root、无 capabilities 的容器内按**原绝对路径**挂载副本，验证新程序启动和现有绑定。容器没有挂载真实状态目录；配置根只读挂载。旧程序也能验证新版启动后的副本签名链，且副本没有升级状态格式。

以下失败均保留：首次复制到不同路径触发目录绑定保护；初次容器探针缺 Python；未挂载原 Hermes 根的探针返回 401；首次宿主切换脚本因 PATH 中 Python 没有 `os.pidfd_open` 在停止旧进程前退出。最终用系统 Python 3.12 的 pidfd 精确停止已核对的旧进程，升级 v2 通过。这些失败没有被覆写成成功，也不是原签名链损坏。

升级前检查宿主无已建立 HTTP 连接；研究端自己的只读检查确认没有未到期 running 租约。历史 running 行保持原样，未以过期推断其远端执行已停止。

## 保留的回退材料与限制

受控目录 `var/flagship/host-daemon-upgrade-20260924/` 保存原环境、状态备份、逐文件摘要、旧程序位置与本次脚本。目录与私有材料仅当前用户可读；其中包含真实凭据，不能公开或提交。

回退时必须重新核对候选 PID/程序摘要、实际 Writer、业务请求与租约，正常停止本次候选，再使用 `original-environment.private.json` 和原程序启动相同 `serve --port 47611`。保留当前状态和新增审计，不能把旧备份直接覆盖回真实目录。旧程序不提供新身份接口，依赖它的新请求路径会重新失败关闭，因此主 API 切换后应配套回退业务执行配置。本轮只验证旧程序读取演练副本，未声称在宿主执行了新→旧→新往返。

当前仍为手动本地候选进程，未注册新服务或新增开机自启，也不是正式签名安装升级。

## 本轮结束线

已关闭：宿主版本缺失接口、原 Hermes 根未传入、新旧状态兼容性和消费端身份读回。

尚未关闭：主 API 与当前沙箱镜像切换、企业控制台真实 IAM/跨站联调、正式发行候选更新及签发安装、原生平台与原固定模型验收。主 API 18081、新预览 18089、回退 18088 和页面 15173 均健康，进程身份未变；预览继续保留执行门禁。外部检查 SEC-F01–SEC-F10 保留后续清单，不加入本轮。

本批[脱敏证据](../evidence/flagship-optimization-20260921/host-daemon-upgrade-20260924.json)由同批 `var/flagship/host-daemon-upgrade-20260924/candidate.json` 引用。原冻结发行候选未更新，不把本次宿主切换计作正式发行完成。
