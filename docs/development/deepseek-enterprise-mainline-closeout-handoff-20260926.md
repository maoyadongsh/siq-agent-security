# DeepSeek 企业主线剩余开发与最终收口——交接

对应执行记录：[deepseek-enterprise-mainline-closeout-execution-20260926.md](deepseek-enterprise-mainline-closeout-execution-20260926.md)
任务书：[deepseek-enterprise-mainline-final-closeout-taskbook-20260926.md](deepseek-enterprise-mainline-final-closeout-taskbook-20260926.md)

**本文件是滚动交接，随 R 单元推进更新。当前时点：R00–R08 已执行到「源码完成 + 隔离验证通过」的相应部分；D-1/D-2/D-3/D-5 已决策并按授权实施，D-4/D-6（部分）/D-7（部分）仍未确认；R09 全部阻塞在真实资源与授权上；**无任何实际交付**。**

状态口径：`todo / doing / blocked / verified / delivered`。`verified` 注明限定环境。**本文件不把任何 CL/ENT 改判为完成**，也不声明源码已冻结、已签发或已交付。

---

## 0. R00 时点基线（2026-09-26）

| 项 | 值 |
| --- | --- |
| HEAD / 分支 | `ebaaf3b60cc554ae67f4553aa1cfd0575a5d7ec0` / `main` |
| 工作树 | `-uall` 813 条 = 已跟踪 105（M 98 + D 7）+ 未跟踪 708；冲突 0；`scan_stable=true` |
| 冻结前盘点 | `source-freeze-preflight.py` → **exit 1 / blocked**（原因 `unreviewed_paths:813`） |
| `signed` / `installable` / `published` | 全部 `false` |
| 7 条删除 | 全部为 `apps/agentshield/internal/ui/embedded/assets/` 哈希换代产物，非功能删除；仍须在 R07/R08 由源重建并复核 |
| R00 窄基线 | control-api 导入 OK（dev 模式；生产模式按不变量 7 失败关闭）；99 项聚焦测试通过；`edge/agent` `go vet` exit 0；`apps/web` `npx tsc -b` exit 0 |
| 未执行 | 全量后端、正式模式前端构建、Edge race、迁移回放、浏览器验收、真实数据库 |

**并行作者**：本机存在其他在途会话（Codex 应用服务约 28h；另一 Claude 会话约 5.6h）。仓库内最近 3 小时无源码改动，但**未取得独占**，按 D-1 门槛处理。

---

## 1. 哪些原要求完成了源码和隔离验证、哪些实际交付、哪些仍缺？

**实际交付（delivered）：无。** 本轮无任何制品签发、无生产部署、无真实设备操作。

### 已完成源码 + 隔离验证（限定合成/隔离环境，不等于生产效果）

| 范围 | 隔离验证证据（历史，来自各责任线交接） |
| --- | --- |
| 安装计划与 CLI 编排 | `installplan` 单测、Go/Python 跨语言字节对等、`setup-enterprise` 五阶段编排 |
| 周期发现（有界调度） | 合同 + 纯时间槽函数、迁移 0028、内部预约事务、真实 PostgreSQL 同槽竞争 18 项、设备签名确认、Edge 轮询 |
| 周期归档/恢复 CLI | 24 子用例 + 底层既有 3 组，`-race` 通过 |
| 两框架原生采集合同 | OpenClaw 8/8 原生旅程；Hermes 截断缺陷修复后脚本 exit 0 |
| 角色—Skill 来源与历史对照 | 后端定向 33 项、前端 27 项、Hermes 来源面板浏览器 7 项 |
| 绑定身份复验与漂移拒绝 | 88 passed（44+44 主开发者复跑）；影响/预览一致性 70/94 passed |
| 批量治理后端链 | 预留/执行/逐项结果回读相关定向测试 |
| 审计精确查询与 OCSF 导出 | audit-wire 后端 2 + 消费者 11；OCSF 45 passed |
| 发行工具 | 候选/组包 17 passed |

### 本轮新增（2026-09-26；更新后时点，含 R09 已执行部分）

| 范围 | 证据 | 完成级别 |
| --- | --- | --- |
| 设备侧待确认周期计划只读枚举（R01） | 新增 `routers/discovery_schedule_pending.py` + 合同 `enterprise-discovery-schedule-pending-list.v1.md` + **8 用例**（7 行为 + **1 接线**，含 3 项认证拒绝、损坏行不投影、损坏行后分页仍推进）；同组 75 用例回归无退化；ruff 通过 | 源码完成 + 隔离验证通过。**已接线**（R01.5：`app/main.py:37/182` 两行；端点存在性经 `app.openapi()["paths"]` 核对，接线用例断言非 404），CLI 侧未接 |
| `snapshot-comparison/v2` 合同（R02） | 新增 `enterprise-role-skill-snapshot-comparison.v2.md`；差异经 `role_skill_sources.py:173-177`、`role_configuration_history.py:37-50`、`role_skill_roots.py:19` 逐行核对，未凭猜测 | 源码完成（文档/合同）+ 只读核对；代码零改动 |
| 证据拓扑：视角与来源边界（R03） | 新增 `app/evidence_topology.py` + 合同 `enterprise-evidence-topology.v1.md` + 10 用例（租户隔离、声明/观测冲突不合并、未归类采集器不归桶、云账号远程视角无来源、可读性原因、行为证据恒不生产）；ruff 通过 | 源码完成 + 隔离验证通过；未接线（内部投影，非端点） |
| 绑定失效联动只读投影（R04） | 新增 `app/binding_invalidation_surface.py` + 合同 `enterprise-binding-invalidation-surface.v1.md` + 7 用例（零写入、跨租户不可区分、历史记录≠当前状态、吊销前 `blocked_if_revoked`→吊销后 `blocked_now`、其它身份与过期草稿不成为"无引用"、截断/不可读降为无法判定、入参边界）；同组 94 passed；ruff 通过；版本链审计基线不变 | 源码完成 + 隔离验证通过；**未接线**（内部投影，非端点）。**未**改 `execution_confirmation_supported`，**未**引入独占性标准 |
| 版本化引用链审计工具（R06） | 新增 `contract-version-chain-audit.py` + 别名表 + 12 用例；在真实工作树实际运行：375 建档版本 / 557 发出字面量 / **断点 1 处**；ruff 与单测通过 | 源码完成 + 隔离测试通过 + 真实工作树运行一次。**未接入 CI/门禁** |
| 后端全量真实基线（R07 抢跑） | `pytest app/tests -p no:randomly` → **9 failed, 2143 passed, 1 skipped**（110s）；以 `--ignore` 排除本轮新增测试后失败集合完全一致，证明非本轮回归 | 只读观测；暴露 2 个既存缺陷（见 §5），**未修** |
| 非后端门禁首次同工作树基线（R07 抢跑②） | 前端全量 **1003 passed**；**正式构建** exit 0 且产物内开发身份命中 **0**；Edge + 12 Connector + AgentShield 全部 gofmt/vet/`-race` 绿；规则包 Python/Go **字节相同**；后端安全子集 **258 passed** | 只读观测 + 临时构建；**未**纳入迁移回放、浏览器验收、真实设备证据；另发现 2 个门禁设计缺陷（见执行记录 R07.5） |
| 统一门禁执行器（R07，首次可重复同候选机制） | 新增 `enterprise-gate-run.py` + **16 合成分支用例**（仅临时合成 git 仓，不跑真门禁）；把四类门禁设计缺陷**编码进工具**（gofmt 限跟踪文件、不带自定义 vitest reporter、后端仅全量、**Go `-count=1` + `(cached)` 即失败的断言**）；三条环境不可用门禁恒 `skipped`，**结论永不可能是"全绿"** | 源码完成 + 隔离测试通过 + **在本工作树实跑三轮**（第三轮 C：**48 passed / 0 failed / 3 skipped，`conclusion=gates_incomplete`，退出码 1**，4 分 44 秒；详见 §5）。报告独占创建（已存在即拒绝覆盖） |
| 两个既存后端缺陷修复（R07.8，D-1(a) 例外授权） | 缺陷 1：`test_deployment_preview_consistency.py` 的 `_reservations()` 不再读**不存在**的 `DeploymentSubmission.state` 列（孤立运行空通过 / 全量运行 8 failed），改为对五个真实字段做行集+内容快照；缺陷 2：`test_enrollment_flow.py::test_tasks_scoped_to_environment` 改用**用例级独立环境**，不再被 `GET /edge/v1/tasks` 的"最旧 10 条"领取语义截断（10 条上限**未动**，属既有 P1-5 语义） | 源码完成 + 隔离验证通过；后端全量 **2164 passed / 1 skipped / 0 failed** |
| 回滚授权边界只读核查（R05） | 临时探针（隔离 SQLite + dev 身份，**已删除、非交付物**）实测三种情形：非 `openshell-cli` 下绑定吊销 / 策略被取代后回滚均返回 **200** 且写 `rolled_back`；既有测试 `test_policy_flow.py:675` 在 `openshell-cli` 下为 **502 + 零写入 + 状态不变**。由此得出**依赖后端取值**的结论与最小修复规格 | 只读核查 + 复现（该核查本身未改任何既有文件） |
| 回滚活体授权链重查实施（R05.5，D-1(b) 例外授权） | 把原先**只写在 openshell-cli 分支里**的授权链判定抽成唯一函数 `_rollback_live_chain`（绑定仍 `active` 且目标未变、变更单 ∈ `{approved, effective, deploying}`、策略 ∉ 被取代/驳回/回滚），非 openshell-cli 分支改为**写前套用同一判定**：不满足 → 审计 `deployment.rollback_refused` + **409 且零状态写入**。新增 4 用例（3 条断链拒绝 + 1 条反例守卫）；反证：以临时插件复现修复前分支，3 条拒绝用例**全部 FAIL** → 非空跑 | 源码完成 + 隔离验证通过；**不是**实际交付。独立回滚审批**仍未实现**（业务决策，未自行选定） |
| 发行准备清单与快照核验（R08） | **62 条**（最近一次）本轮路径允许清单，其中 **49 条**那一版（**38 条**新增/修改的源码·工具·文档·证据 —— 其中 2 条是 R07.11 的浏览器套件与其测试、2 条是 R09.7/R09.9 的真实 HTTP 契约验收工具与其合成回归测试、4 条是 R04 独立回滚审批只读交付、**7 条**是 OpenShell 链接证据（R09.13 只读 5 条 + R09.14 前置核查 1 条 + R09.15 写入闭环留痕 1 条）、2 条是 R09.15 的 `--live` 工具与其隔离测试 + 3 条**未跟踪**合同件的追加段 + **5 条 D-1 例外授权改动的文件**，其中 4 条已跟踪、1 条未跟踪）+ `source-freeze-preflight.py` 实测（最近一次（第 13 次，R09.18 的 P0 提交 `743a745` 后）`2026-09-26T14:07:17Z`）：`requested=62 / verified=62`、`unverified/excluded/missing` 全 0、`conflicts=0`、`head=743a745`、`worktree: 100 tracked / 704 untracked`、`status_entries=804`、`signed=installable=published=false`；`conclusion=blocked`，唯一原因 `unreviewed_paths:804`（**全部**是并行作者线产物，**不可由执行者代为判断**）。该次同口径相减 = 新增 0 / 消失 16（逐条等于 `743a745` 的路径）；**清单内 62 条现已逐条复核在 HEAD 树中**（`git ls-tree -r HEAD` 命中 62/62、0 缺失）。**清单由 46 增至 49**：R09.17b 追加 3 条（只读闭包核验工具 + 其 10 条合成用例 + `08-` 留痕）——**第四节的分条拆解写的是 43 条那一版**，两次数字都在 §7 引言里。**清单由 49 增至 62**（R09.18 追加 **13** 条 = 6 条新增源码/测试 + 1 条 `09-` 留痕 + 1 条行为 fixture 方案文档 + **5 条已跟踪被修改**的适配器合同件，后者是**合同改动**、必须复核）。十三次核验的完整数字、逐条归属与口径说明见 §7 引言 | **仅准备与核验**，不是冻结、不是发布授权；D-7 逐项授权请求已列（见 §7） |
| 可选数据库门禁（R07.10，恒 `skipped` → 可执行） | 新增 `--enable-ephemeral-postgres-gate`：**默认连只读探测都不做**；启用后才探测 docker 与镜像（**绝不自动拉取**），并对取证报告做 6 项断言（`passed is True` / `ephemeral_database is True` / `production_identity_tested is False` / `checks` 非空 / `script_sha256` 与当前脚本一致）；启用但不可跑时降级为**实测原因**的非绿 `skipped`，不冒充通过。新增 9 条合成用例（共 **25 passed**）；ruff 仅余 5 条既存项（1 `UP017` + 4 `E731`，均不在改动行；**后续：R09.10 已把本轮这 5 条清零，既有件 `source-freeze-preflight.py` 仍余 1 条且刻意未改**） | 源码完成 + 隔离测试通过；**该分支已真实执行**（见下一行） |
| **A1：真实 PostgreSQL 迁移回放与部署竞态（R09.4(8)，§3.2 许可已获批）** | 在**一次性、回环、无卷**容器（镜像本机已有 `sha256:ff800890…`，**未拉取**）上实跑 3 次：直接运行 `{"passed": true, "checks": 18}` 7.0s、`migrated_head=0028`；统一门禁全跑 → **49 passed / 0 failed / 0 blocked / 2 skipped**（`gates_incomplete`，4m49s；后端 2164 passed / 1 skipped）；定向 opt-in → 门禁 `passed` 且 `post_check` 18 项与 `script_sha256` 一致。**回收三项核验全绿**：零残留容器/卷/网络、镜像 ID 字节不变、宿主容器集合 `diff` **为空（45 项）**、网关与工作台端口映射未变、目录 `700`、无凭据泄漏 | **隔离验证通过**（`verified`）。**不是**实际交付、**不是**已签候选、**不**产生 `enforcement_verified`、**不**关闭任何 ENT。**同一候选存疑**：取证脚本是并作者未提交改动、不在 26 条允许清单内（见 §7 第 11 项） |
| **A2：30 个浏览器验收脚本（R09.5，另有许可但未使用）** | 实跑 **25/30 通过**（`four-entry-navigation` 24/24、`overview-four-entry` 24/24、`runtime-binding-explorer` 45/45，其余按自述 JSON 累计 199 项 `checks`）。**5 项失败全部落在 UI 可见性/文本断言**，逐条归因于并作者未提交的 `apps/web` 改动（本轮三个提交路径含 **0 条** `apps/web`；该目录有 **122 条**未提交改动，恰含失败点组件）。**实测更正**：28/30 **不需要任何运行中的控制面**（自带 `VITE_DEV_MODE` 模拟构建 + `127.0.0.1` 静态服务 + 路由 mock），2 个需原生 Edge/连接器；因此"一次性服务实例"许可**未被使用** | **隔离验证（部分）**：证明"模拟身份 + mock API 下的前端交互"。**不**证明真实后端 HTTP 契约、**不**证明真实 IAM（脚本自述 `scope: mocked browser only`、`production_deployed: false`）；5 项失败**不加 skip、不记为通过** |
| 浏览器验收**套件与可选门禁**（R07.11 / R07.11b） | 新增 `run-browser-smoke-suite.py`（**实测 21 条用例**，含 R07.11b 新增 3 条）与其门禁接线（**33 条用例**）：参数**从每个脚本自己的 `--help` 读出**、缺原生二进制记 `blocked`、证据附 6 项摘要断言、**失败即失败**；R07.11b 起套件**按 `SIQ_AS_DEV` 逐个度量** `real_dev_api_scripts`（实测 **8/30**）并写进 `result.json`，门禁读不到该字段即判 failed。实跑 E2/E4：**25 passed / 5 failed / 0 blocked（of 30）**，与 A2 独立测得同组数字 | 源码完成 + 隔离测试通过 + 真实执行两次（定向）。**默认关闭**（显式 `--enable-browser-smoke-gate` 才跑）。**边界**：套件**本身不做契约级断言**——该层由 **R09.8** 以独立工具补齐（见下行），两者不重叠 |
| **真实 HTTP 契约级验收（R09.7/R09.8，§3.2 许可：批准并执行完整 R09.7）** | 新增 `http-contract-acceptance.py`（**未接门禁**：无 `--enable-*` 开关，不参与 8 类门禁）。在**一次性回环 dev 控制面**（零容器、零新依赖、临时 SQLite、合成 `X-Dev-*`）上实跑：**83 条判据全部成立**（`contracts_held`，exit 0）——404-先于-403 且**同正文不可区分**、401/403/404/409/422 全族 + 角色矩阵、**409/404 失败路径不写审计（1→1）**、跨租户按 `resource_id` **0 行**、分页/计数头按**已声明**合同（total 全量口径 4、翻页仍 4、`limit=999`→200、租户乙 total=1、逐页不重不漏、cursor 无 limit→422、跨租户 cursor 同 422 同 detail、`Cache-Control: no-store`）、**三类真秘密材料在四个面无命中**、4 类启动 fail-closed 拒绝。**回收三项**（进程退出／端口重新 bind 成功／临时目录消失）全 true，事后无 `/tmp/siq-http-acceptance-*` 残留。**首次运行 2 条红是探针前提写错**（`config.py:78` 的默认值语义），按代码行更正后复跑全绿；**两份报告都留档** | **隔离验证通过**（合成身份、dev 模式、回环、临时库）。**不产生 `enforcement_verified`**、**不覆盖** A2 的 5 项前端 UI 失败、**无纵向基线**故**不构成"不退化"证明**；只核对**已声明**项，未发明新合同 |
| **验收工具自身的合成回归守卫（R09.9）** | 新增 `test_http_contract_acceptance.py`：**16 条**用例，**不启服务、不发真实 HTTP、不依赖 playwright**，专钉"工具自己报假绿"的四个面 —— ① 判定聚合（任一条判据不成立 → 该组 `failed` 且整体 `contracts_failed`；全成立是通往 `contracts_held` 的**唯一**路径；`run_error` 压过一切绿）；② 回收核验不许把"没测到"当"成立"（`process_exited=None` / `port_released=False` 必须**判红**）；③ 秘密扫描**必须能抓到**植入的哨兵（工作目录文件 / 一次性库 / 日志），`signing.seed` 是输入材料故排除在扫描外；④ 启动环境是**白名单**（`PATH/LANG/LC_ALL/TZ` + 6 个 `SIQ_AS_*` + `HOME`）、结论→退出码映射稳定、证据目录已存在时**拒绝覆盖且不触碰原目录** | 源码完成 + 隔离测试通过（`16 passed`，0.16s；`ruff` 仓库口径 `All checks passed!`）。**没有为了测试通过而改动被测工具**；测试自身两次红（`UP037`、`importlib` 未登记 `sys.modules` 导致被加载文件的 `@dataclass` 装饰期抛错）均修在测试侧，如实留痕于执行记录 R09.9。**不产生 `enforcement_verified`**、不参与任何门禁 |
| **本轮工具层的同候选回归 + lint 残留清零（R09.10）** | 按**仓库口径**（`ruff --config apps/control-api/pyproject.toml`，line-length 120，`E,F,W,I,UP,B`）对本轮 8 条工具路径复核，残留 6 条中 **5 条落在本轮路径**（`enterprise-gate-run.py` 1 条 `UP017`、`test_enterprise_gate_run.py` 4 条 `E731`），已做**行为等价**修正（`datetime.UTC` 别名；测试内 4 处 lambda 桩改局部 `def`）。**既有件** `source-freeze-preflight.py:291` 的 1 条 `UP017` **刻意未改**（不在允许清单内）。验证三层：本轮路径 `ruff` → `All checks passed!`；`pytest scripts/enterprise-experience` → **92 passed**（5 个工具测试文件）；改过的执行器**真实运行一次** `--only rulepack_python_go_identity,contract_version_chain` → 两条 `passed`，报告 `/tmp/gate-R099-rulepack-20260926T175635.json`（`0.9s`，时间戳格式与 `head_sha` 均正确，结论 `partial_run_not_a_gate`） | 源码完成 + 隔离测试通过 + 一次真实门禁运行。**边界**：这是**工具层**横向回归，**不是**纵向「不退化」证明（无改前基线），不覆盖后端/前端全量，**更不覆盖** A2 的 5 项前端 UI 失败 |
| **留证断言在真实语料上的演练（R09.11）** | 新增 `--validate-browser-evidence <result.json>` **纯核对模式**（只读；不跑套件/不构建/不写报告；不短路，逐条打印 9 条断言；退出码 0 仅表示「留证自述自洽」，措辞恒为 `verdict=evidence_assertions_held`，**从不出现 `gates_green`**）。在**两份既有真实留证**上实测：R07.11b 之后那份 **8/9 成立**（唯一不成立 = `suite_passed`，即 25/30）；R07.11b 之前那份 **6/9**，`suite_digest_matches` 与 `real_dev_api_measured` 被**真实抓出**（负对照，证明检查非空跑）。**未新增任何路径**（当时清单仍 30 条，其后由 R04.6/R09.13 增至 39 条） | 源码完成 + 隔离测试通过（**41 passed**，新增 8 条）+ **真实语料演练**。**边界**：不使门禁变绿、未演练门禁自身的通过路径、非「不退化」证明、不产生 `enforcement_verified` |
| **受控目标被指定 → 智能分析助手 OpenShell 链接只读实跑（R09.13）** | 使用者指定目标 = `siq-research-engine` 的**智能分析助手**（`siq_analysis`）OpenShell 网关。三件只读实测：① `SIQ_AS_OPENSHELL_ENV_SH=…/scripts/openshell/env.sh` + `openshell_compat_check.py` → **PASS / exit 0**，版本 `v0.0.83`、矩阵 **8/8 一致**；② 对方 CLI `sandbox list`/`policy get` → 网关 `siq-openshell-dev` 在 `127.0.0.1:17671`（+`172.23.0.1`）LISTEN，**两个 `siq-analysis-canary-*` 沙箱均 `Ready`**（2026-09-21），各 `version=2`、`policy_source=sandbox`，网关自报 `status:"effective"`；③ `status --gateway-endpoint https://127.0.0.1:17671`（**TLS 校验开启**）→ `Connected` / `0.0.83`。证据目录 `docs/evidence/agentshield/openshell-siq-analysis-2026-09-26/`（README + 4 份原始输出） | **只读链接成立**（源码零改动、对方仓库零改动、**未启停网关、未创建/删除沙箱、未 `policy set`**）。**不是**强制点生效证据、**不产生** `enforcement_verified`；`status:"effective"` 是**网关自报字段**，不当作我方授权事实。**新发现（同候选退化）**：E149 的只读预览工具按自身文档同一条命令跑同一目标 → **rc=1 / 409 `deployment_target_authority_unverified`**，且失败发生在 `policy get` **之前**；根因是未跟踪新模块 `app/target_authority.py`（2026-09-25）强制算子授权文件，而该工具把 `os.environ` 清成白名单、**构造上不可能**提供它 —— **E149 的 8/8 在当前候选已不可复现**（详见执行记录 R09.13；相关四条路径**均不在**允许清单内，本轮不改） |

| **自查：分支自身不自洽 + 清单第 5/6/7 次核验（R08.7/R08.8）** | ① 允许清单 **30 → 43**（+4 条 R04 只读交付、+7 条 OpenShell 链接证据、+2 条 R09.15 的 `--live` 工具与其隔离测试），最近一次（第 7 次，`--live` 闭环落盘后）`source-freeze-preflight.py` 实测 `requested=43 / verified=43`、`unverified/excluded/missing` 全 0、`scan_stable=true`、`conflicts=0`、`head=6f04c15`、`worktree 105 tracked / 706 untracked`、`status_entries=811`、`conclusion=blocked`（唯一原因仍是 `unreviewed_paths:806`）；三次逐条相减分别为**新增 12 / 消失 0**（39 条那次）、**新增 1 / 消失 11**（40 条那次）、**新增 3 / 消失 1**（43 条那次），后两次三项计数互相闭合。② **抓出并修掉一处自伤**：R09.7 的验收工具 `http-contract-acceptance.py` **从未入库**（`git ls-tree -r HEAD` 命中 0），而其合成测试**已**在 `6516e58` 入库 → 分支 HEAD 干净检出上该测试**收集期即失败**（`FileNotFoundError`，exit 2，用 `git archive` 导出复现，未动共享工作树）；已并入提交，修复后同法复核 **16 passed** | 核验通过仅代表**只读快照完整**，**不代表清单内每条都已入库**；**不**使分支变为"已冻结/可发布"，`signed/installable/published` 仍全 `false` |
| **A7 前置核查：`--live` 获批但拒绝执行（R09.14）** | 使用者批准了对 canary 的真实写入；动手前先核查，发现**批准所依据的两条描述都不成立**：① `cli_backend.py:584-591` 对 `network_policies` 是**整段替换**，写入后该 canary 的 **8 条** provider/内部服务策略会在窗口内**全部消失**（不是我原先写的"多出一条 endpoint"）；② 脚本 `:139` 调 `rollback(sandbox, receipt)` **不传 authorizer**，`cli_backend.py:807-808` 必抛 `openshell_rollback_authorizer_required`（隔离实测复现）→ **"同一 run 内回滚"作为回收手段不成立**，执行后沙箱会被留在"网络策略只剩 `example.com:443`"。故**拒绝执行**，只做只读核查并落盘 `05-live-prereq-blocked.txt`（含脚本全文） | **未执行任何 `policy set`**；对目标只有只读 `policy get/list`、`sandbox list`。BEFORE 全文已留档作**未使用**的恢复材料。三条备选路径（补 authorizer 再跑 / 换一次性专用沙箱 / 停在只读）**待使用者定夺**。**后续：使用者选定路径①，见下一行（R09.15）** |
| **A7 路径①落地：补 authorizer 后 `--live` 真实闭环已执行（R09.15）** | 使用者第二轮答复选定**路径①（先补 authorizer 再跑）**。改动 2 个路径（均在本仓 `scripts/`，**对方仓库零改动**）：`openshell_compat_check.py`（**已跟踪件被修改**：`_live_check` 补 `RollbackAuthorization`——**只从私有操作记录构造**、只认本次 `operation_id`+`target`，`finally` 必回滚，回滚后重读并要求 digest 等于 BEFORE，私有基线与 BEFORE 不一致时**只告警不拒绝回滚**）+ 新增 `test_openshell_compat_live.py`（8 条隔离单测，**负对照**指向改动前版本时 5/7 失败）。`apply_dynamic` 的**整段替换语义刻意未改**（产品行为），改为运行时先打印风险告知。**执行中暴露第三条缺陷**：fixture 缺 `binary_paths` → `policy_safety.validate_network_rules` 在 `compile()` 阶段抛 `openshell_network_binary_required`，**首跑一个字节都没写**（即该路径此前**从未真正写成功过**）；补 `_LIVE_FIXTURE_BINARY` 后重跑成功 | **真实执行结果（退出码 0）**：`BEFORE revision=2 / 7 条归一化网络规则` → `policy set 成功 revision 3` → `读回通过（level=readback_verified）` → `已回滚 revision 4（result=restored）` → `回滚后读回 digest 与 BEFORE 相同`。**独立复核**（对方 CLI 只读、本轮重跑可复现）：网关 `Connected` / `0.0.83`；`policy list` v4 `fed6cc8072d1` **Loaded**、v3 `dfdefc3465ef`（本次写入，此前不存在）、v2 亦为 `fed6cc8072d1`；`--rev 2 --full` vs `--rev 4 --full` 差异**只有头部 4 个元数据字段**，去头后载荷 SHA-256 相同 ⇒ **逐字节复原**。**顺带把 R09.14 更正 1 由读代码升级为实测**（`--rev 3` 的 `network_policies` 只剩写入那一条）。**天花板：`readback_verified`，未产生 `enforcement_verified`；未做任何真实 deny 观测**。终态：策略恢复原状，v3 作为可审计历史修订留在网关；未创建/删除沙箱、未启停网关。**不使任何门禁变绿、不关闭 A7** |
| **把 §五 那条结构性失败修掉：预览工具改从算子授权目录读身份 id（R09.16，使用者「授权我修预览工具」+「改从算子目录读身份 id」）** | 改动 2 个路径（均在本仓 `scripts/enterprise-experience/`）：`openshell-preview-live-check.py`（**已跟踪件被修改**）新增**必填** `--target-authority`（绝对路径、非符号链接、常规文件才收；真正的大小/属主/权限/硬链接/重复键判定**故意复用门禁自己的** `_read_authority_bytes`/`_unique_object`/`TargetAuthority`，符号改名即 `ImportError` 响亮失败，不会静默放宽）与 `--assignment-id`；子进程注入 `SIQ_AS_OPENSHELL_TARGET_AUTHORITY_FILE`；探测后用 `caps.endpoint_fingerprint` 与 `sha256(caps.handshake_gateway)` 对目录条目做**精确元组比对**，不符即**有界失败留痕**（`write_failure()`：只写固定判别码 + 实测摘要，不写路径/策略正文/证书/凭据，恒 `production_eligible=false`、`runtime_policy_mutated=false`、`enforcement_verified=false`）；`tenant_id` 与 Environment/AgentAsset/AgentInstance 的 id **全部取自算子声明的目录条目**（运行时生成的 id 构造上过不了门禁）。新增 `test_openshell_preview_live_check.py`（**5 条**隔离用例，模块级合成 `openshell` CLI（`0o755`、inode/mtime 固定以便指纹可比）+ XDG 沙盒） | **合成闭环 10/10 + 负对照 + 5 passed**。实跑 ①（错指纹目录）`rc=1 / authority_endpoint_mismatch`，两个摘要**都打出来**；实跑 ②（正确目录）`rc=0`、`checks` 10 条全真（含新增 `authority_tuple_matches_live_gateway`、`operator_declared_identity_used`）、`authority_sha256=090c046f…`、`command_count=12`、`revision 4→4`、两侧 `preview_digest` 相同（`9b5e2def…`）；**负对照**：把改动前版本放在 `<repo>/tmp-negctl/inner/` 同 bench 同参数跑 → `line 136 AssertionError: ('/deployment-preview', 409)`、`rc=1`，与真实网关那次留痕同址同码（证明修复确实解掉了那条失败，不是 bench 变宽松）。**一处如实降级**：`changed_context_refused_without_writes` 取代原 `deployment_preview_changed` 断言——上下文一变调用指纹即变、`require_target_authority` **先于**摘要比对失败，故实测判别码是 `deployment_target_authority_unverified`（`deployment_preview.py:252` 在本路径**不可达**）；工具只钉「被拒 + 零写入」，并把实际码记进 `changed_context_detail`，**不假装机制没变**。`ruff` 仓库口径 `All checks passed!`；`pytest scripts/enterprise-experience` → **113 passed**；留痕 `docs/evidence/agentshield/openshell-siq-analysis-2026-09-26/07-preview-tool-operator-authority-2026-09-26.txt` | 源码完成 + 隔离验证通过（**全合成件**：无真实网关、无真实身份、无真实策略写入；`real_gateway: true` 是工具常量**不是测出来的**）。**不产生** `enforcement_verified`；**不**使任何门禁变绿；E149 的 8/8 **仍不可逐字复现**（见下行 R09.17 与执行记录 R09.13/R09.16） |
| **可启动性缺口：本分支 HEAD 检出无法 `import app.main`（R09.17，本轮自查发现）** | 用 `git archive HEAD … \| tar -x` 导出（**未动共享工作树**）后 `python -c "import app.main"` → **`ImportError: cannot import name 'DiscoveryScheduleRecord' from 'app.models'`**。定位到**本轮提交 `6ba1f7c`**（26 文件、+4811/-91）：该提交把 `app/main.py` 重写成「导入六个路由」，但**只入库了 `discovery_schedule_pending.py`**，其余路由模块与 `app/models.py` 的配套改动都还在工作树里未提交。补足后 **25 条路径**即可导入；对照：`app/routers/` 在 HEAD **15 个**、在工作树 **35 个**。**R09.17b 把这一步做成可重复的只读核验**：新增 `import-closure-check.py`（**未接线**、不参与门禁）+ 10 条合成回归用例，对真实仓库实跑出一张逐 ref 表——`HEAD`(=`033f50d`)/`6ba1f7c`/`ad3116e` **全部 `import_closure_open`、补件 25 条且逐条相同**（缺口生于分支**第一个**提交 `6ba1f7c`，此后每个提交都带着它、从未恶化）；负对照 **`ebaaf3b`（父提交/`main`）`import_closure_closed_at_ref`、0 补件、退出码 0** ⇒ 不是"无论给什么 ref 都报缺口"的假探针。**构成由工具逐条更正**（早期手工目测的「1 条已跟踪被改 + 24 条未跟踪」不准确）：**3 条已跟踪被修改**（`app/models.py` `+252/-3` 含既有表约束变更；`app/routers/deployment_preview.py` `+109/-5`；`app/routers/deployment_submission.py` `+39/-16`——后两份**在 HEAD 里存在**、只是工作树版本更新）+ **22 条未跟踪**（20 个 `app/routers/*.py` + 2 个 `app/adapters/openshell/*`）；**总数 25 不变**。全部是并作者在飞的工作，**不得扫入**。 | **只读诊断 + 可重复核验**（`git archive` 导出，零改动；工具与 10 条用例见 R09.17b，留痕 `08-import-closure-check-2026-09-26.txt`）。**结论**：R08 的冻结允许清单只核了**稳定性与冲突**，**没核「导入闭包」**；此前「`git archive HEAD` 干净通过」的说法**只在各自核过的制品导入面内成立**。三条后果：① 分支当前 HEAD **不能作为一个可启动候选**（故 §7 发布清单首位是它）；② 不修则任何「在 HEAD 上重跑」的验收都**只能走工作树**；③ 修它需要把并作者的 25 条未提交路径**并入或另建分支**——**使用者 2026-09-26 就 D-8 选定 C：只诊断不修 + 只读核验**（见 §8）。**天花板**：只证明"导入面是否自足"，**未**证明补入后能启动/迁移/收集测试，**未**证明这 25 条"应该"并入，不产生 `enforcement_verified`；结论**绑定 ref sha** |

| **行为 fixture 通道 P0（R09.18，方案先提交后实施；F-1 批准 P0）** | 新增 6 条路径（**未接线**）：`enforcement_probe.py`（证据结构 + 校验器，纯逻辑无 IO，25 个固定判别码）、`probe_channel.py`（边界内观测通道：固定子命令模板、fail-closed 解析、异常→词表**闭合**映射）、`probe/enforcement_probe_agent.py`（边界内探针，**只报事实**、无 `--expect`）、`openshell-enforcement-probe.py`（一次性工具，默认计划模式）、`test_enforcement_probe.py`（**59**）+ `test_openshell_enforcement_probe.py`（**27**）。合同最小改动 5 个**已跟踪**件：能力位 `CAP_ENFORCEMENT_PROBE`、`VerificationReport` 两个字段、抽象 `verify()` 的**关键字**参数 `probe_evidence=None`、`cli_backend` 升级分支（唯一能产出该级别处）；Fake/HTTP **明确忽略**该参数。三条不变量落成可测事实：原点白名单**拒绝式**（6 个非法原点逐个点名）、判定不在夹具里、不确定就不升级。**实施期自纠三缺陷**：① 初稿要求"模式 == block"，但 CLI 读回恒填 `unknown`（P1-11）⇒ 该门**永远打不开**（纸面门禁），改为**矛盾时从严**；② 计划 argv 与实际 dispatch 分叉（写死 `--timeout 0` vs 实发 `+60`），改为复用通道构建器并加逐字相等断言；③ 拒绝路径原先已先 `upload`，改为只读前提前置、拒绝时**零跨边界动作**。`ruff` 仓库口径 `All checks passed!`；**后端全量 2235 用例 / 0 failed / 0 errors / 1 skipped**（exit 0，`--junitxml` 计数；与 R07.8 的 2164 passed **非同一次测量**，差值含并作者未提交测试）；`pytest scripts/enterprise-experience` **150 passed**（+27）；计划模式冒烟 `probe_plan_only`（握手/读回/dispatch 各 0） | **隔离验证通过**，**不是实际交付**。工具**从未**在真实目标上运行过：未联系任何网关、未 `upload`/`exec`/`policy set`；唯一真实 IO 是两个本机回环套接字。**未实测的前提**：网关是否**按 binary 路径**归因网络规则——兼容矩阵（v0.0.83）**无一条**记录说它被实测过（`interceptor: false`），即 P1-pre，**需单独许可**（含两类本轮从未做过的动作）；在此之前主方案与退路**都不能**当作已成立。留痕 `09-p0-enforcement-probe-2026-09-26.txt` |

历史基线（R00 时点）与上面两行不相加；上一节各行为历史结果，未在本轮重跑。

### 仍缺（本章是 R00 余项表，详见执行记录 R00.7 与 R00.7a）

- **R01**：新设备注册后取得组织周期意图的衔接（`setup_enterprise_linux.go:229-233` 仅限已注册身份）——**设备侧可发现性本轮已补且已接线**（R01.5）；组织侧创建时序未改变；`initial_scan.go` 成功后取消检查；设备侧 CLI 仍无"列出我的待办"入口。
- **R02**：无「精确安装 / 实际加载」关系；目录解析覆盖窄（`external_dirs` 与受信任项目来源不在覆盖集合）；`snapshot-comparison.v2` 合同文件缺失——**本轮已补**。
- **R03**：无宿主/容器/远程视角与不可读原因投影——**本轮已补源码**（`evidence_topology.py`，未接线为端点）；`enforcement_verified` **不是缺生产者而是刻意保留值**（见执行记录 R03.3），只能由 R09 真实行为 fixture 产生。
- **R04**：「失效联动」的只读一半**本轮已补**（`binding_invalidation_surface.py` + 合同 + 7 用例，堵住"吊销后果事前无从核对"）；**仍缺**：五维证据无权威生产者（R09 资源）、沙箱 revision 与 Skill 摘要未进入绑定身份快照（需改既有 `binding_identity.py` 6 字段快照及 4 处调用方，D-1 冻结）、共享独占标准未定（D-3 保持 `unknown`）、TOCTOU 窗口未消除。
- **R05**：执行触发未接线（受 §4 前置约束）；行为核验 `expect_deny` 恒空。**回滚审批/过期边界**：核查完成到可复现粒度（执行记录 R05.4），**其中"非 openshell-cli 分支无重查"这一半已按 D-1(b) 实施**（R05.5：两条分支共用同一处活体授权链判定，断链 → 409 + 零写入，4 条新用例 + 反证）；**独立回滚审批在两条分支上仍都不存在**（业务决策，未自行选定）。
- **R06**：完整版本化引用链未闭合——**本轮已把"是否闭合"变成可核对结论**：工具实测企业主线相关断点已全部闭合（2 处跨文件名建档已补导航段与显式别名），**剩余 1 处**为本地 agentshield 控制台历史兼容读取 `local-admin-session/v1`，属该子系统自身决策，已建议移交；**保留治理仍为零**（`Tenant.retention_days` 为声明值、无任何运行时读取点、无 DELETE/purge 端点，D-5 已决定不做）；**导出截断标记仍缺失**。
- **R07**：**已有**统一门禁执行器并把四类设计缺陷编码进工具（见上表）；旧 wire 样本仍须重生成。**工作树后端全量已转绿**（R07.8 按 D-1(a) 修两个既存缺陷后实测 **2164 passed / 1 skipped / 0 failed**；修复前 9 failed），故 `backend_full_suite` 门禁不再恒红；但 `conclusion` **仍不可能是"全绿"**——3 条声明不可用门禁恒 `skipped`，预期收敛到 `gates_incomplete`。详见 §5 与执行记录 R07.6/R07.7/R07.8。
- **R08**：源码**未冻结**、未签发。**已提交到新分支 `deepseek/enterprise-mainline-closeout-20260926`（提交 `6ba1f7c`，父 `ebaaf3b`；`main` 不动、未推送）**。**本轮已把"待授权"细化到逐文件逐门槛**：26 条本轮路径已核验快照、允许清单可审阅、D-7 六个子请求（提交/推送/并作者归属/既有件例外/签发/部署）逐项列出选项与影响（见 §7；其中 D-7.1 提交与 D-7.4 保留追加段已获答复）。**冻结无法由执行者自行完成**——preflight 的唯一阻塞 `unreviewed_paths:806` 指向并行作者线产物。另登记一处**超出 D-1 字面范围的偏差**：三个合同件（**实测未纳入版本控制、身份归属未确认**，非"已跟踪既有件"）末尾被追加版本导航段（R02 缺口修复），**已决定保留**。**提交的粒度说明见 §7**：Git 按文件暂存，因此三个**已跟踪**文件里非本轮的 hunk 会一并进入快照。
- **R09**：无真实设备/身份/原生平台证据。**本轮已把 R09 拆成 10 个具体项 A1–A10**（每项写清证明什么、缺哪个资源/授权、获准后的确切命令、通过判据），并附三条硬约束（`enforcement_verified` 只能由受控探针真实产生；全链须同一候选；缺资源列 blocked 不降级）。见执行记录 R09.1 / R09.2。**其中两项已执行**：**A1**（真实 PostgreSQL 迁移回放，获批后跑通，`verified`，但取证脚本属并作者未提交改动 → 暂非同一候选）；**A2**（30 个浏览器验收，**25/30 通过**，5 项失败归因并作者前端线，且实测**不需要控制面**）。**A3–A10 仍 blocked**（真实设备/原生平台/受控 OpenShell 探针等；`enforcement_verified` 仍**无生产者**，只能由 A7 真实产生）。

---

## 2. 普通用户安装后还必须手工做什么？每个动作是安全确认还是仍未自动化？

| 步骤 | 当前状态 | 性质 |
| --- | --- | --- |
| 准备安装计划文件（`--plan`） | 需管理员/受控配置提供；`install-options` 依赖 `SIQ_AS_INSTALL_CATALOG_FILE`，仓库内无 catalog 文件 → 未配置即 503 | **仍未自动化**（部署配置缺口） |
| 采集范围确认（`--interactive` 或精确摘要） | 已自动化，默认取消、需显式 yes | **安全确认**（应保留） |
| 注册（register）/ 丢响应恢复（recover） | 已由 `setup-enterprise` 编排；但自动恢复协议未实现，仅保守 `registration-pending.json` 阻断 + 人工处理 | 部分自动化 |
| 周期发现同意 | **仍需人工传 `--schedule-id ID`**（CLI 侧未接新枚举端点）；服务端已具备设备侧只读枚举 `GET /edge/v1/discovery-schedules`，但**未接线**（`app/main.py` 零改动），且仅对已注册身份可用 | **安全确认（必须保留）**；可发现性已补源码但未生效，组织侧创建时序未改变 |
| 服务安装与启动（`--start`） | 已编排；`--start` 触发 `systemctl --user enable --now`，未处理 linger/会话驻留 | 部分自动化 |
| 首扫 / 周期扫描 | serve 已接线首扫心跳与周期心跳 | 已自动化（源码层），原生生命周期留 R09 |
| 撤销周期计划 | 需管理员在 Web 管理面板撤销；设备侧无自动换代 | 部分自动化 |

**关键结论**：安装范围同意与周期同意已正确分离，设备不能自行扩大 roots/include、周期或预算——该安全边界成立且必须保留。

---

## 3. 哪些关系有独立证据，哪些只有声明/候选/历史观察？

| 关系 | 证据等级 |
| --- | --- |
| 设备 → 框架实例（配置来源） | **有独立证据**（签名的 framework-source v1/v2 入库校验） |
| 角色 → Skill 声明选择 | **有独立证据**（OpenClaw skill-selection/v1；Hermes 无对应语义，`toolsets` 不是 Skill 安装关系） |
| 角色 → Skill 目录候选 | **候选**（`ancestor_sha256` 包含声明根摘要，`historical_source_match`） |
| 角色 → Skill **实际加载 / 运行归属** | **无来源**（`runtime_status=unverified`、`effective_permissions=null` 恒成立） |
| 运行绑定 → 沙箱 revision / Skill 摘要 | **未实现**（绑定快照仅 6 字段，不含这两项） |
| 共享运行时占用 | **恒为 `unknown`**（硬编码，无来源；D-3 已决定不猜） |
| 绑定 → 引用它的已登记对象 | **有记录级证据但覆盖不完整**（本轮新增：硬引用取 `Deployment.runtime_binding_id` 外键；软引用只扫调用方自身存活草稿，其它身份不可枚举、过期草稿不扫、扫满/不可读一律降为"无法判定"） |
| 绑定吊销 → 实际生效的拒绝 | **仅使用点可判定**（吊销端点不做依赖检查，拒绝只来自预览/执行侧复验的固定码 `binding_revoked`；**吊销不是回滚**） |
| 设备型号（DGX 等） | 用户标签；主机识别只有 `DMI product_name` / device-tree `model` 等名称级信息，**名称不构成型号识别** |
| 部署配置读回 | **有独立证据**（`readback_verified`） |
| 部署**行为**（allow/deny 实测） | `expect_deny` 恒空，**无生产者**；E151 的 200/403 探针属历史真实验证，不构成本轮候选证据 |
| 授权过期/吊销 → 回滚被拒 | **不依赖后端取值了（R05.5 之后）**：两条分支共用同一处活体授权链判定。`openshell-cli`：写前重查 + 既有测试断言吊销后 **502** 且零后端写入、状态不变（**有独立证据**）。其它取值：授权链断裂 → **409 `rollback_authorization_expired` + 零状态写入 + 拒绝审计**（3 条断链拒绝用例，且经反证确认非空跑）。此前该行为**取决于后端取值**（实测吊销/被取代后仍写 `rolled_back`），见执行记录 R05.4/R05.5 |
| 回滚 → 独立审批 | **无**：回滚与部署共用 `policy:manage`，两条分支都没有独立审批记录或职责分离 |
| 人工登记 / attestation 作为归属证明 | **不成立**（合同明确不升格） |
| 迁移可回放 / 部署竞态（真实引擎） | **有隔离实测证据（新，2026-09-26）**：A1 在一次性回环 PostgreSQL 上 18 项检查通过、`migrated_head=0028`。**但非同一候选证据**——取证脚本是并作者未提交改动、不在允许清单内。**仍无生产环境证据** |
| 前端交互（模拟身份） | **有隔离实测证据（新，2026-09-26）**：A2 30 个脚本 **25 通过 / 5 失败**。**边界**：共享夹具为 `mocked browser only`；但其中 **8 个脚本自起真实回环 dev 控制面**（真实 socket + 真实 HTTP），且 **5 项失败全部落在这 8 个之内**——E1 留存证据里出现服务端生成的 `cr_…`/`pol_…` id，说明失败发生在**真实 HTTP 往返之后**的 UI 断言层。**仍不是契约验收**（套件不做状态码/JSON 形状/隔离序断言），**不是真实 IAM** |
| 真实 HTTP **契约级**性质（状态码族、JSON 形状、404-先于-403 隔离序、无秘密回显、审计-状态同事务、fail-closed 启动拒绝） | **有隔离实测证据（新，2026-09-26）**：R09.8 用一次性回环 dev 控制面 + 真实 HTTP 逐条核对**已声明**的合同，**83 条判据全部成立**（`contracts_held`），含 404-先于-403 的**同正文不可区分**、409/404 **不留审计**、分页与计数头的声明语义、三类真秘密材料在**四个面**（响应/审计/服务日志/临时库文件）零命中、4 类启动 fail-closed 拒绝。**边界**：身份是合成 `X-Dev-*`（**不是真实 IAM**）、dev 模式、一次性临时 SQLite、只绑回环、零容器，**不产生 `enforcement_verified`**；且**不覆盖** A2 那 5 项前端 UI 失败（不驱动浏览器），**不能**用它给 A2 判绿。见 §7 第 14 项 |

---

## 4. 哪些权限可真实约束到何种粒度，哪些不能？哪些有行为验证？

依据 `enterprise-permission-coverage-closeout.md`（16 行矩阵，JSON 一一对应）：

| 域 / 操作 | 状态 |
| --- | --- |
| 网络 / 查看事实 | `view`（`GET /permissions` + PermissionsPage；effective 仅 `openshell_sync`，限定 effective 部署目标） |
| 网络 / 单策略收窄撤销 | `partial`（申请→独立审批→预检→执行→读回已有） |
| 网络 / 跨策略批量撤销 | `partial`（后端链已有；**前端执行入口无调用方**） |
| 网络 / 扩大授权 | `partial`（经标准 CR 审批、SoD 拦自批；专门旅程与模板未收口） |
| 文件系统 / 查看事实 | `view` |
| 文件系统 / 收窄或扩大 | **`unsup`**（`422 static_generation_unavailable`，fail-closed） |
| 进程 / 查看事实 | `view` |
| 进程 / 收窄或扩大 | **`unsup`** |
| 凭据 / 查看事实 | `partial`（Findings 的 credential 风险 ≠ 凭据权限事实） |
| 凭据 / 授予或修改 | **`unsup`**（secrets 只能引用，422 拒明文） |
| 工具/技能 / 查看事实 | `view`（`effective_permissions: null` 如实展示） |
| 工具/技能 / 独立限制 | **`unsup`**（`skill_isolation=not_established` 恒定） |
| 跨域 / 生效验证 | `partial`（独立读回四态；**行为核验缺失**） |
| 跨域 / 回滚恢复 | `partial`（**两条分支现在都重查活体授权链**：`openshell-cli` 下吊销后 502 且零写入、状态不变；其它取值下断链 → 409 + 零写入 + 拒绝审计——**均已测试**，见执行记录 R05.4/R05.5。**仍未实现**：**两条分支上都没有独立回滚审批**，回滚与部署共用 `policy:manage`；缺前端入口；"断链"仅覆盖绑定/变更单/策略三类，**不覆盖授权时效**（D-4 未定 TTL） |
| 绑定 / 撤销漂移联动 | `partial`（执行前复验 + 回滚拒绝已实现；**吊销的引用面事后可核对**——本轮新增只读投影；沙箱 revision/Skill 摘要绑定缺失；**无行为验证**） |
| 审计 / 追溯治理 | `partial`（精确查询 + 批次同事务审计；完整版本链与保留导出治理待做） |

**明确不支持 = 安全边界诚实，不等于原任务要求已实现。** 个人端 `apps/agentshield` 的撤权能力是企业控制面**之外**的内嵌能力，**不计入**企业端覆盖。

**回滚行怎么读**（R05.5 后更新）：这一行过去被记为笼统的"后端有活体授权复验"。核查发现**接入哪个后端会改变这条边界是否存在**——`openshell-cli` 下有，取其它值时一条都没有；**该不一致已消除**（两条分支共用同一处判定）。现在的答案**不再依赖部署配置**，但仍是 `partial`：**独立回滚审批缺失**，且判定的"断链"只覆盖绑定/变更单/策略三类，**不覆盖授权时效**（D-4 未定 TTL）。给用户的安全结论应连同这两点一起说。

---

## 5. 原防攻击能力是否有同候选不退化证据？

**当前时点：有第一批同候选实测证据，但不是完整结论。**

**已取得（同一工作树、同一份未提交源码，只读观测与临时构建）**：

| 能力面 | 证据 |
| --- | --- |
| 共享威胁规则包 Python/Go 双份一致性 | 两份 `threat_rules.v1.json` **字节相同**（md5 `4863baaa…`，13,219 B）；Go 侧 `rulepack_test.go:27` **已有**显式漂移失败断言，`-race` 通过 |
| 提示注入 / 凭据外传识别、污点与准入 | `apps/agentshield` 全模块 `-race` 绿（`threat`、`intent`、`runtimecheck`、`provenance`、`ledger`…）；后端威胁/漂移/证据边界子集 **258 passed** |
| 签名回执、撤权、Authority | `receipt`、`signing`、`grant`、`runtimeidentity`、`runtimeauthz` 相关包 `-race` 绿 |
| Edge 与 12 个 Connector | gofmt / vet / `-race` **全绿** |
| 前端 | 全量 **1003 passed**；**正式构建**（`VITE_DEV_MODE=false`）exit 0；产物内 `X-Dev-*`/`VITE_DEV_MODE`/`tnt-A` **命中 0**（开发身份被常量折叠消除，正式包不可用于模拟身份） |
| **本分支检出自身的可启动面**（只读核验，2026-09-26 补测，R09.17b） | `import-closure-check.py` 对真实仓库逐 ref 实跑：`HEAD`/`6ba1f7c`/`ad3116e` = `import_closure_open`（补件 **25** 且逐条相同），**负对照 `ebaaf3b` = `import_closure_closed_at_ref`（0 补件、退出码 0）**。**这是本节唯一一条"能力面为负"的当期实测**：不是防攻击能力退化，而是**该候选当前不具备可启动性**。留痕 `08-import-closure-check-2026-09-26.txt` |
| 迁移与部署竞态（真实引擎，2026-09-26 补测） | **A1 实跑通过**：一次性回环 PostgreSQL 容器上 18 项检查、`migrated_head=0028`，3 次独立运行结论一致；宿主回收核验全绿。**同一候选存疑**（取证脚本属并作者未提交改动，见"仍然缺"第 2 条） |
| 前端交互（模拟身份，2026-09-26 补测） | **A2 实跑 25/30 通过**；5 项失败均为 UI 可见性/文本断言，归因于并作者未提交的 `apps/web` 改动。**不构成本轮契约证据**（`mocked browser only`） |
| OpenShell 网络策略下发**链路**（真实网关，2026-09-26 补测，R09.15） | `--live` 在对方受控 canary 上真实执行一次闭环：`revision 2` → `policy set` → `revision 3` → **`readback_verified`** → 回滚 → `revision 4`，回滚后读回 digest 与写入前**逐字节相同**；对方 CLI 独立复核 `v4` hash == `v2` hash、`--rev 2 --full` 与 `--rev 4 --full` 载荷相同。**天花板 = `readback_verified`**：**未**产生 `enforcement_verified`，**未**做任何真实 deny 观测，`expect_deny` 是固定兼容样例。即：**"配置已下发且可读回一致"成立；"运行时真的按它执行了"本轮没有证据** |

**同候选证据退化的反例（2026-09-26 新增，R09.13）**：E149 的只读真实预览工具
（`openshell-preview-live-check.py`）按**它自己文档里记的同一条命令、同一个目标**
（`siq-analysis-canary-27d1289f98fa`）在当前工作树运行 → **rc=1**，`/deployment-preview` **409
`deployment_target_authority_unverified`**，且失败发生在 `policy get` **之前**（命令列表里根本没有它）。
根因是**未跟踪新模块** `app/target_authority.py`（mtime 2026-09-25）把算子授权文件
（`SIQ_AS_OPENSHELL_TARGET_AUTHORITY_FILE`）变成 openshell-cli 部署路径的**强制**前置，
而该工具在启动时把 `os.environ` 清成 `PATH/HOME/USER/LANG/LC_ALL` 白名单、**从不设该变量**，
故**构造上不可能**通过自己的预览断言。**后果**：E149 记录的"8 项全通过"（2026-09-23）
在当前候选上**已不可复现**——这是本轮**唯一一条实测到的同候选退化**，属本节问题的**否定性证据**
（不是"防攻击能力退化"，而是"某条已声明的验收证据失去当期支撑"）。相关四条路径
（`target_authority.py` / `routers/deployment_preview.py` / 该工具 / E149 文档）**均不在**冻结允许清单内，
本轮**未改**；是否修复、由谁修复属 D-7.3（并作者归属）决策。

**该反例的后续：工具侧已按使用者授权修掉（R09.16，2026-09-26 追加，合成件验证）**。使用者
授权了「修预览工具」并选定「身份 id 改从算子授权目录读」。修复后：工具**必填** `--target-authority`
（指向算子声明的 `enterprise-runtime-target-authority/v1` 目录），进度**复用门禁自己的**读取/解析实现，
把 `caps.endpoint_fingerprint` 与 `sha256(caps.handshake_gateway)` 对目录条目做**精确元组比对**，
身份 id 全部取自目录；不符则**有界失败留痕**（只写固定判别码 + 实测摘要）。合成闭环 **10/10**，
负对照（改动前版本、同 bench、同参数）在**同一行同一 409** 处失败。**但这不把 E149 的 8/8 复原**，
两点必须分清：① 修复是在**全合成件**上验证的（无真实网关、无真实身份、无真实策略写入），
**不构成真实目标上的复现**；② 即便用完美目录，本路径上的改写使上下文变更的拒绝**提前到授权层**
（实测判别码 `deployment_target_authority_unverified`，而非 `deployment_preview_changed`，
后者在 `deployment_preview.py:252`、**本路径不可达**），且 `checks` 由 8 条变为 10 条
（新增 `authority_tuple_matches_live_gateway`、`operator_declared_identity_used`，旧名不再存在）
⇒ **E149 文本里那份 8/8 记录仍是历史观察，当期不可逐字复现**。本节结论**不变**：
这是"某条已声明验收证据失去当期支撑"，**不是**"防攻击能力退化"，也**不产生** `enforcement_verified`。

**已用统一机制复跑（R07.9，三处既有缺陷修复后）**：`enterprise-gate-run.py` 48 项门禁 → **48 passed / 0 failed / 3 skipped（声明不可用）**，`conclusion=gates_incomplete`，进程**退出码 1（`EXIT_NOT_GREEN`）**，4 分 44 秒，报告 `2026-09-26T08:13:41Z → 08:18:25Z`。逐门禁留痕，报告自带"绿 ≠ 生产效果 ≠ 已签候选 ≠ 冻结 ≠ CI 存证"声明。**读到 `conclusion=gates_incomplete` 与 `exit=1` 都不代表失败**：0 条失败门禁，3 条是**声明为不可用**的 `skipped`，工具只在 `gates_green` 时返回 0 —— 即本次能到达的最好结论就是 `gates_incomplete`，不会是"全绿"。
（对照：实跑 A 为 44 passed / 1 failed / 3 skipped，实跑 B 为 47 passed / 1 failed / 3 skipped —— B 修掉了 Go 缓存门禁缺陷，C 修掉了那两个既存后端缺陷。）

**可选浏览器门禁接入后的全跑（R07.11，F3，2026-09-26）**：48 个基础门禁 + 1 个浏览器门禁 = **49 条记录：48 passed / 1 failed / 0 blocked**，`conclusion=gates_failed`，**唯一失败项即 `browser_acceptance_simulated`**（25 passed / 5 failed / 0 blocked of 30），`skipped` 只剩两条声明级（`migration_replay_postgres` —— **本轮刻意不启用**，再跑容器需按 §3.2 重新取得该次许可，故仍记静态原因 `skipped`、`measured=None`；以及 `real_device_native_evidence`）。后端全量 **2164 passed / 1 skipped / 113.75s**，前端单测与正式构建包均 passed，整体 `real 9m11s`，`head=f1709cd515fe`。
**这不是能力回退**：F3 是**第一次把此前"恒登记为不可用"的声明换成真实可执行门禁**，代价是该门禁实测本来就是红的（5 项 UI 断言失败）。默认（不传 `--enable-browser-smoke-gate`）仍是 C 轮的形态与结论；**启用后如实报红，未给新门禁任何豁免或降级成 skip**。

**范围已按实测改精确（R07.11b）**：原措辞"`mocked browser only`、不是真实 HTTP 契约"**低估**了这批脚本。实测 **8/30 会自起真实回环 dev 控制面**（真实 socket + 真实 HTTP + 合成 `X-Dev-*` 身份），且 **5 项失败全部落在这 8 个之内**；套件按 `SIQ_AS_DEV` 逐个**度量**并写入 `result.json`（`real_dev_api_scripts` / `failed_are_subset_of_real_dev_api`），门禁读不到该字段即判 `failed` —— 报告里的范围描述**有证据背书**，不是转述一句声明。定向复跑 E4 实测 8 条、子集为 `true`（报告 `/tmp/gate-E4-browser-20260926T173911.json`）。**限度的收窄（R09.11）**：门禁的 `post` 校验只在命令通过时执行，而该门禁当前恒红，故证据断言**原先在真实数据上没有被演练过**（只有合成用例覆盖）。现已加 `--validate-browser-evidence <result.json>` **纯核对模式**（不跑套件、不构建、不写报告；不短路，逐条打印 9 条断言；退出码 0 只表示「留证自述自洽」，措辞不含 `gates_green`），并在两份**既有真实留证**上实测：R07.11b 之后那份 **8/9 成立**（唯一不成立的是 `suite_passed`，即 25/30 那 5 项前端失败）；R07.11b 之前那份 **6/9**，恰好把 `suite_digest_matches` 与 `real_dev_api_measured` **抓了出来**——**这就是负对照，证明这些检查在真实数据上不是空跑**。限度因此收窄为「**只剩 `suite_passed` 一条未在真实数据上求值**」，而它能否成立只取决于那 5 项 UI 失败被裁决后 30 个脚本是否全绿，**不取决于工具**。

**这一份结果来之不易，两点说明**：

- **第一轮跑出来 Go 侧是"同内容历史通过"而非本轮执行**（60 条 `ok` 行中 54 条带 `(cached)`，`go_edge_race` 仅 0.111s）。这被当作门禁设计缺陷当场修掉：14 个 Go 门禁改为 `-count=1` 并加"出现 `(cached)` 即失败"的新鲜性断言。**第二轮重跑 0 条缓存**，`go_edge_race` 8.87s、`go_agentshield_race` 137.4s，全部真跑通过，且失败集合两轮逐条一致——所以"缓存没掩盖失败"这一点现在**有证据而不是推断**。**本节以第二轮为准。**
- **前端/构建/规则包是当场真跑**（单测 2.2s、正式构建 5.6s）。

R00 与本轮均未发现任何被删除的断言、被关闭的校验或被加入的 skip。

**仍然缺（所以不能说"不退化已证明"）**：

1. **后端全量已转绿（本轮唯一一处会变绿的门禁）**：R07.8 按 D-1(a) 修掉两个既存缺陷后，后端全量实测 **2164 passed / 1 skipped / 0 failed**（修复前 R07.6 实跑 B 为 9 failed / 2150 passed / 1 skipped）。两个缺陷的根因与修法：
   - `test_deployment_preview_consistency.py:58-63` 的 `_reservations()` 读取 `DeploymentSubmission.state`，而该模型**没有 `state` 列**。**孤立 8 passed / 全量 8 failed**——列表推导只在存在行时才求值属性，表为空即空通过。这既是 8 条失败的原因，也是"单独跑测试文件"作为门禁不可靠的直接证据。**修法**：改为对五个**真实存在**字段做行集+内容快照，未给预留记录凭空造状态机。
   - `test_enrollment_flow.py:182` 用**会话级共享环境** `env_a`，而 `GET /edge/v1/tasks` 按 `created_at` 只取**最旧 10 条**（`environments.py:486-491`，领取语义），全量运行时新任务排在第 11 位之后。**修法**：给该用例独立环境，**未**放宽 10 条上限。
   - **注意这不等于"不退化已证明"**：后端转绿只说明那两个既存缺陷不再遮挡结果；迁移回放、浏览器验收、真实设备证据仍在下面第 2、3 条里缺着。
2. **迁移回放与浏览器验收：两项现已实跑，但都不能算作"不退化已证明"。**
   - **A1 迁移回放已执行并通过**（2026-09-26，在 §3.2 许可下用一次性回环容器；全跑 49 passed / 0 failed / 2 skipped，`migrated_head=0028`，18 项检查，宿主回收核验全绿）。这填上了原先"恒 `skipped`"的格子。**但它是单次隔离观测，不是同候选证据**——A1 依赖的取证脚本 `deployment-postgres-check.py` 在**工作树**中是并作者**未提交**改动（96+/15−），不在 26 条允许清单内；证据里的 `script_sha256` 绑定的正是那份工作树脚本，因此**只有在并作者确认脚本归属并入清单后**，A1 才能升格为同一候选的证据。
   - **A2 浏览器验收已实跑：25/30 通过，5 项失败**（归因见 §1 表与执行记录 R09.5，均指向并作者未提交的 `apps/web` 改动）；**R07.11 的套件独立复跑得到同一组数字（25/5/0）**，且失败集合逐条一致——所以这 5 项失败**可复现、非偶发**。**第三方交叉确认**：全跑 F3 的门禁报告 stderr 尾部列出 5 个失败脚本名（`candidate-review` / `change-execution` / `deployment-preview` / `onboarding` / `workspace`-browser-smoke），与 A2 独立测得集合、E1 从各脚本自身 JSON 反推出的集合**三方一致**。浏览器验收脚本**本轮实测为 30 个**（`scripts/enterprise-experience/*-browser-smoke.py`），此前沿用的「24」是更早时点数字，未复核——**以 30 为准**。**这 25 项通过的东西不是"不退化"的证明**：`production_deployed: false`、`production_identity_tested: false`，身份是合成的 `X-Dev-*`。**范围已按实测改精确（R07.11b）**：原写"`mocked browser only`、不是真实 HTTP 契约"，**低估了这批脚本**——其中 8 个自起真实回环 dev 控制面（真实 socket + 真实 HTTP），5 项失败全在这 8 个内；准确说法是"**共享夹具为 mock，8/30 另走真实 HTTP；但套件不做契约级断言**"，缺的那一层已由 **R09.7/R09.8** 补齐（独立工具、83 条判据全绿、**零容器**）。**但两者不重叠**：R09.8 不驱动浏览器，**不能**用它的绿给这 5 项失败判绿；这 5 项仍归并作者未提交的 `apps/web` 改动。
3. **真实 HTTP 契约级性质：同候选上有行为证据，但仍不等于"不退化已证明"。** R09.8（2026-09-26，§3.2 许可下的一次性回环实例）对**已声明**的合同跑了 **83 条判据，全部成立**：其中直接属于防攻击能力的是**对象级隔离序**（同租户无权限→403、不存在与跨租户→**404 且正文逐字节相同**）、**角色矩阵**（`tenant_admin` 无 `audit:read`→403）、**三类真秘密材料在四个面零命中**（响应/审计/服务日志/一次性库文件）、**启动 fail-closed 4 类拒绝**、**失败路径不写审计**（409/404 后审计总数 1→1）。**为什么仍不能算"不退化"**：① 身份是合成 `X-Dev-*`、dev 模式、临时 SQLite、零容器——**不是生产配置**；② 这是**同候选上的一次横向验收，没有与本轮之前的基线做纵向对比**，所以它证明的是"这些已声明性质在当前候选上成立"，不是"没有退化"；③ **不产生 `enforcement_verified`**（仍只能由 A7 的受控探针产生）。另有一个**证据强度自知的限度**：它**不能**用来解释或抵消 A2 那 5 项前端 UI 失败（不驱动浏览器）。
4. **无真实设备 / 原生平台行为证据**（`enforcement_verified` 的升格路径仍空）。
5. **工具层：同候选回归证据（新，R09.10），同样不是「不退化」证明。** 本轮 5 个工具测试文件合跑 **92 passed**，本轮 8 条工具路径 `ruff` 仓库口径全绿，且改过的门禁执行器**真实跑过一次两门禁**（两条 `passed`）。**为什么仍不算「不退化」**：没有「改前同命令」的基线读数，所以它证明的是「当前候选上这些工具自检通过」，**不是**「本轮没有让工具退化」；且这 92 条是工具自身的单元/合成用例，**不触达**后端与前端全量。

按 §8 质量底线，本轮**未删断言、未关校验、未加 skip**。D-1 原为「只写新增文件」，主开发者 2026-09-26 已放开为**例外授权**，据此改了 **5 个文件**：`routers/policies.py`（R05.5 回滚活体授权链重查）、`app/main.py`（R01.5 接线，两行纯追加）、`tests/test_policy_flow.py`（R05.5 新增 4 用例）、`tests/test_deployment_preview_consistency.py`（R07.8 缺陷 1；该文件**未跟踪**）与 `tests/test_enrollment_flow.py`（R07.8 缺陷 2）。**这 5 项是唯一超出「只写新增文件」的改动**，已在冻结允许清单第四节逐条登记（该清单同时含三个未跟踪合同件的"版本导航"追加段）。

---

## 6. 哪些历史/未决材料被保留，升级、失败和回滚是否可恢复？

- **保留**：7 条已删除条目经核对为内嵌 UI 哈希换代，原始历史制品未覆盖；各责任线的失败证据（如 Hermes 截断失败 24 通过/1 失败、批量 wire 前端 4 failed、标准构建 exit 1 的历史记录）**原样保留在各自 handoff**，未改写。
- **升级/降级**：迁移 0028 等已有「历史非空拒绝降级」保护；`bindings` / 批次 / 周期计划均保留 CAS 与家族外键。
- **失败恢复**：注册恢复（`registration-pending.json` + `--resume`）、部署丢响应（`deployment_submission_unconfirmed`，不盲重放）、周期确认（`confirmation_result_unknown` / `confirmation_receipt_not_saved` 分离）均已实现。
- **已知不可恢复窗口（如实保留）**：周期归档中「历史副本已写但未决标记未发布」时重复准备被拒绝，需人工处置；复验→`apply_dynamic` 之间的 TOCTOU 窗口未消除；`unconfirmed` 结果无自动恢复驱动器。
- **回滚可恢复性（R05.5 之后）**：`openshell-cli` 下回滚有精确前置快照 + 写前活体授权复验，授权过期即拒绝且后端零写入、部署状态不变——这条路径可恢复且已测试（既有测试 `test_policy_flow.py:675`）。**此前**非 `openshell-cli` 取值下回滚只是**账面状态迁移**且**不重查**实时授权链：绑定已吊销、策略已被取代时仍写下 `rolled_back`，并把 `superseded` 的变更单**改写**为 `rolled_back`（`superseded` 标记因此丢失，不可还原）。**该不一致已按 D-1(b) 消除**（R05.5）：两条分支现在共用同一处活体授权链判定，断链 → `409 rollback_authorization_expired` + 审计 `deployment.rollback_refused` + **零状态写入**（3 条断链拒绝用例 + 1 条反例守卫）。**仍未实现**：独立回滚审批（两条分支都没有，属业务决策）。
- **本轮新增工具对历史材料的处理**：`contract-version-chain-audit.py` 报告**独占创建、已存在即拒绝覆盖**（exit 3），不会覆盖既有报告；别名表是显式声明而非推断，只登记经逐字核对"语义已建档、文件名 family 不同"的条目，不用于掩盖真实缺口。剩余断点如实保留在报告中。
- **A1 执行后的材料保留与"回收"的边界**：被回收的只有**容器/卷/网络**（一次性资源，`--rm` + `finally` + 断言）；**证据不回收**——`/tmp/a1-evidence-*` 取证报告、统一门禁报告、定向 opt-in 报告与前后容器清单 diff **全部保留**（复核命令与路径见执行记录 R09.4(8)）。统一门禁报告本身仍是**独占创建**（目标已存在即拒绝覆盖，exit 3），所以**本轮 A1 的三份报告不会覆盖 R07.9 的 C 报告**——升级/失败/回滚的复查可以用"同命令重跑 + 新旧报告并存"的方式做。
- **R09.15 的失败证据同样保留（不掩盖）**：`--live` 的**首次运行（失败）**与补齐后的**成功运行**两份原始输出都在（`05-` 记的是"获批但拒绝执行"的判定，`06-` 同时收录首跑 `openshell_network_binary_required` 与成功闭环）。**网关侧的历史修订也全部保留、未被清理**：`policy list` 现为 `4 Loaded / 3 Superseded / 2 Superseded / 1 Superseded`，本次写入的 `v3`（`dfdefc3465ef`）作为**可审计历史**留在网关，未删除任何修订；`v2`/`v1` 同样在位。因此这条链路的"回滚可恢复性"本轮有一份**真实**证据：回滚后的 `v4` 与写入前的 `v2` hash 相同、载荷逐字节相同。**边界**：这只覆盖**网络策略下发**这一条链路，且止于 `readback_verified`（**不是**"运行时真的按它执行了"）。
- **R09.8 的失败证据同样保留（不掩盖）**：契约验收的**首次运行报告**（2 条红）与更正后的全绿报告**两份都在**（`/tmp/r09-contract-20260926T175013` 与 `…175026`），且执行记录 R09.8 明确写出"首次那两条红是我的探针前提写错"及其代码行依据。工具本身也是**独占创建证据目录**（已存在即拒绝，exit 2），不会覆盖既往报告；`report.json` 之外还留 `exchanges.json`（每一次 HTTP 往返的状态码/响应头/正文）与 `server-stdout-stderr.log`，所以**下游可以自行复算判据**而不必相信摘要。工具内不含任何破坏性动作：临时目录在 `finally` 里 `rmtree`，数据库是它自建的临时 SQLite。
- **R09.16 的负对照与失败留痕同样保留（不掩盖）**：留痕文件 `07-…txt` 同时记录**实跑①的失败**（错指纹目录 → `rc=1` / `authority_endpoint_mismatch`，两个摘要都打出来）与**实跑②的通过**（`nc=0`、10 条 `checks`），并单列 5 条"**本轮没有证明什么**"（无真实网关 / 无真实身份 / `real_gateway: true` 是常量 / 未做任何真实策略写入 / 不产生 `enforcement_verified`）；已改工具新增的 `write_failure()` **只写固定判别码 + 实测摘要**，不写路径、策略正文、证书或凭据，恒 `production_eligible=false` / `runtime_policy_mutated=false` / `enforcement_verified=false` ⇒ 失败留痕本身不制造新的敏感材料，也不冒充授权事实。负对照的临时件（`tmp-negctl/`）用完即删，证据以 `07-` 内的逐字输出为准。
- **未提交路径的保留与归属边界（R09.17，构成由 R09.17b 逐条更正）**：本轮核到本分支 HEAD 检出**无法 `import app.main`**，补足导入闭包需 **25 条路径**（3 条已跟踪被修改：`app/models.py`/`app/routers/deployment_preview.py`/`app/routers/deployment_submission.py`；22 条未跟踪）。这些**不是本轮产物**：`app/models.py` 的未提交差异 **252 行插入**且含**既有表的约束变更**，属并作者在飞的工作。按 §3.2「不覆盖历史制品 / 不擅自扫入他人工作」与 D-7.3 归属规则，本轮**只诊断、不并入、不改动**，并在 §7 发布清单首位列出 ⇒ 升级/回滚的"可恢复性"不受影响（未提交工作仍在工作树原位，未被提交、未被丢弃、未被覆盖）。

---

## 7. 精确提交/签发/部署待授权清单

本轮**未**推送、未签发、未发布、未部署（提交与分支见下方第 1 项）。待授权项：

本轮已把"待授权"细化到**逐文件、逐门槛**，证据见执行记录 R08.4 / R08.5。**提交、推送、签发、部署是四个独立门槛，不得打包成一次授权。**

**已备好的提交候选（仅本轮，46 条路径，已核验快照）**：
[`deepseek-enterprise-mainline-closeout-freeze-allowlist-20260926.txt`](deepseek-enterprise-mainline-closeout-freeze-allowlist-20260926.txt) —— 用**既有** `source-freeze-preflight.py` 核验。**A1/A2 记录与门禁登记更正落盘后复跑一次（`2026-09-26T08:52:14Z`，`--out /tmp/preflight-r09-20260926T165214.json`）**：`requested=26 / verified=26`（含**内容级 sha256**）、`unverified=0 / excluded=0 / missing=0`、`scan_stable=true`、`conflicts=0`、`head_commit=7209a76`、`worktree: 106 tracked changes / 704 untracked / 0 conflicts`，`signed=installable=published=false`，`conclusion=blocked`（唯一原因 `unreviewed=806`）。
**R07.11 两个新文件加入清单后复跑一次（`2026-09-26T09:17:54Z` / 本地 `20260926T171754`，`--out /tmp/preflight-r0711-20260926T171754.json`）**：`requested=28 / verified=28`、`unverified=0 / excluded=0 / missing=0`、`scan_stable=true`、`conflicts=0`、`head_commit=f1709cd`、`worktree: 107 tracked changes / 706 untracked / 0 conflicts`，`conclusion=blocked`（唯一原因仍是 `unreviewed_paths:806`，**全部是并行作者线产物，非本轮路径**）。**清单由 26 增至 28**：新增 `run-browser-smoke-suite.py` 与其测试；清单范围声明同步由「R00–R08」改为「R00–R09」。**两次的 106/704 与 107/706 是同一口径（报告自身字段）的两次测量**，不对跨口径数字（如默认 `git status --porcelain` 的折叠值）作比较（见 R08.6 的口径提醒）。**核验通过只表示这些文件的只读快照确定完整，不是发布授权、不表示源码已冻结、不是发行来源清单。**
**R09.7 的新工具加入清单后复跑一次（`2026-09-26T09:50:46Z` / 本地 `20260926T175046`，`--out /tmp/preflight-r097-20260926T175046.json`）**：`requested=29 / verified=29`、`unverified=0 / excluded=0 / missing=0`、`scan_stable=true`、`conflicts=0`、`head_commit=66aae3d`、`worktree: 103 tracked changes / 705 untracked / 0 conflicts`，`conclusion=blocked`（唯一原因仍是 `unreviewed_paths:806`）。**清单由 28 增至 29**：新增 `http-contract-acceptance.py`（R09.7/8 的验收工具，未接门禁）。**R09.9 的回归测试加入清单后复跑一次（`2026-09-26T09:53:26Z` / 本地 `20260926T175326`，`--out /tmp/preflight-r098-20260926T175326.json`）**：`requested=30 / verified=30`、`unverified=0 / excluded=0 / missing=0`、`scan_stable=true`、`conflicts=0`、`head_commit=66aae3d`、`worktree: 105 tracked changes / 706 untracked / 0 conflicts`，`conclusion=blocked`（唯一原因仍是 `unreviewed_paths:806`）。**清单由 29 增至 30**：新增 `test_http_contract_acceptance.py`。
**`6516e58` 提交后再跑一次同一条命令（`2026-09-26T09:55:13Z` / `--out /tmp/preflight-r099-20260926T175513.json`）**：`requested=30 / verified=30`、`conflicts=0`、`head_commit=6516e58`、`worktree: 102 tracked changes / 705 untracked / 0 conflicts`、`conclusion=blocked`（唯一原因仍是 `unreviewed_paths:806`）——相对上一份**差恰好 −3 / −1**，与 `6516e58` 的路径构成逐条对应。
**R04 只读交付与 R09.13 证据加入清单后复跑一次（`2026-09-26T11:21:24Z` / 本地 `20260926T112124`，`--out /tmp/preflight-r0913-20260926T112124.json`）**：`requested=39 / verified=39`、`unverified=0 / excluded=0 / missing=0`、`scan_stable=true`（`unstable.files=[]`）、`conflicts=0`、`head_commit=0355db0`、`worktree: 105 tracked changes / 714 untracked / 0 conflicts`，`signed=installable=published=false`，`conclusion=blocked`（唯一原因仍是 `unreviewed_paths:806`）。**清单由 30 增至 39**：新增 4 条 R04 路径（`rollback_approval_readiness.py`、其测试、`enterprise-rollback-approval.v1.md`、R04 交接文档）+ 5 条 R09.13 证据路径（`docs/evidence/agentshield/openshell-siq-analysis-2026-09-26/` 下 README 与 4 份原始输出）。
**R09.13 追加 1 条证据后复跑一次（`2026-09-26T12:32:27Z` / 本地 `20260926T123227`，`--out /tmp/preflight-r0913b-20260926T123227.json`）**：`requested=40 / verified=40`、`unverified=0 / excluded=0 / missing=0`、`scan_stable=true`、`conflicts=0`、`head_commit=d214e00`、`worktree: 104 tracked changes / 705 untracked / 0 conflicts`，`conclusion=blocked`（唯一原因仍是 `unreviewed_paths:806`）。**清单由 39 增至 40**：新增 `05-live-prereq-blocked.txt`（`--live` 的前置核查产物）。**同口径逐条相减 = 新增 1 / 消失 11**：消失的 11 条正是 `d223ad8` 的 13 条路径中除"仍在改动状态"的 `README.md` 与允许清单之外的 9 条新文件 + 提交的执行记录与交接文档；`819 − 11 + 1 = 809`，`tracked 105→104`、`untracked 714→705` **三项计数互相闭合**。
**`--live` 工具改动与写入闭环留痕加入清单后复跑一次（`2026-09-26T21:11:48Z` / 本地 `20260926T211148`，`--out /tmp/preflight-r0915-20260926T211148.json`）**：`requested=43 / verified=43`、`unverified=0 / excluded=0 / missing=0`、`scan_stable=true`、`conflicts=0`、`head_commit=6f04c15`、`worktree: 105 tracked changes / 706 untracked / 0 conflicts`、`status_entries=811`、`signed=installable=published=false`，`conclusion=blocked`（唯一原因仍是 `unreviewed_paths:806`）。**清单由 40 增至 43**：新增 `06-live-readback-rollback-2026-09-26.txt`（写入闭环留痕）+ `test_openshell_compat_live.py`（新增未跟踪）+ `openshell_compat_check.py`（**已跟踪件被修改**，`--live` 补 authorizer）。**同口径逐条相减 = 新增 3 / 消失 1**：消失的 1 条是 `05-live-prereq-blocked.txt`——它已被 `6f04c15` 提交，故不再出现在工作树状态里（**不是被删除**，内容仍在分支上）。三项计数**互相闭合**：条目数 `809 − 1 + 3 = 811`、`tracked 104 + 1 = 105`（`openshell_compat_check.py` 变为已跟踪改动）、`untracked 705 − 1 + 2 = 706`（05 入库、06 与测试文件新增）。
**R09.16 的预览工具改动、其隔离测试与 `07-` 留痕加入清单后复跑一次（`2026-09-26T13:25:27Z` / 本地 `20260926T132527`，`--out /tmp/preflight-r0916-20260926T132527.json`）**：`requested=46 / verified=46`（含**内容级 sha256**）、`unverified=0 / excluded=0 / missing=0`、`conflicts=0`、`head_commit=ad3116e`、`worktree: 107 tracked changes / 706 untracked / 0 conflicts`、`status_entries=813`、`signed=installable=published=false`，`conclusion=blocked`（唯一原因仍是 `unreviewed_paths:806`）。**清单由 43 增至 46**：新增 `07-preview-tool-operator-authority-2026-09-26.txt`（新增未跟踪）+ `test_openshell_preview_live_check.py`（新增未跟踪）+ `openshell-preview-live-check.py`（**已跟踪件被修改**）。**同口径逐条相减 = 新增 5 / 消失 3**：新增的 5 条 = 上述 3 条 R09.16 路径 + 两份滚动文档（我自己在两次核验之间的编辑）；消失的 3 条 = `06-live-readback-rollback-2026-09-26.txt`、`test_openshell_compat_live.py`、`openshell_compat_check.py`——**全部已被 `ad3116e` 提交**，故不再出现在工作树状态里（**不是被删除**）。三项计数**互相闭合**：条目数 `811 − 3 + 5 = 813`、`tracked 105 − 1 + 3 = 107`、`untracked 706 − 2 + 2 = 706`；`head` 同期由 `6f04c15` 前移到 `ad3116e`，可独立佐证那 3 条是**提交**而非消失。
**该提交后再跑一次同一条命令（第 9 次，`2026-09-26T13:26:51Z` / 本地 `20260926T132651`，`--out /tmp/preflight-r0916b-20260926T132651.json`）**：`requested=46 / verified=46`、`unverified=0 / excluded=0 / missing=0`、`conflicts=0`、`head_commit=f8dc646`、`worktree: 102 tracked changes / 704 untracked`、`status_entries=806`、`signed=installable=published=false`、`conclusion=blocked`（唯一原因 `unreviewed_paths:806`）。**同口径逐条相减 = 新增 0 / 消失 7**（消失的 7 条**逐条等于本次提交的 7 条路径**，其中 5 条已跟踪改动 + 2 条未跟踪，**不是被删除**）；三项计数**互相闭合**：`813 − 7 = 806`、`tracked 107 − 5 = 102`、`untracked 706 − 2 = 704`。**一处可复算的巧合值得指出**：第 8 次 `unreviewed=806 = 813 − 7`（7 条与清单重叠），第 9 次 `unreviewed=806 = 806 − 0`（那 7 条已入库变干净、不再重叠）——**两次 806 同值但来源不同**，不可当作"未复核范围没变"来读。
**R09.17b 的只读闭包核验工具、其 10 条合成用例与 `08-` 留痕加入清单后复跑一次（第 10 次，`2026-09-26T13:40:02Z` / 本地 `20260926T134002`，`--out /tmp/preflight-r0917b-20260926T134002.json`）**：`requested=49 / verified=49`（含**内容级 sha256**）、`unverified=0 / excluded=0 / missing=0`、`conflicts=0`、`head_commit=033f50d`、`worktree: 106 tracked changes / 707 untracked`、`status_entries=813`、`signed=installable=published=false`，`conclusion=blocked`（唯一原因 `unreviewed_paths:806`）。**清单由 46 增至 49**：新增 `import-closure-check.py` + `test_import_closure_check.py`（均新增未跟踪）+ `08-import-closure-check-2026-09-26.txt`（新增未跟踪）。**同口径逐条相减（对第 9 次）= 新增 7 / 消失 0**：新增的 7 条 = 上述 3 条 + **4 条已跟踪被改**（两份滚动文档、允许清单、证据 README 的 §九）；三项计数**互相闭合**：条目数 `806 − 0 + 7 = 813`、`tracked 102 + 4 = 106`、`untracked 704 + 3 = 707`。**同值不同源的第三次**：第 10 次 `unreviewed=806 = 813 − 7`，第 9 次 `806 = 806 − 0`，第 8 次 `806 = 813 − 7` —— **标量相等不是判据，同口径相减才是**。
**该提交后再跑一次同一条命令（第 11 次，`2026-09-26T13:40:57Z` / 本地 `20260926T134057`，`--out /tmp/preflight-r0917bb-20260926T134057.json`）**：`requested=49 / verified=49`、`unverified=0 / excluded=0 / missing=0`、`conflicts=0`、`head_commit=7891f7e`、`worktree: 102 tracked changes / 704 untracked`、`status_entries=806`、`signed=installable=published=false`、`conclusion=blocked`（唯一原因 `unreviewed_paths:806`）。**同口径逐条相减 = 新增 0 / 消失 7**，消失的 7 条**逐条等于 `7891f7e` 的 7 条路径**（**不是被删除**）；三项计数**互相闭合**：`813 − 7 = 806`、`tracked 106 − 4 = 102`、`untracked 707 − 3 = 704`。**同值不同源的第四处**：第 11 次 `806 = 806 − 0`，第 10 次 `806 = 813 − 7`——四次 `806` 分属四种来源，**只可同口径相减，不可比标量**。
**逐条归属（本轮四份报告的 `worktree.status_entries` 逐条相减；这是本节唯一一处对"归属"下结论的地方）**：`r0711b 108/704 → r097 103/705` 的差异 = **−6 条已跟踪**（并行作者线提交了 6 个已跟踪文件：两份滚动文档、`enterprise-gate-run.py`、`run-browser-smoke-suite.py` 及其两个测试；`head` 同期由 `b4586c1` 前移到 `66aae3d`，可独立佐证是提交而非改动消失）+ **+1 未跟踪**（R09.7 新工具）+ 清单文件由干净回到未暂存修改状态（**我自己**的编辑）；`r097 103/705 → r098 105/706` 的差异 = **+2 条已跟踪**（就是我在两次核验之间写的**这两份滚动文档**，mtime `17:51:22` / `17:52:20`，均在 r097 之后）+ **+1 未跟踪**（R09.9 的测试文件）；`r099b 102/705 → r0913 105/714` 的差异 = **+3 条已跟踪**（就是我在五次核验之间写的**这两份滚动文档**与**允许清单本身**，即 R08.7 那三条工作树状态以空格起头（modified）的条目）+ **+9 条未跟踪**（4 条 R04 交付 + 5 条 R09.13 证据，逐条见 R08.7 的归属表）——**新增 12 / 消失 0**，与本次改动逐条对应，并作者线一条未动。
**R09.18 的行为 fixture 通道 P0（6 条新增源码/测试 + `09-` 留痕 + 方案文档 + 5 条**已跟踪**合同件）加入清单后复跑一次（第 12 次，`2026-09-26T14:15Z` 前后 / `--out /tmp/preflight-r0918-20260926T141500.json`）**：`requested=62 / verified=62`（含**内容级 sha256**）、`unverified=0 / excluded=0 / missing=0`、`conflicts=0`、`head_commit=108fb29`、`worktree: 108 tracked / 712 untracked`、`status_entries=820`、`signed=installable=published=false`、`conclusion=blocked`（唯一原因 `unreviewed_paths:804`）。**同口径逐条相减（对第 11 次）= 新增 14 / 消失 0**：6 条新增源码/测试 + 1 条 `09-` 留痕 + 1 条行为 fixture 方案文档（**8 条未跟踪**）+ 3 条**本轮首次变脏**的已跟踪适配器（`base.py` / `client.py` / `fake_backend.py`）+ 3 条滚动文档（第 11 次提交后已干净，本节编辑后重新变脏）。三项计数闭合：`806 + 14 = 820`、`102 + 6 = 108`、`704 + 8 = 712`。**一处必须点明的归属事实**：我在清单里列了 **5 条**已跟踪合同件，但同口径相减显示**只有 3 条是新出现的** —— `contracts.py` 与 `cli_backend.py` 在本轮开工**之前**就已是修改状态（属第 11 次报告的 `102` 之内）。这正印证清单注释里写的"这些文件的 diff 不全是 P0 的"；P0 在其中只占约 25 行。
**该提交（`743a745`）后再跑一次同一条命令（第 13 次，`2026-09-26T14:07:17Z` / `--out /tmp/preflight-r0918b-20260926T140717Z.json`）**：`requested=62 / verified=62`（含**内容级 sha256**）、`unverified=0 / excluded=0 / missing=0`、`scan_stable=true`、`conflicts=0`、`head_commit=743a745`、`worktree: 100 tracked / 704 untracked`、`status_entries=804`、`signed=installable=published=false`、`conclusion=blocked`（唯一原因 `unreviewed_paths:804`）。**同口径逐条相减 = 新增 0 / 消失 16**，消失的 16 条**逐条等于本次提交的 16 条路径**（8 条已跟踪改动 + 8 条未跟踪，**不是被删除**）；三项计数**互相闭合**：`820 − 16 = 804`、`tracked 108 − 8 = 100`、`untracked 712 − 8 = 704`，`head` 同期由 `108fb29` 前移到 `743a745` 可独立佐证是提交。**本轮窗口内并作者线一条未动**（新增 0 就是这句话的实测形式）。
   - **第 13 次是"同值不同源"的第五处，而且这一处最有信息量**：`unreviewed` 第 12 次与第 13 次**都是 804**，但来源完全不同 —— 第 12 次是 `status 820 − checked∩status 16 = 804`，第 13 次是 `status 804 − checked∩status 0 = 804`。请只读报告自身字段复算：12 次 `checked∩status = 16`，13 次 `checked∩status = 0`。**这次相等恰好证明了两件事**：① 那 16 条被提交的路径**正是**当时被清单覆盖的那 16 条（否则 `checked∩status` 不会归零）；② `checked−status` 由 **46** 变为 **62** —— 即清单里**每一条**现在都处于"已跟踪且干净"状态。第 ② 点把 R08.8 那类"清单里有、分支上却没有"的缺陷在本轮**排除掉了**：我已用 `git ls-tree -r HEAD` 逐条复核 **62/62 全部在 HEAD 树中、0 条缺失，且对这 62 条 `git status --porcelain --untracked-files=all` 输出为空**。仍然只能得出"本轮清单内每一条都已入库"，**不能**得出"本轮没有 R08.8 那类问题"之外的任何结论，**更不是**冻结或发布授权。
   - **提交后仍未变的两个非本次作者文件**：`apps/control-api/app/adapters/openshell/bounded_command.py` 与 `scripts/enterprise-experience/deployment-postgres-check.py` 在本工作树中仍是修改状态（在 `100 tracked` 之内），**非本次作者**，已明确排除在 `743a745` 之外；它们的归属仍待 D-7.3。
**订正本节上一版的错误结论**：上一版写「`tracked 102→103` 的 **+1 不是本轮**…未做归属判定，属 D-7.3」——**这是错的**，那个 +1 就是我自己对允许清单文件的编辑。**后果**：本轮**不再存在"未归属的已跟踪改动"**；D-7.3 剩下的未决项只有两个，都与并作者文件有关（见下方第 2 项与第 240 行附近的 `deployment-postgres-check.py`），**不是**这个 +1。
**口径提醒照旧**：这几次的 106/704、107/706、108/704、103/705、105/706、108/712、100/704 都是**同一口径（preflight 报告自身字段）**；工具自报的 `git status --porcelain --untracked-files=all` 计数（807）**与 preflight 的 untracked 不同源**，两者不可相减。**标量相等不是判据**：`unreviewed` 这一个数在四次报告里出现过 806（三次、三种来源）与 804（两次、两种来源），**只有同口径逐条相减才能判归属**——这一点在第 13 次上有了最干净的一次演示（见上）。
**一处必须说清的时序限制**：preflight 的 `head_commit` 是 `f1709cd`，而被它核验的内容随后提交为 `52cb855`；本节"提交号 + 前后账目"这几行**必然写在 preflight 之后**（提交号在提交前不存在），因此**文档自身比被核验的快照多出这几行**。这不是核验失效，而是"报告不能自指"的固有性质：**要复核，请对 `52cb855` 重跑同一条 preflight 命令**（`--allowlist ... --out <新路径>`；目标已存在即拒绝覆盖，故用新路径），而不是只读本节数字。同理适用于上面**四次** preflight（26/28/29/30 条清单各一次）：它们各自绑定当时的工作树，不绑定本节文字。**R09.9 的账目同理**：`6516e58` 之后的 `102 / 705` 读数写在 `6516e58` **之后**，因此本节这几行必然比被核验的快照晚；要复核请现场跑 `git log --oneline ebaaf3b..HEAD` 与同一条 preflight 命令，**不要**把本节的提交数当成静态事实（账目提交本身也会 +1，见下）。**账目提交后的同口径读数（`2026-09-26T09:55:49Z` / `--out /tmp/preflight-r099b-20260926T175549.json`）**：`102 tracked / 705 untracked`，且 `status_entries` 与上一份**逐条相同**——中间那次"交接文档被再次编辑"的 +1 只存在于两份快照之间，**不在任何快照里**，这一点如实写在这里而不是装作没发生。

1. **提交授权（D-7.1）**：**已决策 2026-09-26：提交到新分支（`main` 不动），不推送。已执行**——分支 `deepseek/enterprise-mainline-closeout-20260926`，**完整提交串**（`git log --oneline ebaaf3b..HEAD`，实测 **19 个**）：`6ba1f7c`（父 `ebaaf3b`，26 条路径）→ `1f50a16`（2）→ `7209a76`（4）→ **`34722e8`**（4，R09.5/R09.6）→ `f1709cd`（2，文档账目）→ **`52cb855`**（7，R07.11）→ `532d3b3`（2）→ `d66e226`（1）→ `075c310`（2）→ `b4586c1`（1）→ **`bed0fbb`**（6，R07.11b）→ `66aae3d`（2，文档账目）→ **`6516e58`**（4，R09.9）→ `b27608a`（1，文档账目）→ `f67f0ed`（4，R09.10）→ `0355db0`（4，R09.11）→ **`d223ad8`**（**13**，R04 只读交付 + R09.13 OpenShell 只读证据 + R08.7/R08.8 账目 + 补入从未入库的 R09.7 工具）→ `d214e00`（1，R08.8 修复后复核账目）→ **`6f04c15`**（**5**，R09.14 的 `--live` 前置核查与 `05-` 证据）→ **`ad3116e`**（**3**，R09.15：`--live` 工具改动 + 其隔离测试 + `06-` 写入闭环留痕，附两份滚动文档账目）→ **`f8dc646`**（**7**，R09.16：预览工具改动 + 其隔离测试 + `07-` 留痕 + 证据 README 的 §八 + 清单 43→46 + 两份滚动文档账目；`git show --name-only` 与清单逐条一致、无清单外路径）。**本次提交前的 HEAD = `ad3116e`**（现场实测 `git rev-list --count ebaaf3b..HEAD` = **20**）；**本次提交后的 HEAD = `f8dc646`，提交数 = 21**（现场复核 `git log --oneline ebaaf3b..HEAD` 应显示 21 个）。**R09.17b 单元后来再提交一次**：**`7891f7e`**（**7**，父 `033f50d`：只读闭包核验工具 + 其 10 条合成用例 + `08-` 留痕 + 证据 README §九 + 清单 46→49 + 两份滚动文档账目；`git show --name-only` 与清单逐条一致、无清单外路径），**提交数 = 23**。**R09.18 的 P0 单元再提交一次**：**`743a745`**（**16**，父 `108fb29`：校验器 `enforcement_probe.py` + 通道 `probe_channel.py` + 后端 59 例 + 工具 + 探针 agent + 工具层 27 例 + `09-` 留痕 + 方案文档 + **8 条已跟踪**〔5 条适配器合同件 + 3 条滚动文档/清单〕；`git show --name-only` 与清单逐条一致、无清单外路径，`bounded_command.py` 与 `deployment-postgres-check.py` **已排除**），**提交数 = 25**（现场 `git rev-list --count ebaaf3b..HEAD` = **25**，HEAD = `743a745`）。**提交后的工作树账目已用同一条 preflight 收口**（第 9 次：`813 − 7 = 806` 条，`tracked 107−5=102`、`untracked 706−2=704`，**新增 0 / 消失 7** 逐条等于本次提交路径）。**再往后的编辑（例如本节这几行）仍会晚于被核验的快照**——这正是报告不能自指的老问题，复核请现场跑命令，**不要**把本节数字当静态事实。`main` 仍为 `ebaaf3b`（`origin/main` 同），**未推送**、**未签发**、**未部署**。**作者字段不可用于归属**：本仓所有提交的作者都是同一个 `maoyadongsh`（共享工作树），因此并作者线与执行者的提交**只能按内容与时间对齐**，不能按作者名区分——这也是下面这条归属证据必须用 preflight 报告 `status_entries` 逐条相减、而不能用 `git log --author` 的原因。
   - **一处自查更正（R08.8，2026-09-26）**：此前写到"本轮文件不再以未跟踪状态裸放在共享工作树"——**这话对 39 条路径里的一条并不成立**。`scripts/enterprise-experience/http-contract-acceptance.py`（R09.7/R09.8 的契约级验收工具）**从未进入任何提交**（`git log --all --full-history -- <该路径>` 为空、`git ls-tree -r HEAD` 命中 0），却一直出现在允许清单里并通过 preflight——因为核验的是**工作树**状态，而工作树里它确实在。**后果**：它的合成测试 `test_http_contract_acceptance.py` 已在 `6516e58` 入库，而该测试按同目录定位被测脚本，**在分支 HEAD 的干净检出上连收集都过不去**（实测 `FileNotFoundError` / exit 2；复现命令见执行记录 R08.8，用 `git archive` 导出，**未**把共享工作树换回 HEAD）。**已并入本次提交**，提交后该工具与其测试在分支上成对在场。**这提醒一条更一般的口径**：允许清单 + preflight 只证明"工作树上的只读快照完整"，**不证明"清单里每一条都已入库"**；这两件事必须分开核。
   - **`6516e58`（R09.9）的路径与前后账目**：4 条路径 = 新增 `test_http_contract_acceptance.py` + 允许清单文件 + 两份滚动文档。**同口径复核**：该提交前最后一次 preflight `2026-09-26T09:53:26Z` = `105 tracked / 706 untracked`，提交后再跑同一条命令（`--out /tmp/preflight-r099-20260926T175513.json`）= `102 / 705` —— **差恰好 −3 已跟踪 / −1 未跟踪**，与本次"3 条改动既有件 + 1 条新增未跟踪"逐条对应，**并作者线一条未动**。
   - **`bed0fbb`（R07.11b）的路径与前后账目**：6 条路径 = `enterprise-gate-run.py`、`run-browser-smoke-suite.py`、`test_enterprise_gate_run.py`、`test_run_browser_smoke_suite.py` + 两份滚动文档。这正是 preflight 从 `2026-09-26T09:44:46Z`（`108 / 704`）到 `09:50:46Z`（`103 / 705`）之间**减少的那 6 条已跟踪项**，且 `head` 同期由 `b4586c1` 前移到 `66aae3d` —— **两次独立观测互相印证**。`52cb855` 为 **7 条路径**：`run-browser-smoke-suite.py`、`test_run_browser_smoke_suite.py`（两条**新增**，即清单由 26→28 的增量）+ `enterprise-gate-run.py`、`test_enterprise_gate_run.py` + 三份文档。**工作树账目用同口径复核**（`--untracked-files=no` / `=all`，而非会折叠未跟踪目录的默认 `git status --porcelain`）：`34722e8` 前 `tracked_changes=106` / `untracked=704`，之后 `102` / `704` —— **差恰好 4**（=本次提交路径数）；`52cb855` 前 `107` / `706`（**与同次 preflight 报告字段逐字一致**），之后 `102` / `704` —— **差恰好 7**。两次提交的前后差值都等于各自路径数，并作者线一条未动。见执行记录 R08.6 与 R09.6。**提交粒度必须读清**：Git 只能按**文件**暂存，所以 `policies.py`、`main.py`、`test_policy_flow.py`、`test_enrollment_flow.py` 这四个**已跟踪**文件里，**非本轮**的 hunk 与**本轮**的 hunk 会**一起**进入快照（本轮的改动合计只占其中一部分行数；例如 `main.py` 本轮只加 2 行，其余 40 行是并行作者线的 router 接线）。同理，未跟踪文件（含三个合同件、若干 R0x 新文件）会把该文件**全文**纳入快照，而其中非本轮撰写的正文无法在文件粒度上剔除。**因此该分支是一个"共享工作树快照"，不是"执行者独立成果"的干净切片**；`main` 上的 `ebaaf3b` 仍**不含**本轮成果。合并/复核时**必须逐条对照并作者线**（D-7.3 未决）。
2. **并行接管授权（D-1）**：仍在写本仓的 Codex / GLM 会话各自负责哪些文件、何时交接。**影响**：preflight 已报 `conclusion=blocked`，唯一原因是 `unreviewed_paths:806`——这些**绝大部分是并行作者线产物**，执行者不得代为判断或收编，故冻结无法自行完成。
3. **推送/发 PR 授权（D-7.2）**：默认否，不影响本轮结果。
4. **D-1 例外确认（D-7.4）**：**已决策 2026-09-26：保留追加段**（R02 在三个合同件末尾追加"版本导航"段，符合仓储既有约定）。**措辞已订正**：这三个路径**未纳入版本控制**（`git ls-files` 无记录、HEAD 中不存在），故准确说法是"追加到工作树内既存、归属未确认的未跟踪合同件"，而不是"修改已跟踪既有件"——详见执行记录 R08.4。该决定**不覆盖**同一门槛下的另一半：D-1 例外的三处最小改动已按"全部放开"另行授权并实施（R01.5 / R05.5 / R07.8）。
5. **签发授权（D-7.5）**：受控签发入口引用、可信公钥、独立 verifier 身份、发布目的地与回滚方案。**签发的前置条件（冻结 commit + 清单收敛 + 独立审阅）当前不具备**，故本轮不请求签发，只请求确认门槛分离。不读取或索要私钥正文。
6. **部署/切换授权（D-7.6）**：目标环境、操作范围、备份与恢复方案。**本轮不请求**（无目标、无身份、无备份授权）。
7. **真实验收资源（D-6）**：Linux ARM64/DGX、Linux AMD64 原生、第二设备、第二租户、独立审批者、~~受控 OpenShell 测试目标~~（**已于 2026-09-26 指定**：`siq-research-engine` 的智能分析助手网关，`siq-analysis-canary-27d1289f98fa`；只读链接已实跑，见第 15 项与 R09.13）、Mac 浏览器访问远程 Linux。
8. **接线授权（R01）**：**已决策 2026-09-26（D-1 = 全部放开）并已实施**——`app/main.py:37/182` 两行纯追加，端点可达性经 `app.openapi()["paths"]` 核对 + 1 条接线用例断言非 404。见执行记录 R01.5。**未做**：设备侧 CLI 的"列出我的待办"入口。
9. **移交事项（R06）**：本地 agentshield 控制台 `local-admin-session/v1` 历史兼容读取的处置（补说明或移除分支），属该子系统负责人决策，不在本轮企业主线范围内。
10. **D-1 例外的三处最小改动**：**已决策 2026-09-26："全部放开"并已逐项实施 + 隔离验证**（执行记录 R01.5 / R05.5 / R07.8，文件级清单见冻结允许清单第四节）。
    - (a) **R07 两个既存后端缺陷**：已修（改读真实字段 / 改用例级独立环境），后端全量由 9 failed 转 **2164 passed / 0 failed**。
    - (b) **R05 非 `openshell-cli` 回滚分支**：已按"补实时授权链重查"这一支实施（两条分支共用同一处判定，断链 409 + 零写入），4 条新用例含反证。
    - (c) **R01 接线**：已完成（见上第 8 项）。
    - **仍属业务决策、未随 (a)(b)(c) 一起授权**：**独立回滚审批**——需先明确回滚是否要与部署职责分离、由谁批。当前 `policy:manage` 仍可单方回滚，两条分支都一样。
11. **A1 一次性数据库容器许可——已获批并于 2026-09-26 执行完毕**：答复为"批准并执行完整 A1"。按执行记录 **R09.4** 的方案执行（镜像本机已有 `sha256:ff800890…`、**未拉取**；只绑 `127.0.0.1` 随机端口；tmpfs 无卷；`X-Dev-*` 合成身份；环境白名单；`finally` + 断言回收）。**结果**：直接运行 `{"passed": true, "checks": 18}`、`migrated_head=0028`；统一门禁全跑 **49 passed / 0 failed / 0 blocked / 2 skipped**（4m49s）；定向 opt-in 门禁 `passed` 且 `post_check` 与 `script_sha256` 一致。**宿主未受影响**：容器集合 `diff` 为空（45 项）、端口映射未变、零残留卷/网络、镜像字节不变、目录 `700`、无凭据泄漏——**点名过的真实服务（`siq-platform-*`、`siq-org-iam-*`、`milvus-*`、`hrsight-*` 等）一条未触碰**。
    - **仍待并作者确认的偏差（D-7.3 的一部分）**：A1 依赖的 `scripts/enterprise-experience/deployment-postgres-check.py` 是**并作者未提交改动**、**不在 26 条允许清单内**（与 HEAD 差 96+/15−，本轮只读取未写入）。证据里的 `script_sha256` 绑定的是"实际执行的那份工作树脚本"，**故 A1 目前不是同一候选的证据**；要升格，需并作者确认该脚本归属并把它纳入清单。
12. **A2 目标选择——已执行，且"一次性服务实例"许可未被使用**：实测**推翻了**我原先的请求前提——30 个脚本**全部自带隔离环境**（`VITE_DEV_MODE=true` 模拟身份构建 + `127.0.0.1` 静态服务 + Playwright 路由 mock），**不需要任何运行中的控制面**；另 2 个需原生 Edge 与连接器二进制（已临时 `go build` 就位）。故宿主上那个 2026-09-24 的既有控制面**未使用**、**未启动任何服务**、**未新建实例**。**结果 25/30 通过**，5 项 UI 断言失败已归因（见 §1 表 / R09.5），**未加 skip、不记为通过**。**范围更正（R07.11b）**：本条初稿写"28 个自带隔离环境、3 个另带隔离 dev API"，**低估**了这批脚本——实测 **8 个**会自起**真实回环 dev 控制面**（真实 socket + 真实 HTTP + 合成 `X-Dev-*` 身份），且 5 项失败**全部落在这 8 个之内**；套件按 `SIQ_AS_DEV` 逐个度量并写入 `result.json`，不是硬编码。
    - **因此 R09.1 中 A2"证明什么"一栏需要收紧**：这批脚本证明的是"模拟身份 + mock 下的前端交互"，**不是**真实 HTTP 契约。若要真实端到端契约验收，需另立方案（那才是"一次性实例"许可的正当用途），本轮**不请求**。
13. **浏览器验收门禁（R07.11）的资源依赖——默认关闭，无需新授权即可复核**：`--enable-browser-smoke-gate` 需要 (a) 一个带 playwright 的解释器（本机 `/home/maoyd/miniconda3/bin/python`，playwright 1.58.0；**不安装依赖**，缺了只记 `playwright_not_importable`），(b) 一次**模拟身份**前端构建（`VITE_DEV_MODE=true`，落独立临时目录、名带 `NOT-RELEASABLE`、默认跑完删除），(c) 2 个需要原生二进制的脚本要 `--browser-smoke-edge` 与 `--browser-smoke-connector-dir`（本轮用临时 `go build` 产出：`edge-agent` sha256 `00adf228…`、`hermes-connector` sha256 `31c28628…`，**两者都不是发布产物**；不提供时这两个脚本记 `blocked`，套件不可能通过）。**另有只读核对入口（R09.11，不是门禁）**：`--validate-browser-evidence <result.json>` 逐条核对一份既有留证的 9 条断言，不跑套件、不构建、不写报告，可离线复核既往证据。**不启动任何控制面服务、不连数据库、不启容器**——脚本各自在本机 `127.0.0.1` 起临时静态服务并用路由 mock。因此它属于任务书 §3.2 允许的"隔离合成测试及临时构建"，**不需要额外许可**；但它**仍不是**"真实 HTTP 契约验收"——那一层已另立 **R09.7/R09.8**（见第 14 项），**已执行**。
14. **真实 HTTP 契约验收（R09.7/R09.8）——已获批并执行完毕（2026-09-26）**：主开发者当日答复的许可是**"批准并执行完整 R09.7"**（7 类判据全跑；状态写入只允许落在那一份一次性临时 SQLite 内）。执行结果（证据 `/tmp/r09-contract-20260926T175026/report.json`）：**83 条判据全部成立，`contracts_held`，exit 0**，含 404-先于-403 的**同正文不可区分**（`{"detail":"not_found"}`）、401/403/404/409/422 全族、角色矩阵（`tenant_admin` 无 `audit:read`→403 / `auditor`→200）、**409 回滚与 404 定位失败均不写审计（总数 1→1）**、跨租户按 `resource_id` 查询 0 行、两侧分页与计数头的**声明**语义（total 全量口径 4、翻页不变、`limit=999`→200、租户乙 total=1、逐页不重不漏、cursor 无 limit→422、跨租户 cursor 同 422 同 detail、`Cache-Control: no-store`）、**三类真秘密材料在四个面零命中**、4 类启动 fail-closed 拒绝。**回收核验**：进程退出/端口重新 bind 成功/临时目录消失全 true，事后另行确认无 `/tmp/siq-http-acceptance-*` 残留。**边界**：合成 `X-Dev-*` ≠ 真实 IAM、非生产、零容器，**不产生 `enforcement_verified`**（仍只能由 A7 产生）；**不覆盖** A2 的 5 项前端 UI 失败（不驱动浏览器）。**首次运行曾出现 2 条红，是探针前提写错**（`SIQ_AS_ALLOW_SQLITE` 在 `config.py:78` 的默认值就是 `dev_mode`），按代码行更正后复跑全绿——两条报告都保留，说明见执行记录 R09.8。新工具 `http-contract-acceptance.py` **未接门禁**（无 `--enable-*` 开关，是否接成第三条显式 opt-in 门禁属 D 门槛决定）。**本条已闭**，不需要新许可。**补记（R09.9）**：该工具的**合成回归守卫**已随 `6516e58` 落盘（16 条用例，不启服务、不发 HTTP、不依赖 playwright，专钉「工具自己报假绿」；`16 passed`，`ruff` 仓库口径全绿），清单随之由 29 增至 **30**；它**不改变**本条证据的效力范围，也**不**产生 `enforcement_verified`。
15. **A7（行为核验）不得由本轮制造**：`enforcement_verified` 至今**无生产者**，R09.2 第 1 条要求它**只能**由 A7 的受控探针真实 deny 观测产生。**受控目标已由使用者于 2026-09-26 指定**（= `siq-research-engine` 的智能分析助手 OpenShell 网关），**只读链接已实跑成立**（R09.13：兼容矩阵 8/8、两个 `Ready` 的 `siq-analysis-canary-*` 沙箱、回环 HTTPS `Connected`/`0.0.83`，**未启停网关、未 `policy set`**）。**仍缺两件、需分别授权**：
    - **(a) `--live` 真实写入门 —— 已获批，但前置核查后本轮「拒绝执行」**（2026-09-26）。使用者当轮答复为"批准"，**但我随后做前置核查时发现该批准所依据的两条描述都不成立**，故**没有执行**，并把更正后的判定落盘在 `docs/evidence/agentshield/openshell-siq-analysis-2026-09-26/05-live-prereq-blocked.txt`：
      - **更正 1（写面比我原先描述的大）**：`cli_backend.py:584-591` 对 `network_policies` 是**整段替换**、不是追加一条。该 canary 现有 **8 条** provider/内部服务策略（Kimi/MiniMax/StepFun/Tavily/relay/data-broker/egress-guard/internal-services）在变更窗口内会**全部消失**；若分析助手此刻正在该沙箱内运行，其模型调用与检索会被一并阻断——而**不是**我原先写的"短暂多出一条样例 endpoint"。
      - **更正 2（回收手段不存在）**：脚本 `openshell_compat_check.py:139` 调 `backend.rollback(sandbox, receipt)` **不传 `authorizer`**，而 `cli_backend.py:807-808` 在无作者时抛 `openshell_rollback_authorizer_required`（隔离实测已复现，对网关只有一条只读 `policy get`）。即 **"同一 run 内回滚"作为回收手段在当前候选上不成立**：`policy set` 会发生，回滚必然失败并被脚本吞成"FAIL: 回滚失败"，沙箱被留在"网络策略只剩 `example.com:443`"的状态。
      - **三条备选路径**：① 先给脚本补 `authorizer`（从私有操作记录构造）与"写后必回滚"断言，**使其先具备回收能力**再跑；② 由目标侧提供一个与生产隔离的**一次性专用沙箱**再跑；③ 停在只读。
      - **后续（2026-09-26 晚，R09.15）：使用者选定路径①，`--live` 已真实执行闭环。** 补齐 `authorizer`（**只从私有操作记录构造**、只认本次 `operation_id`+`target`）+"写后必回滚"+回滚后 digest 必须等于 BEFORE，共 2 个路径（均在本仓 `scripts/`，对方仓库零改动），并配 8 条隔离单测（**负对照**指向改动前版本时 5/7 失败）。**执行中暴露第三条缺陷**：fixture 缺 `binary_paths` → `policy_safety.validate_network_rules` 在 `compile()` 阶段抛 `openshell_network_binary_required`，**首跑一个字节都没写**，即该路径此前**从未真正写成功过**；补 `_LIVE_FIXTURE_BINARY` 后重跑成功（退出码 0）：`revision 2` → 写入 `revision 3` → `readback_verified` → 回滚 `revision 4` → 回滚后 digest 与 BEFORE 相同。**独立复核**（对方 CLI 只读、本轮重跑可复现）：`policy list` v4 `fed6cc8072d1` **Loaded**、v3 `dfdefc3465ef`（本次写入，此前不存在）、v2 亦 `fed6cc8072d1`；`--rev 2 --full` vs `--rev 4 --full` 差异**只有头部 4 个元数据字段**，去头后载荷 SHA-256 相同 ⇒ **逐字节复原**。**顺带把更正 1 由"读代码"升级为"实测"**：`--rev 3` 的 `network_policies` 只剩写入的那一条。原始留痕见 `06-live-readback-rollback-2026-09-26.txt`。
      - **产出上限不变**：`verify()` 只比对读回配置，上限仍是 **`readback_verified`**——本次已执行，**未产生 `enforcement_verified`**；其 `expect_deny` 是**固定兼容样例**（"拒绝"来自*不在允许集里*），**不是**真实拒绝行为观测，本次**未**观测到任何 deny。终态：canary 网络策略恢复原状，v3 作为**可审计历史修订**留在网关、未删除；未创建/删除沙箱、未启停网关。**`--live` 到此为止，它不使任何门禁变绿、不关闭 A7。**
    - **(b) 行为 fixture 通道**：`enforcement_verified` 的**唯一合法生产者**，当前**三个后端都没有**（`base.py:52-58` / `cli_backend.py:726-732`），实现它要动 `app/adapters/openshell/*` 与 `app/routers/policies.py`，**均不在允许清单内**，需 D-1 范围明确授权；且**不得**由夹具/mock/推演生成该值（R09.2 第 1 条）。**2026-09-26 使用者答复：授权在隔离环境内建该通道**——范围与边界（改哪些文件、fixture 如何证明"强制点真的拦住"而非"读回配置对得上"、如何避免把该值降级为夹具产物）尚待我方提交具体方案后再动手，见执行记录。
    - **(c) 顺带发现的既有阻塞（已获批修，且修法比原设想深一层）**：E149 那条只读预览路径在当前候选上**构造性失败**（见 §5 反例）。**2026-09-26 使用者答复：授权我修预览工具**。但深挖后确认**仅放行环境变量不足以恢复 E149**：`authorize_runtime_target`（`target_authority.py:125-133`）对授权目录做**精确元组相等**匹配（`tenant_id, environment_id, asset_id, agent_instance_id, endpoint_fingerprint, gateway_name_sha256, backend_target_id`），而该工具的 `environment_id`/`asset_id`/`agent_instance_id` **都是运行期新生成的**，算子不可能在运行前写进目录。**要恢复 E149，必须让工具的身份 id 变成算子可预声明的输入**（改从算子目录读取它要用的那组 id），这等同上一条业务决策；**本轮未动**。
      - **后续（R09.16，同日）：使用者第二轮答复选定路径"改从算子目录读身份 id"，已实施 + 合成闭环 10/10 + 负对照。** 工具**必填** `--target-authority`（绝对路径、非符号链接、常规文件；真正的安全读取与重复键判定**复用门禁自己的** `_read_authority_bytes`/`_unique_object`/`TargetAuthority`），并把 `caps.endpoint_fingerprint` 与 `sha256(caps.handshake_gateway)` 对目录条目做**精确元组比对**；身份 id（`tenant_id` / `environment_id` / `asset_id` / `agent_instance_id`）**全部取自算子声明的目录条目**，不再运行期生成。不符即**有界失败留痕**（`write_failure()`：只写固定判别码 + 实测摘要，恒 `production_eligible=false` / `runtime_policy_mutated=false` / `enforcement_verified=false`）。新增 5 条隔离用例；留痕 `docs/evidence/agentshield/openshell-siq-analysis-2026-09-26/07-preview-tool-operator-authority-2026-09-26.txt`（含实跑①失败 / 实跑②通过 / 负对照 / 5 条"没有证明什么"）。**必须分清**：这**不把 E149 的 8/8 复原**——修复只在**全合成件**上验证，且 `checks` 已由 8 条变 10 条、上下文变更的拒绝提前到授权层（`deployment_target_authority_unverified`）。故 E149 仍是"当期不可逐字复现的历史观察"，**不产生** `enforcement_verified`、**不使任何门禁变绿**。
16. **可启动性阻塞（R09.17）——已决策，C 并已实施**：`git archive HEAD … | tar -x` 导出后 `python -c "import app.main"` → **`ImportError: cannot import name 'DiscoveryScheduleRecord' from 'app.models'`**。根因是分支**第一个**提交 `6ba1f7c` 只把 `app/main.py` 的 router 接线与 `discovery_schedule_pending.py` 入库，**其余被 `main.py` 导入的模块仍在工作树未提交**。补足后 **25 条路径**即可导入（构成见 §1 该行；对照 `app/routers/` 在 HEAD 15 个、工作树 35 个）。**影响**：① 分支 HEAD 当前**不能作为一个可启动候选**；② 任何"在 HEAD 上重跑"的验收**只能走工作树**；③ R08 的允许清单核了稳定性与冲突但**没核导入闭包**，此前"`git archive HEAD` 干净"的说法**只在各自核过的制品导入面内成立**。**2026-09-26 使用者就 D-8 选定 C（只诊断不修）并已实施**：新增只读 `scripts/enterprise-experience/import-closure-check.py` + 10 条合成回归用例（**未接线**、不参与门禁、不产生 `enforcement_verified`），对真实仓库实跑出逐 ref 表——`HEAD`/`6ba1f7c`/`ad3116e` 全 `import_closure_open` 25 条且逐条相同，**负对照 `ebaaf3b` 自足 0 补件、退出码 0**。**证据**：逐字命令、25 条路径清单、三次自伤留痕见 `docs/evidence/agentshield/openshell-siq-analysis-2026-09-26/08-import-closure-check-2026-09-26.txt`，执行记录 R09.17 / R09.17b。**重新开闸条件**：并作者确认归属后可转 (A) 并入 / (B) 另建分支最小子集。

**未签候选保持 `signed/installable/published=false`；无签发条件时只完成准备，CL-08 不关闭。**

**本章与 R09.1 的关系**：本章是**授权请求**（要什么、给不给）；执行记录 R09.1 是**具体项清单**（每项获批后跑什么命令、什么算通过），R09.2 是执行时的三条硬约束。两者一起构成"授权到位即可机械执行"的验收计划。

---

## 8. 本轮提出的决策门槛

| ID | 门槛 | 阻塞单元 | 未确认时行为 | 状态 |
| --- | --- | --- | --- | --- |
| D-1 | 并行接管：哪些作者仍在写哪些文件、何时交接 | 全部 | 不覆盖冲突文件，继续不冲突项 | **已决策 2026-09-26（两段）**：① 初答「只写新增文件」；② 随后放开为例外授权「全部放开」，据此实施 5 个既有件的改动（R01.5 / R05.5 / R07.8，清单见冻结允许清单第四节）。**并作者文件归属仍未确认**（见 D-7.3） |
| D-2 | R01 组织侧如何为新注册设备表达周期意图（A 环境级默认意图 / B 设备侧「我的待确认计划」查找 / C 维持现状） | R01 | 保持只读，不隐式授权 | **已决策 2026-09-26：B 最小路径**；只新增只读枚举，不自动激活、不降低确认强度 |
| D-3 | R04 共享覆盖/独占的判定标准与独立来源 | R04、R05 | 保持 `unknown`，执行确认继续禁止 | **已决策 2026-09-26：保持 `unknown` 不猜**；不新增判定来源 |
| D-4 | 证据时效（有效期/失效/复验） | R04 | 不自行设 TTL | **已决策 2026-09-26：维持不设 TTL**。不自行设有效期；证据只在其**绑定对象**不再存在时失效（本轮证据均绑定到具体 ref/commit sha 与允许清单 sha256，文档里逐条写明）。**因此"证据仍有效"= 绑定对象仍在，而不是"没过期"** |
| D-5 | 保留期限、法律保留、允许删除对象及授权流程 | R06 | 不删除、不自动清理 | **已决策 2026-09-26：只补齐声明与缺口**；不做删除/归档执行 |
| D-6 | 原生验证平台与受控 OpenShell 目标 | R03、R09 | 仅隔离合成验证 | **已决策 2026-09-26：资源可用**——数据库容器、真实 Linux 实机、受控 OpenShell 目标。**使用按 §3.2 逐次许可**：① **A1（数据库容器）已获批并执行完毕**，`action=run` 分支首次真实跑通（49 passed / 0 failed / 2 skipped；宿主零影响），R07.10 可选门禁由"恒 skipped"变为"可执行且已验证"；② **A2（浏览器验收）已执行**，实测**不需要控制面、不需要容器**，故"一次性服务实例"许可**未使用**（25/30 通过，5 项归因）；**R07.11 已把它做成可复核的显式 opt-in 门禁**（默认关闭、证据带 6 项摘要断言、失败即失败；接入后全跑 F3 = 48 passed / **1 failed**（就是它）/ 2 skipped，见 §5；见 §7 第 13 项）；③ **已执行完毕（2026-09-26）**：**真实 HTTP 契约验收（R09.7/R09.8）**——主开发者从四个候选项中选定并批准完整执行；一次性回环 dev 控制面（**零容器、零新依赖**），**83 条判据全部成立**（`contracts_held`），回收核验三项全 true、无残留；见 §7 第 14 项。**已闭**（R09.9 补记：其工具的合成回归守卫已落盘，清单增至 30）。**更新（2026-09-26 晚，R09.13）**：④ **受控 OpenShell 目标已由使用者指定**（= 智能分析助手网关），**只读链接已实跑成立**（兼容矩阵 8/8、两个 `Ready` 的 `siq-analysis-canary-*` 沙箱、回环 HTTPS `Connected`/`0.0.83`；**未启停网关、未 `policy set`、对方仓库零改动**）；**A5/A7 仍未启用**——行为 fixture 通道（`enforcement_verified` 唯一合法生产者）仍不存在且实现要动允许清单外路径，见 §7 第 15 项（b）。⑤ E149 只读预览工具在当前候选上**构造性失败**（409 `deployment_target_authority_unverified`），其"8 项全通过"已不可复现，相关四条路径均不在允许清单内、本轮未改（见 §5 反例与 R09.13）。⑥ **最新（2026-09-26 晚，R09.15）**：`--live` 经使用者选定**路径①（先补 authorizer 再跑）后已真实执行闭环**（`policy set` → `readback_verified` → 回滚，回滚后载荷由对方 CLI 独立复核为**逐字节复原**），**天花板仍是 `readback_verified`，未产生 `enforcement_verified`，未做任何真实 deny 观测**；路径②（一次性专用沙箱）**未使用**。**A3+（真实 Linux 实机）**仍 blocked，启用前须先写出隔离/目标/回收方案并取得该次许可。其余资源（ARM64/DGX、第二设备/租户、独立审批者、Mac 浏览器）未确认 |
| D-7 | 精确提交清单、签发入口、发布目的地与回滚方案 | R08、R09 | 不提交、签发、发布或切换 | 未确认。**已细化为 6 个子请求 D-7.1–D-7.6（见 §7），其中提交/推送/签发/部署为四个独立门槛**。**新增阻塞（R09.17）**：分支 HEAD 检出**无法 `import app.main`**（导入闭包差 25 条未提交路径），故在 D-8 决策前**不具备"可启动候选"资格**，签发/部署的前置条件比原判断更不成立（见 §7 第 16 项） |
| D-8 | **冻结闭包与可启动性（R09.17，新）**：本分支 HEAD 检出不能 `import app.main`，补足导入闭包需 **25 条未提交路径**（3 条已跟踪被修改，含 `app/models.py` 的 **+252/-3** 与**既有表约束变更**；22 条未跟踪，属并作者在飞工作） | R08 冻结、D-7 签发/部署、一切"在 HEAD 上重跑"的验收 | **已选 C**：只诊断、只落盘结论，不动别人的在飞改动 | **已决策 2026-09-26：C（只诊断不修 + 只读闭包核验）——执行者建议项被采纳，已实施**（R09.17b）：新增只读 `import-closure-check.py` + 10 条合成用例，实跑出逐 ref 表（`HEAD`/`6ba1f7c`/`ad3116e` = `import_closure_open` 25 条且逐条相同；**负对照 `ebaaf3b` 自足 0 补件**），并把首次记录的构成更正为 **3 条已跟踪被修改 + 22 条未跟踪**。**(A) 并入本轮提交 / (B) 另建分支补最小子集** 仍**未选**——两者都要在**归属未确认**的文件上写历史（§3.2「不覆盖历史制品」、D-7.3 未决），**重新开闸条件**是并作者确认归属。**三个选项都不等于"可发布"**，`signed/installable/published` 仍全 `false` |

| **D-9** | **行为 fixture 的 P1-pre 真实前置测量（R09.18，新）**：`enforcement_verified` 的**唯一**生产者是"边界内真实行为观测"。P0 已把通道与校验器做成可测的（隔离验证通过），但**前提未实测**：网关是否真的**按 binary 路径**归因网络规则（兼容矩阵 v0.0.83 无一条记录；`interceptor: false`）。不验则主方案（同 endpoint 双 binary）与退路（双 endpoint + 双向可达性对照）**都不能**当作已成立 | R03、R05、R09（`enforcement_verified` 仍无生产者） | 不执行：主方案与退路**均不得**被采信；级别停留 `readback_verified`（R09.15 的实测天花板）；**不擅自**把任何门禁或报告调成绿 | **使用者 2026-09-26 就方案四问答复**：F-1 **批准 P0**（已实施完毕，见 §1 表 R09.18 行）/ F-2 **暂不申请 P1-pre，先要 P0 结果**（P0 结果已出，故本门槛**现在可以重新提出**）/ F-3 边界内探针由**我方固定摘要上传**（`--agent` 必填、摘要进证据并参与绑定）/ F-4 主方案与退路**由 P1-pre 实测自动选择**。**P1-pre 需要单独一次许可**，且含两类**本轮从未做过**的动作（`sandbox upload` / `sandbox exec`）；按 §3.2 须先给出隔离/目标/回收方案（方案文档 §6 P1-pre 节已备），并注意写入窗口内该沙箱会只剩写入的那一条网络规则。**不请求**：不新增沙箱、不动生产、不自动选择目标 |

**D-7 为什么现在提、而不是等 R09**：R08 的剩余内容**全部**卡在这几项授权上。提交授权已到位（本轮 26 个文件不再以未跟踪状态裸放在共享工作树）；并作者归属缺位时，冻结清单仍无法收敛（`unreviewed_paths:806` 不是执行者能代为判断的范围）。签发与部署**前置条件当前不具备**，故本轮只请求门槛分离确认，不请求签发本身。

已决策项不改变"实际交付"计数：本轮交付仍为 **无**。
