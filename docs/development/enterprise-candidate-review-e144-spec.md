# 企业候选处理增量（E144）

2026-09-23，接续 E143。落实发现→角色→资产确认的实际操作，及 ENT-UX-02 中候选处理权限表达；不代表完整组织登录/角色工作台完成。

新增只读 inventory-access/v1，GET /api/v1/inventory/access，使用验证身份，要求 agent:read；can_confirm 来源 agent:confirm，can_discover 来源 env:read，can_manage_policy 来源 policy:manage。不输出用户目录、不新增权限。候选确认/驳回路径先租户定位再检查权限。驳回仅允许 candidate/needs_review，防止过时页面驳回已确认资产或重复写审计；不宣称新增并发事务隔离。

企业资产列表移除离线示例及宽泛智能扫描，发现入口转到已实现的环境接入流程。中文状态明确区分已确认与已纳管；列表条数注明已加载，保留分页。桌面使用表格，小屏改为保留全部字段和操作的卡片，避免逐字断行。URL 的 view=candidates 保存候选视图，详情返回保留该视图；接入完成可以直接查看候选。

候选处理弹窗展示对象、框架、现有业务用途；可填写中文业务角色，不要求用户输入负责人内部 ID（已有负责人保持，目录选择后续独立实现）。确认资产不是批准权限。取消不写入，提交期间禁止关闭和重复提交。提交前读回对象状态与更新时间，状态变化时要求核对；写后 GET 同 ID 读回目标状态，确认用途与请求一致。失败保留输入并在弹窗内反馈；写结果未知时只允许核对结果，禁止盲目重发。核对可能反映其他操作者动作，不宣称自己执行成功。

前端按只读权限投影隐藏处理按钮并说明联系组织管理员；后端权限仍是权威。未读取权限、断连/加载期间不允许写入。现有列表/详情读回使用实际 API，刷新保持视图。详情修正已确认状态文案与证据读取失败误报为空；详情创建策略入口同样按验证身份的 policy:manage 显示，无权限或读取失败不显示写入口；不改写现有策略治理架构，完整策略编辑/审批读回仍待后续。

验证：Schema/权限/租户与驳回状态负向/审计不增；真实隔离 API + 两框架 Connector 浏览器接续候选确认、驳回、用途持久化、刷新与详情返回、只读角色拒绝、冲突和响应丢失恢复。前端测试/构建、API 全量/ruff。无 DB 结构变更；原企业环境与个人结果旅程回归。原 Hermes 0.21、真实设备状态与发行安装保持。回滚只逆转本批增量，保留历史状态与审计。
