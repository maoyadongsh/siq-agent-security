# 当前收尾状态（2026-09-24）

本页替代旧收尾清单中的即时状态；旧文档和证据保留为历史，不再把已完成的 API 部署、镜像切换、报告模型交接重复列为待开发。原目标尚未全部验收，不能仅以四平台构建通过宣布正式交付。

## 已完成、无需重复开发

- 个人端和企业端的框架、角色、Skills 发现、权限管理、运行记录和结果入口已有实现及前后端测试；客户端 E163、企业端 E164 保留固定候选。企业生产部署与真实 IAM 联调不在这项完成声明内。
- Hermes 0.21 / OpenShell / Qwen 真实模型报告链路已在隔离业务身份下通过：八项材料、执行确认、独立复核、发布和三种格式读回；不冒充真实生产账号验收。
- 宿主 AgentShield、当前请求沙箱镜像、研究主 API 和 Web 已更新。当前页面 http://127.0.0.1:15173 连接 18081；本次读回两服务 active、HTTP 200，两类恢复器 ready。仍保持 legacy 执行配置，新的报告请求权限未启用。
- 新报告工具的客户端增量已从 E163 基线隔离为 11 文件补丁并完成构建验证。旧冻结候选和现有发行签名未改变。

## 本次完成的客户端验证

来源为 `fd02384d6de5f96a849f9d04bbfbfaff38cf6148` 加可审阅补丁，补丁 SHA-256 为 `718a448fd423840f3ae6093afffa846c12afc3322f4ecb66871740c49d04cccb`。版本标记 `0.4.0-rc.2-preparation`，没有创建新提交，没有签发。

首次验证失败：包装脚本设置进程 umask 077，改变了测试夹具预期的文件权限，五项测试失败。首次结果保留。第二次仅将 Go/构建子进程的 umask 设为 022；日志所在目录继续 0700，日志继续 0600，没有修改产品代码或降低权限检查。

第二次 gofmt、go vet、Go 44 包测试、Python 共享合同测试均通过；Go 记录 3,668 个测试及子测试通过事件、36 个跳过事件，不将这些事件冒充独立用例数量或跨平台原生测试。Linux amd64/arm64、macOS arm64、Windows amd64 四目标编译完成。

构建和摘要位于 `var/flagship/client-report-release-preparation-20260924/verification-v2/validation.sanitized.json`。这些是未签名准备产物，尚未对本批二进制执行原生安装、升级和回滚。不要直接沿用旧 E163 签发命令：`package.py --source-sha` 只导出指定提交，旧提交会遗漏本批报告工具增量。

## 剩余结束条件

| 工作 | 当前缺口 | 完成标准 |
| --- | --- | --- |
| 更新客户端正式候选及安装验收 | 增量已验证，仍需形成正式源码身份、接入已有签发环境并执行安装验收 | 新源码包含上述补丁；原发行密钥签发、独立验签、旧版升级和受支持回滚通过 |
| 真实报告执行接入 | 已提交具体权限变更单，决定仍待返回；当前 legacy 不变 | 通过实际授权接口接入指定公司范围，真实业务账号完成确认、报告生成及独立复核 |
| 企业部署与身份联调 | 企业控制台导航已实现；生产部署、真实组织/账号/IAM 接入未完成 | 同一部署下连续跨站登录、权限拒绝、审批及事件出口验证 |
| 原定模型与平台验收 | 固定 Nemotron 质量/性能结果和原生 CI/安装证据尚缺 | 按原任务完成相应环境验收；Qwen/Step 及交叉编译不替代 |

用户已选择“提供现有受控签发环境”，但当前记录中没有实际环境入口或受控密钥引用。真实 IAM 测试接入及原生 runner 也仍缺少可用信息。以上并非全部外部阻塞：候选整理、安装验收和企业部署接线仍有工程工作；不把它们隐藏在“等密钥”下面。

报告权限范围见 [具体变更单](business-runtime-permission-plan-20260924.md)，已发出的决定请求继续有效，不重复询问。签发只接受现有受控入口或引用，不需要在对话提供密钥正文。

## 收尾范围

接下来只处理表内未完成事项及其直接失败；已通过且源码未变的检查不再重复。SEC-F01–SEC-F10、Host Sensor 和风险聚合保持后续排期，不插入此次交付。控制面容器化及 AppArmor 改造不作为新增完成条件。用户未要求提交或发布前，不自动创建提交、推送或发布。

拖延原因包括真实报告执行合同和恢复链路的补缺，也包括此前部署方式探索与重复叠加的状态记录；不能全部归因于外部条件。用本页的具体完成凭据收尾，不再给出没有明确分母的百分比或缺乏依据的完成时间。

本批验证记录见 [脱敏证据](../evidence/flagship-optimization-20260921/client-report-release-preparation-20260924.json)，由同批 `candidate.json` 引用。服务部署详细证据仍以 [主 API 切换记录](main-api-cutover-20260924.md) 为准。
