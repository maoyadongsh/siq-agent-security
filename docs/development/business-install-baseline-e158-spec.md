# E158 历史迁移前置表修复

E157 的真实空库回放在历史 015 因缺少 `user_artifacts` 失败。目标是在原迁移命令中补齐有版本、可核验的前置基线，让空库和固定 v008 都能按序升级到 022，同时保持历史 SQL/checksum/账本不变。

研究仓新增独立 `migration_prerequisites` 清单（复用已有不可变文件与 SHA-256 检查），只有一个明确映射：001 artifact 基线 → 编号迁移 015。不是任意排序或补造旧版本。runner 连接前验证两套源清单、数据库锁内先验证主账本及已有前置账本；在执行尚未应用的 015 时，同一事务执行前置建表、015 原 SQL、前置账本和主账本。失败全笔回滚，重试无重复。已有 015 的旧库不补造前置应用记录。

前置基线只创建缺失 artifact 表/普通索引，既有表不加列、不改类型或填值；不完整既有表仍报错，不静默修补。唯一索引和既有去重语义继续由原 015 负责。前置表与模型一致性、空库/旧快照升级、存量保留、并发、事务失败恢复、源篡改/符号链接、共改 SQL 与 checksum 的 Git 历史拒绝、数据库账本异常都须测试。

扩展既有 Git 不可变历史门禁覆盖前置目录，保留 v008 冻结快照。读取和部署审计需区分主账本 current 与前置验证；不借此宣称全部应用数据库生产初始化或全链路业务已完成。不碰真实业务库，不重启服务；仅独立 PostgreSQL 验证。回退代码保留两本账及所有数据，禁止删表回退。正式产物、控制台接入和终态提交前恢复继续执行。
