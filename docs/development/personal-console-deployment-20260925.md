# 个人管理台简化版部署（2026-09-25）

按用户“请部署”的明确授权，现有个人端已从 `0.4.0-dev-connect` 切换到已验证的 `0.4.0-dev-console`。这是本机源码候选部署，不是新签名安装包或公开发布。[本批证据](../evidence/personal-experience/console-deploy-20260925/candidate.json)。

## 已运行与验证

| 项目 | 实际读回 |
| --- | --- |
| 入口 | `http://127.0.0.1:47611/agents`；旧 `/overview` 自动跳转 |
| 版本与防护 | `0.4.0-dev-console`；`block` 保持不变 |
| 进程 | 新 PID `1458311`，原 PID `820429` 已优雅退出 |
| 程序 | `var/flagship/console-deploy-20260925-r2/siq-agent-security` |
| SHA-256 | `2bcbde4bc46a95b4aa599a94d7ad1aa707f9209823951ddbc438e2dfe5ec7b2e` |
| 状态 | 沿用 `.tmp/flagship/as01-state-v2-20260921`，目录身份一致 |
| 数据 | 411 个原文件切换后摘要一致（运行锁除外）；12 份运行身份读回相同；回执链校验通过且内容不变 |
| 浏览器 | 11 项通过：四入口、权限台只读、风险/审计、旧工作台、连接、固定 24 小时会话、刷新不续期、退出、窄屏及无 JS 错误 |
| 浏览器检查后 | 原审计前缀保留、只追加；其他原状态文件不变 |
| 企业端 | API/Web 仍 healthy，未重启或修改 |

部署沿用原环境和已有业务权限，没有执行批量撤权、授权审批、宿主改配、模型调用或任务重放。线上浏览器验证仅创建临时管理会话、读取页面并退出；实际批量撤权仍使用上一批隔离夹具验收，不拿用户授权做试验。

## 切换过程与私密备份

首次预检查因已建立的浏览器连接暂停，旧服务未停止。核实应用 `serveLocalHTTP` 的 drain/Shutdown 流程后，在接收/发送队列为空、无未过期请求级身份、状态备份校验通过的条件下，使用 pidfd 对准确核验的原进程发送 SIGTERM。等待进程退出且 Writer 释放后，启动同目录状态的新候选；没有强杀进程。

首次浏览器脚本因“安全中心”附带真实待办计数而精确文本选择失败，未发生业务操作。选择器改按稳定路由定位后完整复测通过。失败轮临时浏览器已关闭，其服务端管理会话按固定期限失效；成功轮显式退出。不撤销其他用户浏览器会话。

私密备份、原环境、运行日志和部署脚本保存在 `var/flagship/console-deploy-20260925-r2/`（0700、Git 忽略）。首次未切换的准备记录保存在 `var/flagship/console-deploy-20260925/`，没有覆盖或删除。这些目录含真实状态/凭据，不能公开或提交。公开证据仅保留摘要与计数。

执行脚本：`deploy.py`、`browser_check.py`、`preservation_check.py`，均位于上述 `-r2` 目录。部署脚本是一次性精确进程切换，禁止不经复核重复运行。

## 使用与回退

用户在 Mac、服务在 Linux：继续使用原 SSH 本地转发，刷新原地址或打开 `/agents`。重启使旧浏览器会话失效；点击“通过智能体连接”，把页面的新请求交给本机 SIQ Skill 进行明确确认。仅确认浏览器会话，不授予业务权限。

旧程序保留在 `var/flagship/skill-connect-deploy-20260925/siq-agent-security`，SHA-256 为 `02acea34b368e7dfa693826f17e0133309609c0bcf940da6559f55689a8916e2`。回退前重新核对实际运行 PID、程序摘要、Writer 和在途操作，再优雅停止候选并用已保存的原环境启动旧程序。不得以备份覆盖当前状态或丢弃部署后的新增审计。本批未执行真实往返回退，也未新建系统服务/开机自启。

源码仍未提交/推送，既有签名发行包未更新；安装包分发需另行固定源码身份并按发行流程验收。
