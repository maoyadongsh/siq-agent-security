# 本机开发源码整合（2026-09-24）

用户在核查后明确要求按建议完成合并和提交。本批从远端主线 `af275efb1732d9bc1fcb9ea624e55a2ced58032c` 整理独立分支 `codex/local-development-integration-20260924`，保留 E163 的 `7b6e650`、`fd02384` 和 E164 的 `4a4bcd0` 原始祖先关系，再追加合同、实现和文档提交。整合目标为仓库 `main`，不创建发行标签或替换历史签名。

## 来源与范围

- 原主工作区公开源码快照 tree：`0f7735679f368ba1a1a6259f258c752dc7ebae94`。取样时主线与远端相同，暂存区为空。
- 与主线相比，快照包含 690 项文件变化；与 E164 相比为 367 项，后者包含 333 个新增和 34 个修改。两组数字为不同基线，不相加。
- 覆盖报告生成授权合同、企业业务导航、OpenClaw 执行复查、DGX Spark 诊断/原生 CI 门禁、发行源码清单校验，以及本机开发与验收记录。
- 16,590 个 `var/` 未跟踪文件为本机运行、构建或私有验收材料，排除并加入目录级忽略。公开 JSON 证据保留原文、原摘要和失败记录；私有候选/跨仓报告引用改为明确路径文字，避免干净检出中的无效下载链接。
- 历史 worktree、两个 stash、旧签名发行内容和明确排除的 PR #68 保留。兄弟仓库仅在既有证据中被引用，其源码与部署不属于本次 Git 整合。

## 本次独立源码验证

| 检查 | 结果 |
| --- | --- |
| Control API | `uv sync --dev --locked`、`uv run ruff check app`、全量 1089 项 pytest 通过，含合同与授权负向用例 |
| AgentShield | `gofmt -l .`、`go vet ./...`、`go test ./...` 通过；44 个包报告测试通过 |
| 客户端与 decision relay | Linux AMD64/ARM64、macOS ARM64、Windows AMD64 四目标编译通过 |
| Edge 与全部 12 个 Connector | 各模块 `go vet ./...`、`go test -race ./...` 通过；SIQ Connector 补入远端 CI 矩阵 |
| Web | 54 文件、299 项测试通过；企业及本地构建通过，本地内嵌产物与 E163 字节一致 |
| Hermes / OpenClaw | Hermes 143 项 pytest、OpenClaw 钩子/业务工具回归和 15 项检查点恢复测试通过 |
| PostgreSQL | 临时容器的空库迁移 0001→0017、空表降级/再升级、幂等/审计/行锁并发及非空降级拒绝，共 11 项通过；临时容器已清理 |
| 发行 / DGX / 仓库 | 18 项发行测试、40 项 DGX 测试、13 项仓库测试通过；导航、冻结证据、研究台账、合同校验及 CI 静态检查通过 |

测试日志保留于本机 `/tmp/siq-integration-20260924-n1PDt9/`，不作为公开运行产物提交。API 仍有 Starlette/httpx 弃用提示，本地 Web 构建仍有 500 kB 分块提示。

首次整合检查发现四处 Markdown 行尾空白、私有/跨仓链接不能在干净检出解析，已修正；曾误用 unittest 运行 pytest 风格 Hermes 测试，结果为零测试，随后改用 pytest 并通过 143 项，零测试结果不计入通过数。

秘密扫描首次检出 24 项，逐条核对为 23 项源码/凭据 SHA-256 摘要和 1 项 systemd invocation ID。只按精确文件、字段、值加入例外，公开证据未修改。新增校准实际运行扫描器，证明改值、改字段、复制到其他路径及新增凭据均继续检出。推送前全历史扫描覆盖 793 个提交、约 641 MB，无泄漏。

整合 PR 为 [#107](https://github.com/maoyadongsh/siq-agent-security/pull/107)。首轮远端扫描器校准暴露随机十六进制负向测试值偶尔达不到扫描熵阈值的问题，本机重建 CI 固定版本后亦复现。改为固定高熵合成值并增加不含凭据的缺失断言定位，两个本地扫描器构建均校准通过；不放宽扫描规则或证据例外。

## 交付边界

本批完成源码归档与合并验证。已部署服务仍保持各部署记录中的提交加补丁身份；Git 合并不自动重建、重启服务或把现有二进制改标为新提交构建。客户端正式签发与签名安装/升级/回滚、真实业务 IAM/授权、固定 Nemotron 和原生 CI/安装验收仍是独立待办。后续 SEC-F01–SEC-F10 及 Flow 环境故障保持原排期。
