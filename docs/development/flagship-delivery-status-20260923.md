# 本轮交付状态与剩余结束条件

**企业候选已部署（2026-09-24）：** 原平台安全 API/Web 已更新，实际数据库升至 0017；备份恢复、新旧镜像切回、原 Gateway 与桌面/手机登录页检查通过。企业代码部署已完成，真实账号/跨站业务联调和导航 origin 仍待接入。[当前运行身份、失败记录与恢复命令](enterprise-runtime-delivery-20260924.md)。

**客户端发行接续（2026-09-24）：** 新报告工具 ARM64 二进制已通过两条隔离原生启动流程；打包前源码清单校验已实测拒绝旧 E163 提交，18 项发行测试通过。正式源码提交、签发及签名升级仍待完成。[结果与具体交接命令](client-report-release-handoff-20260924.md)。

**当前收尾状态（2026-09-24）：** 报告工具客户端增量已通过 Go 44 包、共享合同校验及四目标构建；仍为未签名准备产物。主 API/Web 健康。剩余工作统一以[当前收尾清单](flagship-closeout-current-20260924.md)为准；下方早期部署状态仅为历史记录。

**2026-09-24 主 API 与页面切换完成：** 新主服务在 18081 运行，15173 已接入；两类恢复器真实就绪，实际主端口应急转接及新版冷启动恢复通过。沿用宿主运行方式与原 legacy 权限，不再以控制面容器化/AppArmor 改动为发布前提。报告工具授权、真实身份和正式发行仍待完成。[当前服务身份、降级恢复及结束条件](main-api-cutover-20260924.md)。
**2026-09-24 宿主依赖与恢复检查：** 固定源码的十项宿主连接、五项恢复器检查通过；默认 Docker AppArmor 拒绝用户服务管理，连接成功仅在一次性 unconfined 诊断中成立。原主恢复锁仍有效，未切主 API；服务管理约束仍须落实。[准确验证范围](api-host-runtime-readiness-20260924.md)。
**2026-09-24 API 恢复基线：** 1,637 项固定源码已在原路径容器挂载、独立恢复库下完成首次启动及冷启动，六项检查通过，容器/临时库清理。原主服务仍在运行；这证明当前版本可冷启动，不代表原旧进程的源码恢复或主 API 已切换。[范围与结果](fixed-api-recovery-baseline-20260924.md)。
**2026-09-24 镜像交付：** 当前请求沙箱镜像已切到真实报告 v8 候选，新→旧→新演练通过。主 API 的 994 项源码归档与待应用配置已准备，未停止旧主进程；报告工具的实际权限变更已形成具体变更单并请求用户决定。主 API 切换、真实身份和正式发行继续待办。见[切换结果与剩余条件](runtime-image-delivery-20260924.md)。
**2026-09-24 宿主切换完成：** 原端口 AgentShield 已升级，恢复原 Hermes 根配置；三份现有绑定通过业务消费端严格校验，历史身份/权限及 31 条签名回执保持完整。主 API、当前沙箱镜像、企业生产联调与正式发行仍未完成。见[实际切换、回退与剩余项](host-daemon-upgrade-20260924.md)。
**2026-09-24 部署收尾：** 实际库完成报告发布 024 迁移；15173 已连接新版 API 18089，报告确认/发布/读取及事件导航研究端已加载，切回 18088 再恢复新版演练通过。18084–18087 旧预览已停止；主 API/生产身份/企业控制台与发行门禁仍未关闭。[当前入口与回退](business-report-deployment-20260924.md)。

**2026-09-24 导航收尾：** 安全控制台新增已签名事件到原业务结果的可信入口，业务端独立检查当前账号及数据权限；控制台/业务端分段实际浏览器均通过。源码尚未部署到既有服务，不关闭连续跨站生产身份、正式发行门禁。[实现与验证范围](business-navigation-20260924.md)。

核对日期：2026-09-24。本页汇总 E171 及后续收尾修复的交付状态，不新增开发范围，不代替各批次验证证据，也不将原标杆目标标记完成。

**最新进展：真实模型报告交接 v8 通过。** 用户执行确认、八项材料生产、原运行保存、三身份独立复核发布、三个格式读回和签名回执核对已通过。六项浏览器检查另通过；既有服务及当前镜像未切换。安全控制台可信导航、主服务部署/生产联调、原模型与原生平台门禁、正式签发仍未完成。本批新安全二进制未包含在原冻结发行候选中，不能把剩余工程工作写成仅等待签发入口。本批验收与边界（外部仓库路径：`siq-research-engine/docs/reports/business-execution-confirmation-20260924.md`）。下方早期“模型交接未通过”等段落为当时记录，当前结论以上述 v8 为准。

## 收尾口径（回应本次进度追问）

没有及时收尾的执行问题是：功能候选交付、业务安全验收和正式发行一直混在同一进度中，持续补验收却没有明确给用户一个阶段结论。不能把这一问题归咎于用户尚未提供签发入口，也不能把仍未完成的工程工作写成仅剩外部依赖。

当前按以下三个交付范围核对，不新增功能、不插入外部安全审查后续任务，不重跑没有变更的已通过检查：

| 交付范围 | 当前结论 | 收尾还缺什么 |
| --- | --- | --- |
| 个人客户端及企业控制端功能候选 | 已冻结，客户端四平台候选包已生成；企业端冻结源码可审阅 | 正式签发与安装升级回滚尚未完成；企业端尚未替换在线部署 |
| 研究业务场景 | 本机预览已上线，真实 Hermes/OpenShell/Qwen 已有正常及故障链路证据 | 原任务中剩余运行/子任务/结果提交故障窗口、项目附件及报告等相关授权边界、真实账号与生产部署回退验收；现有预览仍保留执行门禁 |
| 标杆生产验收与发行 | 原总目标仍未完成 | 原生 CI、固定模型的质量/性能门禁、真实 IAM/审批/事件出口以及签名发行；不以合成身份、交叉编译或部分故障通过代替 |

其中可以继续自主处理的是剩余实现与定向验收；需要外部接入的是已询问的签发入口、真实业务测试账号/组织/登录入口，以及原生 runner/远端工作流的发布配置。用户选择“提供现有受控签发环境”尚不包含实际入口。具体依赖保留在下方结束条件表，不重复发起同一问题。

可查看入口：研究业务预览 <http://127.0.0.1:15173>；客户端体验包及固定候选包见下方“已交付的内容”。阶段交付不等于正式上线，不代表原目标完成。

## 最近验证记录

**2026-09-24 固定报告工具实现：** 报告生成已接入具备明确副作用和公司/运行范围的 MCP 工具；通用 terminal 仍拒绝。研究端 47 通过、1 因 SDK 缺失跳过，Go 全包 test/vet 与四平台构建通过；候选容器实际生成八项材料并验证摘要和权限。真实 SDK 工具发现及执行确认离线检查通过，但模型调用新工具到业务审核发布尚未验收，既有服务未切换。交付与下一项验收（外部仓库路径：`siq-research-engine/docs/reports/business-report-generation-tool-20260924.md`）。

**2026-09-24 真实模型报告交接未通过：** 隔离候选选版及授权请求形状修复后，真实业务 HTTP、八次模型桥请求、来源保存与释放通过；报告 terminal 命令仍被 `runtime_effect_unknown` 拒绝，零报告产物，审核准备 409。后续需要受限报告执行合同；不能将失败回复中的成功标记计作产物。两轮失败记录保留、资源清理完成，既有服务不变。具体缺口和本批测试（外部仓库路径：`siq-research-engine/docs/reports/business-report-model-20260924.md`）。候选交付可进行，完整标杆验收仍未完成。

**2026-09-24 实际公司报告脚本链路通过：** 修复运行 profile 的 Wiki 根目录解析后，在准确新候选中对已入库公司完成 prepared→ready_for_review，八项材料共 811395 字节且摘要匹配。数据库未连接，核查基于本地 Wiki；利息费用、市值、同业样本保留待复核，未批准发布。96 项回归通过；这次禁网 Docker CLI 检查不替代原生模型/业务身份端到端验收。结果及限制（外部仓库路径：`siq-research-engine/docs/reports/business-report-pipeline-20260924.md`）。

**2026-09-24 报告候选镜像已交付：** 新报告生产代码已装入准确候选镜像，实际容器摘要、非 root/私有 profile 权限、两处 CLI 导入、MCP 和网关离线检查通过；16 项构建相关用例通过。前两轮权限和 builder 兼容失败已修复并保留记录。当前镜像指针与四个既有服务不变，未执行完整模型报告或业务部署。镜像与验证范围（外部仓库路径：`siq-research-engine/docs/reports/business-report-image-20260924.md`）。

**2026-09-24 Hermes 报告材料入口补齐：** 请求级报告自动使用本次运行目录，调用精确事实核查与质量校验后生成审核清单；生产器到实际消费器及负向用例等共 72 项通过。未构建新版沙箱、未部署或迁移业务库，未声称真实自然报告验收完成。实现与验证边界（外部仓库路径：`siq-research-engine/docs/reports/business-report-input-producer-20260924.md`）。本批候选引用对应 evidence，后续只处理收尾清单原剩余项。

**2026-09-24 报告页面与按钮链路完成：** 研究前端新增报告目录、材料查看、独立复核批准和指定版本读取，从运行结果可准备审核包。25 个不同后端用例和 58 个不同前端行为用例通过；真实 Chromium → Vite → API → 隔离 SQLite 六项检查通过，包含实际授权撤销和手机页面检查。entry/list 两份 HTTP 合同校验通过，构建、严格类型和本批静态检查通过。未部署到既有预览或主服务，未迁移业务库；剩余自然报告产物、安全控制台可信导航和生产验收不变。实现、失败历史和验证范围（外部仓库路径：`siq-research-engine/docs/reports/business-report-ui-20260924.md`）。

**2026-09-24 报告审核发布后端结项：** 五个 HTTP 端点、固定材料副本、独立复核员批准、版本并发约束、指定版本读取及迁移 024 已实现。八模块组合 170 通过、2 个迁移数量断言失败；修正后定向补跑 2 通过，共 172 个不同用例通过，无跳过。七份 HTTP 输出通过六份 Schema 校验。源码和 024 未部署，真实报告产物、前端目录/审核/版本操作与安全控制台导航尚待接线；不将后端验证写为全链路完成。新增模块 Ruff 通过，既有聚合路由仍有 21 项 B008，详情见阶段交付（外部仓库路径：`siq-research-engine/docs/reports/business-report-publication-20260924.md`）。当前重新确认两个固定工作树干净、预览 HTTP 200；不重复已有全量测试。

**2026-09-24 正式报告来源绑定：** 原请求计划、业务范围和输出目录身份已纳入结果保存 v3 信封及原子审计，历史 v1/v2 继续可读且不补造来源。实时授权查询及逐级 no-follow 目录检查已实现；112 个选定用例通过，包含独立 PostgreSQL 补跑并清理临时库。公开结果接口不新增报告发布声明；源码尚未部署，reader 升级与回退限制已记录。实现范围及剩余链路（外部仓库路径：`siq-research-engine/docs/reports/business-report-source-binding-20260924.md`）。正式批准、发布、指定版本读取及前端接线仍未完成。

**2026-09-24 监督退出终态缺口关闭：** 核对原恢复合同后修正诊断：运行租约不续期，终态关闭时间单独验证，原租约到期后的 `authority_lost` 不误判为普通 `failed` 缺失。20 项诊断测试及 2 项恢复合同测试通过；v4 真实 HTTP 故障验收全部通过，子凭据在父身份仍有效时已撤销，沙箱/临时库/测试服务清理，既有服务与预览未变。v1–v3 失败历史保留。仅诊断和文档修改，无生产代码部署。[报告与证据](business-supervisor-exit-acceptance-20260924.md)。正式报告发布与权限读取是下一项工程工作。

**2026-09-24 本次阶段交接：** 正式业务入口的监督退出 v2 已结束。实际注入信号后 API 保持存活、无成功 done、沙箱回收及子凭据撤销通过，临时库与测试服务已清理。但数据库终态不符合诊断的 `failed` 预期，整轮保持失败；不推断未保存的实际终态、不改断言冒充通过。本次不再开启新测试批次，候选及预览按现有边界交付。[实际结果与待闭环项](business-supervisor-exit-result-20260924.md)。

**2026-09-24 排队过期、第二租户与错误提示收尾：** 真实 HTTP 排队约 60 秒自然失权、第二服务端租户拒绝仍有效原授权的两项验收通过。首轮暴露 500，已修复为固定 403/503 和中文说明，流式不提示自动重放；226 项不同用例均执行通过（包含独立 PostgreSQL 补跑）。修复已部署到预览 **18088**，七项检查与切回 18087/恢复新版通过，主服务和模型不变。[报告、失败历史及边界](business-queue-tenant-acceptance-20260924.md)。正式 API 监督退出及正式报告链路仍继续。

**2026-09-24 附件修复预览交付完成：** <http://127.0.0.1:15173> 已切至 **18087**，七项部署检查及实际切回 18086/恢复新版通过；主 API、模型和桥未重启。下方附件“尚未部署”现仅指主服务。[部署报告](business-preview-attachment-update-20260924.md)。同时完成[业务故障证据对照](business-runtime-acceptance-matrix-20260924.md)，不重复已有通过项，队列过期、第二租户和正式报告业务链等未验证项仍保留。

**2026-09-24 本轮收尾核对：** 项目会议附件的跨项目传入、符号链接下载、PDF 解析目录及历史复用边界已修复，九个相邻模块 **127 项测试通过**；源码尚未部署，预览仍为 18086 原进程。见修复及验证范围（外部仓库路径：`siq-research-engine/docs/reports/project-attachment-access-20260924.md`）。客户端和企业端冻结提交再次核对一致。本轮不再增加功能或插入外部审查项；剩余工作以[有限收尾清单](flagship-closeout-20260924.md)为准，未完成项不删减、不冒充外部阻塞。

**2026-09-24 结果保存修复已部署到预览：** <http://127.0.0.1:15173> 现连接 API **18086**，已加载上一轮的结果先提交、后清理修复。六项真实部署检查和切回 18085/恢复新版演练通过；主 API 18081、旧 API 18084/18085 未重启。预览仍保留必需恢复门禁，尚非正式执行/真实账号验收。[入口、证据与撤回](business-preview-result-update-20260924.md)。下方“结果提交顺序修复待部署”现在仅指主服务；历史报告不改写。

**2026-09-24 未持久化子任务验收通过：** 诊断显式触发已有 Hermes 修复函数，确认主任务结束且子任务正在产生模型输出后终止隔离 API；恢复器仅持有原主任务行，约 124 秒后自动回收整个执行环境，无重放/换绑，子凭据 401，临时资源清理。28 项证明器测试及 1 项修复函数测试通过。[实际范围](business-unknown-child-recovery-20260924.md)。不冒充财务分析自然触发或多层委派验收。

**2026-09-24 结果提交顺序修复（待部署）：** 已确认主任务及子任务终态的回复先提交结果和同事务审计，再删除运行资源，避免清理成功后回复尚未保存。229 项组合回归全部通过，包含独立 PostgreSQL；实际提交前/后两轮 SIGKILL 均通过。提交前回滚无伪造结果，提交后重启可读原回复、撤权 403，约 123 秒自动回收且无重放，原服务/预览未变。[报告和限制](business-result-commit-recovery-20260924.md)。尚未提交的内存正文恢复、未知 writer 后置保存及在线部署仍不能记为完成。

**2026-09-24 Hermes 创建确认后崩溃验收通过：** 真实任务已创建但任务 ID 尚未入库时，仅终止隔离 API；同库重启后等待租约自然到期，约 134 秒自动释放原执行。未重放、子凭据 401、临时数据库和服务清理、既有服务与预览身份保持一致；21 项定向测试通过。[证据与边界](business-create-ack-crash-20260924.md)。这关闭原 run ID 保存窗口，不代替其他子任务或结果提交故障验收。

**2026-09-24 原生 CI 核实：** 修复工作流使用不存在的 `runner.labels` 上下文，改为只读核验当前 GitHub 作业，40 项门禁测试通过。远端真实查询显示仓库注册 runner 为 0，原生工作流尚未出现在远端；未推送、注册或伪造 CI 运行。[修复与外部依赖](native-ci-readiness-20260924.md)。

**2026-09-24 预览已更新：** <http://127.0.0.1:15173> 现连接新 API 18085，已加载下述近期修复；六项实际部署检查、切回旧预览再切回新版的演练通过。主 API 18081 和旧预览 API 18084 保持原进程。下方历史记录的“待部署”现指尚未部署到主服务；预览仍保留必需恢复授权门禁，不开放缺少恢复权限的 OpenShell 执行。[当前入口、验证与撤回](business-preview-refresh-20260924.md)。

**2026-09-24 共享 Wiki 脚本修复（待部署）：** 引用、附注和三表工具的内部公司解析及文件读取已接入原请求范围，受限缓存按公司隔离。145 项回归、18 项打包测试通过，实际安装 wheel 与源码一致；相同代码跨市场隔离和授权公司财务表格正常读取均验证。修复、安装及边界记录（外部仓库路径：`siq-research-engine/docs/reports/shared-wiki-scope-20260924.md`）。在线服务未切换；项目附件、报告发布权限及原生产验收不据此记为完成。

**2026-09-24 聊天附件修复（待部署）：** 普通聊天和专业智能体的普通/流式入口统一核验附件所属用户、文件身份和路径；PDF 解析路径由服务端推导，不采信客户端路径；历史附件复用同样过滤跨用户引用。74 项回归通过，含四个 HTTP 入口的跨用户拒绝及正常附件读取。修复与验证范围（外部仓库路径：`siq-research-engine/docs/reports/chat-attachment-access-20260924.md`）。共享 Wiki 脚本、项目附件及报告发布权限仍须分别核实，不据此关闭全部宿主检索验收。

**2026-09-24 宿主数据库修复（待部署）：** 财务兜底已绑定业务授权的市场/公司及市场数据对象权限，多市场查询强制规范公司 SQL 条件，CN 旧查询不再从未命中的授权代码退回其他公司名称。149 项回归、实际 PostgreSQL 独立库 11 项检查通过，临时库已删除；同 ticker 但公司 ID 冲突的记录也被拒绝。修复记录（外部仓库路径：`siq-research-engine/docs/reports/host-database-scope-20260924.md`）及[源码绑定证据](../evidence/flagship-optimization-20260921/host-database-scope-20260924.json)。共享脚本、报告/附件等剩余边界及在线部署仍未完成。

**2026-09-24 宿主 Wiki 修复（待部署）：** 机密请求的 API 公司解析绑定已授权目录，相关 JSON/Markdown 和报告路径拒绝目录越界、符号链接及目录替换；并发和流式任务范围隔离通过。真实业务首轮暴露英文连通性测试被误判为财务问题，修复并加入中英文财务负向测试后，原真实 Hermes/OpenShell/Qwen 验收通过；临时资源清理、原服务未变。数据库兜底及共享脚本内部读取等边界仍未全部验证。修复、失败及成功证据（外部仓库路径：`siq-research-engine/docs/reports/host-wiki-scope-20260924.md`）。

**收尾期间新增修复（待部署）：** 研究业务宿主记忆读取、请求消息保存和沙箱流式结果保存已补传后端验证的租户/用户，避免错误使用默认租户；公司选择补齐顶层和嵌套字段的一致性检查，拒绝跨公司及跨市场提示。236 个不同定向测试通过，旧版/新版合成参数对照确认原缺口及修复行为；未重启在线服务，未关闭整个宿主检索隔离验收。源码合同及验证范围见研究业务合同补充（外部仓库路径：`siq-research-engine/docs/architecture/qwen38-request-runtime-v1.md`）。客户端与企业端冻结候选保持不变。

E171 完成沙箱创建结果未保存、监督 invocation 未保存两轮真机验收：自然租约到期后保留原行/占位、恢复未就绪，无自动重放；诊断独立核对并补齐本次观察后，API 才完成清理。32 项诊断测试通过，临时资源清理，原服务和预览未重启。[报告及明确限制](business-unknown-startup-e171-20260923.md)。

E170 已完成实际 API 在沙箱创建后、身份签发与监督启动前被 SIGKILL 的验收：同库重启后等待原租约自然到期，API 自动释放沙箱并记录原执行失权，不恢复业务。48 项诊断测试通过；临时资源清理，主服务及预览未重启。[报告](business-early-crash-e170-20260923.md)。

E169 已修复并真机验证失败 ExecStopPost 的自动清理重试：独立身份服务中断导致原退出清理及 API 扫描失败，恢复依赖后 API 自动释放原执行、撤销子身份，磁盘证明与清理均确认。189 项组合及 16 项诊断测试通过；主服务及预览未重启。[报告](business-cleanup-retry-e169-20260923.md)。

E168 已启动研究业务本机预览 <http://127.0.0.1:15173>，页面连接新 API 18084；构建、结果接口匿名拒绝、桌面/手机登录页及撤回后重启均通过，旧 API 保持原进程。预览不持有恢复权限，保留必需恢复门禁，尚不提供依赖该恢复器的 OpenShell 执行；未完成主服务切换或真实账号验收。见 [E168 部署边界与证据](business-preview-delivery-e168-20260923.md)。正式签发、升级回滚及其他原目标门禁仍待完成。

E167 已对实际研究业务库完成 022/023 迁移，完整私有备份恢复、迁移演练和实际库幂等审计通过。API 未重启，新代码/页面部署仍未完成，见 [数据库交付报告](business-database-delivery-e167-20260923.md)。

E166 已修复初始输出流撤权复核缺口，补齐模型输出后在途业务 grant 撤销的实际验收，并验证正常 SSE 完整完成，见 [撤权验收报告](business-grant-revoke-e166-20260923.md)。在线 API 尚未部署该修复。

E165 已补齐“模型输出后 API 崩溃、同库重启回收”的实际验收，见 [故障验收报告](business-api-crash-e165-20260923.md)。客户端、企业端冻结候选与下面交付包均保持不变。

## 为什么尚未结束

执行中没有及时把现有功能交付、正式发行、生产标杆验收分开管理；持续增加验收批次，却没有给用户一个明确的交付入口。客户端功能现在已经冻结，企业控制端既有代码也已冻结。继续重复正常路径回归或扩展体验功能，不是当前收尾的必要条件。

整个原目标仍有未完成项，不能归因于“只差签名”，也不能用累计测试数或早期约 75% 的估算表示当前整体完成度。

## 已交付的内容

| 交付范围 | 已核实状态 | 依据与限制 |
| --- | --- | --- |
| 个人客户端 | 环境发现、Hermes/OpenClaw 接入、权限操作、运行记录和输出查看已有实现及分批真实链路验证；源码冻结 | [客户端冻结报告](client-release-freeze-e163-20260923.md)。固定提交 `fd02384d6de5f96a849f9d04bbfbfaff38cf6148`；Go 44 包、前端 290 项、四平台构建通过。交叉编译不等于四平台原生安装通过 |
| 企业控制端 | 既有 API、Edge、SIQ Connector、企业前端和验收脚本已冻结 | [企业端冻结报告](enterprise-source-freeze-e164-20260923.md)。固定提交 `4a4bcd0b3695dfa56e73c24d8f42a547fe94be31`；API 1071 项、隔离 PostgreSQL 11 项通过，Edge/Connector 测试与构建、企业 Web 构建通过。未替换在线部署 |
| 研究业务案例 | Hermes/OpenShell/本地 Qwen 正常非流式、流式请求和输出后取消已有真实验证；业务结果读取与历史页面已有分批验证 | [执行台账](flagship-optimization-progress-20260921.md) E126–E130、E156–E160。合成企业授权与候选 API 不能替代生产 IAM；研究业务更新没有随客户端包自动部署 |

本次只复核了两个冻结工作树的提交与干净状态，以及以下交付包 SHA-256；上述测试数来自对应冻结报告，没有在本次重复运行全量测试。

- DGX Spark / Linux ARM64 体验包（本机私有路径：`var/flagship/ux-e162/siq-agent-security-linux-arm64-preview-e162.tar.gz`）：`13c9ae5a946252ee9d746f0f8b2e0b05cf7de3da9a5662406d5abe62c1b610e5`。这是 E162 独立状态体验包，尚非正式签名发行包。
- 0.4.0-rc.1 四平台未签名候选包（本机私有路径：`var/flagship/ux-e163/final-unsigned-release/siq-agent-security-0.4.0-rc.1-unsigned-candidate.zip`）：`f93550a6688f6f74a27d63624c06f060904640e8b303df0d32d73966bdc731b7`。来自 E163 固定源码，不包含企业端或研究业务服务部署。

现有本地候选未合并、推送或发布；主工作树既有改动保留。

## 尚未完成及明确结束条件

| 剩余范围 | 结束条件 | 当前依赖 |
| --- | --- | --- |
| 客户端正式签发 | 固定 E163 源码使用原发行身份签发，包内容与清单独立验签一致 | 用户已选择使用现有受控签发环境，但具体脚本路径、密钥引用或执行入口尚未提供；已有问题待答，不重复索取，不需要密钥正文 |
| 签名安装、升级与回滚 | 独立实例完成签名安装、已验签 0.3.1 → 新候选升级、历史保留和受支持回滚；负向校验拒绝篡改 | 旧版基线已核验，新候选尚未签发。[既备命令与验收步骤](client-signing-handoff-e163.md) |
| 原业务链故障与授权验收 | E165/E166 完成输出后 API 崩溃/重启与在途撤权；E169/E170 完成退出清理重试及已记录沙箱的早期崩溃恢复；E171 完成沙箱创建/监督身份未保存时的保留与诊断核对。其他业务 run/child/结果提交窗口和宿主检索隔离仍按原合同验收 | 属于原任务中的工程工作，不能以签发入口缺失为由停滞；指定故障时点通过不代表所有故障时点通过，诊断核对不等于未知状态自动恢复 |
| 企业生产集成与原生 CI | 真实 IAM、业务审批及事件出口联调，部署与回退验证，指定原生 runner 的固定源码验收通过 | 既有隔离数据库、合成身份或四平台编译不替代这些条件；具体现场接入信息按实施需要核实 |

原总目标保持未完成。客户端与企业端本轮功能冻结，不因上述生产验收重新扩展功能。存在实际失败时只修复与验收直接相关的问题；已通过的检查没有新变更或失败依据不重复执行。

用户提供的外部安全审查继续按 [采纳决议](security-review-arbitration-20260923.md) 和任务书 SEC-F01–SEC-F10 后续清单管理，不插队替代本轮交付，也不从未完成清单中删除。Host Sensor、风险评分等不得被写成已经实现。
