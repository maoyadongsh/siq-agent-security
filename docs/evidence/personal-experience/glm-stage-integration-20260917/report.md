# GLM 已提交成果阶段集成复核（2026-09-17）

## 集成范围

以 main `7a58ee783b5b1a1bcc9ddda2aff5e4678ce26518` 为基线，在独立工作树合并 `ff310716cc4c7e045456e3f44357f2c48baf3589`，保留 `f7a88ce`、`48f728e`、`ff31071` 三个既有提交。集成提交 `bc2b6ec` 保留两个父提交及既有作者历史。仅 `docs/agentshield-dev-spec-v1.md` 存在文本冲突，按并存方式保留 Mac 空配置保护、OpenShell 会话策略授权、缺失签名身份保护及策略加载等待要求。

另外提取 v6 已有的独立材料隔离规则：忽略根 `.artifacts/`、证据目录下 `.db` 与 `.db-*`。不把运行数据库、私钥或原始私有材料带入集成分支。

## 行为及安全复核

- 策略应用与回滚等待 CLI 加载确认，超时、不支持等待选项均拒绝，不无等待重试。
- 会话接口仅 `policy_apply`，不会执行任意命令；批准绑定目标、网络端点、程序路径、修订、Grant 摘要和后端指纹。写前复验当前授权。
- 缺失历史签名身份时拒绝自动生成新身份；首次引导仍允许创建身份。
- 确认最新 main 中 Mac 路径别名限制与 OpenClaw 空配置往返测试通过集成后的全量 Go 测试。
- 这次不启用真实网关/宿主，不提升历史证据等级，不签名、不发布产品。

## 本次验证

`checks.json` 记录本轮实际命令和结果：Go 全量与相关 race、vet、格式、225 项 Python 合同/OpenShell 测试、四目标 CGO=0 构建均成功。Python 一项依赖弃用 warning 保留。

`cross-builds.json` 是合并内容在提交前的构建摘要；构建发生于 main 基线加合并工作区，含 dirty VCS 元数据，是组件构建验证，不是已签名发行候选。历史性能及现场结果不能移用为本次二进制通过证明。

10 份合入的历史 SHA256SUMS 按各自路径基准全部通过；首次统一按清单目录运行时，baseline 清单的两个仓库相对路径无法定位，按其仓库根目录基准复核即通过，未修改清单或证据。见 `historical-manifests.json`。

最终 PR CI 状态以 GitHub 对准确头提交的记录为准；本报告不预先宣称 CI 或合并成功。

## v6 暂不纳入的部分

`glm/local-o05-v6-20260917` 的任务执行实现、UI、合同和候选证据仍保持原工作树状态，未 reset、搬走或覆盖。此次仅复制上述忽略规则。

阻塞完整任务执行合入的要点：启动前 ReadEffective 修订/摘要一致不等于执行平面加载确认；本地停止不能证明远端停止；新增锁、授权和审计修复后需要对应候选重新完成实际任务/浏览器/性能验收。D08 驱动修改后也需要独占目标重跑。应先修复或显式关闭未满足前提的执行路径，再申请独立 PR。不能用既有 policy_apply 等待修复代替新增 ExecTask 路径验证。

Windows/macOS 实机、WorkBuddy、托管源 DNS 与发行签名继续单独记账，不阻塞已验证的独立策略/身份修复代码集成。

## 合并方式与恢复

通过普通 PR 运行既有检查，保留审阅与主分支治理，不修改 CODEOWNERS 或 branch protection。不重写历史，不强推，不删除开发分支。若需要回退，在主分支以新增 revert 提交撤销对应 PR merge；不得 reset 活动工作树。回退会重新暴露已修复的签名身份/加载风险，须单独评估。
