# R04 当前候选更新与断连恢复集成验收

- 日期：2026-09-14
- 工作树：`/home/maoyd/siq/worktrees/siq-personal-v4-r01-20260914`
- Git 基线：`b303c6f92392f3a44c306d81ad7323c6291ef4f2`
- 未提交候选二进制 SHA256：`5ed4dc82bbb300b94155561f91322b818c32b8cc42109e068258162f42d8b961`
- 构建输入树摘要：1210 个当前 Go/Web 输入文件，组合 SHA256 `a51118ed88c852cc5be84f23cea51c0c6950d535b772fac692709e073f485189`
- 状态：本机集成证据已落盘；未提交、未推送、未合并、未发布
- 结论：**真实候选二进制 + 隔离 daemon/profile + Chromium 集成通过；宿主原生与真网上游仍未验，R04 保持 partial**

## 更新旅程

`skill-update-browser-smoke.py` 使用两个真实落盘的合成 Skill、全新隔离状态和 Hermes profile，完成 7 项：

1. V1 导入、权限确认与安装后，V2 在未批准时只展示内容及权限差异，V1 文件保持不变。
2. V2 批准后准备更新副本；刷新只读并要求重新核对，不能自动提交。
3. 桌面与 390px 移动宽度均能看到最终确认控件。
4. 双击确认只发送一次；服务已完成而响应丢失时，界面不盲目重放写操作。
5. 显式查询恢复成功结果；目标文件等于 V2，旧 Grant 已撤销，新 Grant 保持 approved 且没有自动激活运行身份。
6. 完成页刷新零写入，并链接到新版权限准备。
7. 第二次更新在准备阶段资源耗尽时进入可恢复状态；用户显式中止后保留上一版本文件和权限。

测试还验证 Hermes 配置文件前后逐字节一致。这里的“使用”是读取并核对安装目标文件，没有启动 Hermes
模型或在 Hermes 插件进程中执行工具，所以不标宿主原生通过。

## 管理与断连恢复

`session-browser-smoke.py --discovery` 在同一候选上完成 28 项，包括：一次性配对、Cookie 边界、刷新/第二
标签页恢复、同名 Skill 分离、显式重复扫描不生成重复身份、移动端导航、适配器预览取消零写入、卸载保留
未知文件、WorkBuddy 独立未验证状态、外部修改拒绝覆盖、跨标签页退出、daemon 重启后重新配对、手动范围
跨重启保留，以及 `/ui-config.json` 故障后的“重新连接”按钮恢复。页面无 JavaScript 错误和水平溢出。

## 尚未完成

- 公网 Git/ZIP 上游检查受当前 198.18/15 保留地址解析阻断，未启用生产 Git。
- 没有在真实 Hermes/OpenClaw 宿主中执行 Skill 工具，也没有 WorkBuddy Linux 运行时。
- 没有覆盖 Windows/macOS 实机；对应工作仍由 sunbo/Luke 的独立任务书推进。
- 本批没有完整的“真实上游发现 V2”链，因此不能把 R04/N03/N08/N09 标记完成。
