# F05 新 UUID 绑定候选的独立性能测量

2026-09-17，在真实 OpenShell 0.0.83 网关和本批专属沙箱上，Linux/arm64 本地候选 SHA256 `ceddc59c929da6ae449fdcb06f5e4f5915bf2e3ca73dad73457be421fbb194e7` 按测前冻结的 [`protocol.md`](protocol.md) 串行运行。协议 SHA256 `9dd516397cb76562964db8223f91ca573d9ac9515984bae36bb116e91c5cd049`。三轮完成，**150 个有效样本、0 个无效样本、0 个预算超标、324/324 驱动检查通过**；未剔除样本。原始逐样本数据与运行条件见 [`samples.json`](samples.json)、[`report.json`](report.json)。

| 场景 | 正式样本 | p95 | 最大值 | 测前预算 p95 / max | 判定 |
| --- | ---: | ---: | ---: | ---: | --- |
| S1 真实 doctor 读回 | 45 | 53 ms | 53 ms | 15,000 / 30,000 ms | 绝对预算通过 |
| S2 同内容策略加载 | 15 | 91 ms | 91 ms | 60,000 / 60,000 ms | 绝对预算通过 |
| S3 经批准的真实只读任务执行 | 45 | 787 ms | 792 ms | 2,000 / 15,000 ms | 绝对预算通过 |
| S4 已持久化任务状态读回 | 45 | 30 ms | 31 ms | 1,500 / 15,000 ms | 绝对预算通过 |

S3 只计执行请求的 HTTP 往返，预览、批准和绑定准备在计时区间外；它不是完整用户旅程时延。每轮 S2 后按协议做不计时的沙箱就绪条件化，时长和条件都留在原始报告。S1/S2 没有同协议独占重测 main 基点，故相对性能 **not_measured**；S3/S4 在基点无对应路由，故 **not_comparable**。旧候选 `c8048691…` 的测量不能与本轮混为一份候选报告。

第一次启动在 P01 排他预检发现无关 `rsync` 竞争进程，**0 个正式样本**，随即停止；失败报告、样本空表与原因保存在 [`invalid-preflight/`](invalid-preflight/README.md)。该进程结束后，在原冻结协议和相同候选上重新进行独占测量；这不是按性能结果重跑择优。报告记录的 929 个源文件绑定在本轮前后稳定，[`source-hashes.json`](source-hashes.json) 可复核；源绑定在构建后二次观察，不能单独证明脏工作树的可复现构建。测量完成后，D05 基础驱动新增 K02 的 `probe.ok` 早期拒绝回归；该脚本变化不在前述 Go/embedded 源绑定内，也不回写或改变历史样本。

本批专属沙箱 `siq-v6-f05-identity-20260917` 原 UUID `b2044454-289a-4d06-bc27-2c6091d4f2b6` 经列表核对后按确切名称删除，只读列表确认不再存在；共享网关未重启。隔离 daemon 状态根由驱动清理。原始私有材料仅留在忽略的 `*-private/`；公开 `SHA256SUMS` 不包括私有目录。此次结果是本地未提交候选的真实网关**绝对预算**验收，不等于发行性能验收或其他宿主/OS 测量。
