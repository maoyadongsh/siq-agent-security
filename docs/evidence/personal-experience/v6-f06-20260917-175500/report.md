# F06 Linux 隐私、失联与通知阶段复核

候选二进制 SHA256 `c8048691276590a064b5314837e35c80a2e8fba48471f1bf12c434f6b9ae9853`；工作树仍未提交。各腿分别计数，不能合成一个完整旅程通过。

| 腿 | 当前候选结果 | 实际证据等级 | 未覆盖 |
| --- | --- | --- | --- |
| OpenClaw 服务失联及副作用 | 16/16，脚本退出 0；真实 `openclaw agent --local`、真实插件钩子，授权写入正控、daemon 停止后文件/网络独立副作用为零，四条 pending 恢复后进入签名回执链 | Linux 原生 OpenClaw + 隔离模型夹具 | 无授权出网正控；Hermes/WorkBuddy/其他 OS 未由此证明 |
| OpenClaw 原生原文采集 | 19/19，脚本退出 0；托管插件真实钩子在显式任务 Grant 后采集参数、结果各 1 条，管理端逐字段读回匹配真实调用；只撤销原文 Grant、保留运行时身份时，同一原生会话的授权读取继续执行但无新原文记录；之后撤销身份，所有新调用拒绝 | Linux 原生 OpenClaw + 隔离模型夹具 | Grant 自然到期的原生新调用、真实用户操作及其他宿主仍待 |
| Hermes 原生 SEC/归属支持腿 | 11/11，脚本退出 0；真实 Hermes CLI/插件/工具生命周期，跨 Skill、跨任务身份复用拒绝与回执归属验证 | Linux 原生 Hermes + 隔离模型夹具 | 本腿没有停止 daemon；失联另见独立腿 |
| Hermes 原生失联 | 最终加强版 9/9，脚本退出 0；在线授权读取正控后只停止本批 daemon，原生读/写被拒，受保护正文及文件副作用均未发生，回执链不变 | Linux 原生 Hermes + 隔离模型夹具 | 无出网副作用正控；写入腿在线亦缺授权 |
| 双任务 export/trace-export | 192/192，脚本退出 0；跨任务隔离、签名回执、快照及撤权后历史行为 | 真实 daemon HTTP + 合成运行时绑定任务 | 原生宿主采集、同候选自然到期与响应组装并发突变 |
| 计数通知 | 6/6，脚本退出 0；同候选固定摘要、真实 Linux session D-Bus `Notify` 方法、仅计数正文，无工具参数泄露 | 会话总线传输 + 自动化 hold 夹具 | 人眼视觉显示、点击跳转、其他 OS |
| 原文到期 | 两份独立三任务 seed 经真实墙钟等待后各 19/19 verify 通过；过期密文定向清理、24 小时存活腿、导出隔离均已核验 | 本机隔离 direct-process daemon + 产品原文 API，旧候选 | 原文 Grant 到期后的原生宿主新调用、桌面视觉及新 UUID 候选未覆盖 |

逐腿原始结果分别见 [OpenClaw 失联](../v6-f06-openclaw-service-down-20260917-173000/report.json)、[原生采集与撤权](../v6-f06-native-final-20260917-180200/report.json)、[Hermes 原生 SEC](../v6-f06-hermes-native-final-20260917-181200/report.json)、[Hermes 失联](../v6-f06-hermes-offline-final-20260917/report.md)、[双任务导出](../v6-f06-export-20260917-173500/runner-output/report.json)、[通知总线](../v6-f06-notify-bus-20260917-174100/report.json) 和 [到期状态](../v6-f06-expiry-20260917-174400/status.json)。导出报告自有 `SHA256SUMS` 已复核。当前候选通知脚本接受显式 `--binary` 与 `--expected-sha256`；错误摘要在启动前以退出码 1 拒绝。一次较早的通知试跑自行编译了不同二进制 `9d90b53f…`，留在原目录，仅作为非同候选历史材料，不计入上述 6/6。原生采集初跑公开 JSON 含完整本地 Grant ID，已移入忽略的私有目录；随后 17 项重跑验证脱敏，19 项重跑加入原文 Grant 撤销。第一次 19 项报告只列了前四次模型请求，最终报告修正为全部八次；两轮 19 项均通过，最终报告为本批依据，不择取失败样本。Hermes 原生 SEC 初跑 11/11 后只补证据文件属主权限，以第二轮同候选 11/11 为本批依据。Hermes 失联初跑 9/9 后增加了原生读取正文不泄露断言，最终重跑仍 9/9；两份结果都留档。

到期 seed 首次使用相对 run-dir 时，脚本相对可执行路径在切换 cwd 后无法找到程序，daemon 未启动；失败私有目录保留。修复为记录绝对 run-dir 后，同候选的绝对路径 seed 和相对路径回归 seed 都通过；随后分别在 UTC 10:32:16 和 10:35:46 的真实到期点之后，以同一旧候选二进制逐份 verify，均退出 0、19/19。两份种子、状态、密文及配对过程仅保存在忽略的 `expiry-private/`（不进公开证据）；未改系统时钟。公开 [脱敏逐项结果](../v6-f06-expiry-20260917-174400/verify-summary.json) 及独立清单可复核。私有结果是在 runner 终版权限收紧前生成，验收后已逐项归一为目录 0700、文件 0600；后续运行的输出创建权限和拒绝覆盖另有回归测试。

本批 daemon/模型/接收端由各脚本隔离创建并结束；没有停止已有 OpenClaw 或 Hermes 服务。F06 仍为 partial，尤其原文 Grant 自然到期在原生新调用上的停止行为、桌面视觉确认和尚未覆盖的其他系统仍待完成。Linux/WorkBuddy 已退出本机目标，不作为待解锁腿。
