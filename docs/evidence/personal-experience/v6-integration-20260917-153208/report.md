# v6 集成阶段报告（2026-09-17）

## 1. 状态

**整本 F00–F10 任务书尚未完成。** F00 为阶段完成；F01 与 F03 仅有组件级成果，F02 在组件级补齐停止竞态与持久证据签名但远端停止/重启旅程仍未验证；F04–F07 真实同候选、性能、Linux 原生采集与企业服务腿未关闭；F08 环境条件阻塞；F09 仅形成跨平台复测交接；F10 处于阶段验证与材料整理。原 D/E/B/N/R/O 分母均保持，不从旧候选迁移 `passed`。

本阶段新增的安全修复：同一执行键第二条运行在 spawn 前拒绝，停止请求与本地句柄取消在同一锁内按“先持久化、后动作”完成；任务计划/发起/结果/停止证据均签名，读时核对签名、种类、预留 ID；`succeeded` 还要求签名 observation 绑定同一结果摘要。篡改、缺签名、错绑定不从可编辑 JSON 投影成功。
这些签名沿用本机 daemon 密钥的既有信任边界；能读取并使用该密钥的同一 OS 身份或 root 仍可伪造本地材料，不能把它描述成独立硬件/远端见证。

## 2. 验证与级别

实际命令/退出码见 [checks.json](checks.json)：Go vet、全量测试、openshell/server race、定向负例、Python 合同与全量测试、Ruff、Web 测试及 enterprise/local 构建、四目标交叉构建、review guards 与 `git diff --check` 均退出 0。新 Go 修复后重跑全量 Go、race、定向负例及四目标编译；其他未改动域沿用本集成树此前同批验证。所有这些是**组件、静态或构建级**，不是网关、浏览器、企业服务或 OS 原生验收。

CLI 能力只读复核见 [F02](f02-stop-capability.md)；本机 DNS/WorkBuddy 条件见 [F08](f08-conditions.md)。F04 真实执行、E01–E13、浏览器、F05 独占性能、F06 宿主原生采集、F07 企业 HTTP 到网关、Windows/macOS 实机均未在本阶段运行。

## 3. 缺口与解除条件

- F01：当前进程内加载证明绑定调用配置指纹、目标、修订/摘要及 `Loaded` 标记，但仍无可信的**网关实例身份**与完整 network binary 范围。须在专属目标核实与实际版本匹配的加载/身份协议，补缺陷和负例；未通过前不把执行路径视为生产已验收。
- F02/E08：本机 v0.0.83 CLI 帮助中没有按单次执行 ID 查询/停止入口。网关内部协议未核实；本地取消不等于远端终止。需同版本官方协议与专属沙箱独立效果验证，或保持 `uncertain`。
- F04/F05：源码尚未冻结为最终候选，且 F01 实机前提未通过；真实网关矩阵及独占性能均待新候选。旧 D05/D06 证据不直接沿用。
- F06/F07：宿主原生采集、桌面通知与企业服务身份/租户/策略恢复仍需各自真实入口。组件与 HTTP fixture 不代替。
- F08：GitHub 三域名在本机仍解析到 198.18/15，WorkBuddy 可执行宿主未找到；不得放宽 SSRF 或冒用 CodeBuddy。
- F09：sunbo/Luke 需在同一稳定候选上完成各自 Windows/macOS × 宿主矩阵；本机交叉编译不计实机。

## 4. 来源与候选

基线 `9b8c09af742cb9df94c6d02e6f2db6ecccd68951`；来源/采纳与排除见 [integration-map.md](integration-map.md) 和 [source-summary.json](source-summary.json)。当前工作树有未提交改动；[candidate.json](candidate.json) 只绑定**组件快照**、embed 与四目标未签名二进制 SHA，不是 F04 冻结候选或发行制品。原 GLM 工作树、主线工作树与历史证据均未改写。

## 5. 影响、恢复与 Git

新签名规则使旧试验性无签名任务证据返回 `task_evidence_unverified`，不自动升级、不重放；这是保守兼容边界。保留原文/导出、旧 `policy_apply` 路由与旧审批语义。当前分支 `codex/personal-v6-integration-20260917` 未 commit、push、merge、签名或发布；后续应按任务书 §13.2 对安全核心、状态/UI、真实驱动及台账拆分审查，中间提交各自可构建测试。发现需要回退时保留本集成树与私有 F00 快照，按已审查补丁撤销，不 reset 原 GLM 工作树。

## 6. 资源

本阶段没有创建正在运行的服务、监听端口或沙箱；已确认归属且保留的私有快照和四平台未签名二进制见 [resources.json](resources.json)。共享网关与既有监听未重启、未改配置。尚无 F04 专属测试目标，也没有将旧沙箱作为本批资源清理。
