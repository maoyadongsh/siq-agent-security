# R07 旅程证据复核（2026-09-17）

本次仅复核[原 R07 批次](../v6-f06-r07-journey-20260917/report.md)的源码断言、机器可读结果、候选副本和源摘要，未启动宿主、浏览器或 daemon。原五文件清单全部通过；候选 `0b16e5e0…`、931 文件源绑定 `ba8d266e…`、两轮脚本摘要 `e1d9e4f6…` 相符。成功轮 25 个唯一旅程检查与嵌套 R04 的 30 个检查均记录 pass；失败轮保留 16 个已通过检查和 TimeoutError。原文件摘要见 [review.json](review.json)，原证据不改写。

## 采纳范围

可以采纳同候选、真实 OpenClaw、内嵌 UI、headless Chromium 和本地确定性模型下的**现有脚本范围旅程**，修正旧台账“尚未跑同候选旅程”的笼统表述。证据包含实际原生 SEC 读取、配对/恢复/退出、审批 UI 与 resolve 重放拒绝、更新流程、原文 opt-in/撤销、未过期数据保留及默认导出排除。

F06/R07 仍为 partial：R06 已安装 systemd 服务到旅程的联合验证、原生宿主 hold 消费、权限范围内越权、任务专属导出生命周期、真实保留期到期删除、桌面通知/人工视觉及其他 OS 都不能据本次关闭。Hermes 仍有独立分腿证据，不继承 OpenClaw 的完整脚本旅程。

## 两项复核限制

1. 原报告将第一次失败判断为“瞬时等待 flake”，证据不足。可确认的是**一次 phase-c 超时后，同命令重跑通过**；失败 JSON 只有 TimeoutError 和前 16 个检查，没有具体 locator、等待阶段或安全的 DOM 状态摘要。根因保留未确认。后续宜记录不含输入值的固定阶段 ID 与等待目标类别，不能只靠重跑通过关闭稳定性问题。
2. `r07_step4_attribution_correlates_grant_install_sec` 校验 Grant 相等、content_hash 非空、source_digest 非空，未校验两者的对应关系。嵌套 R04 的 V1 检查也有同样限制。现有结果证明字段存在及其他独立 SEC 断言，**未单独证明安装摘要到 SEC/回执的完整密码学关联**。不能直接比较不同摘要域；后续应按安装记录、managed-instance、SEC 及回执各自字段的关系补正向和摘要错配负向断言，再重新跑该腿。本复核不把原 pass 改造成补验完成。

## 验证与交付

复核通过：25 项唯一 ID 与结果一致、嵌套 30 项逐项 pass、两轮候选和脚本摘要一致、当前产品源摘要一致、私有目录 0700/文件 0600 或候选可执行副本 0700。只更新当前任务书、台账和阶段报告；未改产品实现或旅程驱动，未 commit/push/merge/发布。
