# F07 企业 HTTP 服务至 OpenShell 阶段验收

本批专属沙箱 `siq-v6-f07-174600`、UUID `443352f0-3b3e-43b5-90c8-0abfa7697056`，初始策略 Hash `21ca0b7c8c0ee3fe71660bb18960bdb2d572b6904f6a22338eba727ca26add43`。现有 GLM D08 驱动启动真实 uvicorn 服务进程，通过生产 HTTP 路由执行；以独立 CLI 读回验证网关状态。当前集成工作树的企业 Python 实现未在本腿前冻结成发行候选。

脚本退出 0，[结果](d08-service-gateway.json)为 33/33、fatal null。验收覆盖开发环境中的身份缺失 401、viewer 403、跨租户对象 404、提议者自批 409、未启用后端 409；已批准变更应用后 `readback_verified/config_readback`、独立读回和 `receipt-verify`；授权回滚、重复回滚拒绝、二次应用；全新服务进程的未知操作回滚 502 且后端零变更；网关预检超时 502、没有部署行且后端零变更。服务外清理恢复初始 Hash，最终版本 5。删除本批精确 UUID 沙箱后，后续只读列表确认已缺席；共享网关没有重启或配置修改。

此腿只证明**隔离开发身份、SQLite 与真实 HTTP 服务**的行为，不证明生产 OIDC/PostgreSQL、跨进程原子 CAS、远端执行拦截或 `enforcement_verified`。驱动在测量后只补了 `umask(0077)`，使以后公开摘要也按属主权限创建；本次结果文件已单独修正为 0600。运行时驱动文件的独立哈希未预先留档，因此不是完全冻结的发行候选。签名种子、数据库、服务日志和原始 CLI 读回保存在已忽略的 `d08-private/`，不放入公开清单。F07 保持 partial，生产身份/数据库门禁需另验。
