# F05 独立任务执行性能测量

2026-09-17，基点 `9b8c09af` 上的未提交集成候选 `c8048691276590a064b5314837e35c80a2e8fba48471f1bf12c434f6b9ae9853` 在真实 OpenShell 0.0.83 网关与本批专属沙箱上完成一次串行测量。驱动退出码 0；冻结协议先于测量写入，SHA256 为 `32246f7b3a3d8e137aade9a1fbe45c408fb9f42aaedd1db33136c4b40b537288`。三轮均完成，150 个有效样本、0 个无效样本、0 个预算超标；323 个驱动检查为 pass。没有按结果剔除样本或重跑取优。

| 场景 | 样本数 | p95 | 最大值 | 测前预算（p95 / max） | 判定 |
| --- | ---: | ---: | ---: | ---: | --- |
| S1 真实 doctor 读回 | 45 | 52 ms | 54 ms | 15,000 / 30,000 ms | 绝对预算通过 |
| S2 同内容策略加载 | 15 | 91 ms | 91 ms | 60,000 / 60,000 ms | 绝对预算通过 |
| S3 经批准的真实只读任务执行 | 45 | 656 ms | 708 ms | 2,000 / 15,000 ms | 绝对预算通过 |
| S4 已持久化任务状态读回 | 45 | 26 ms | 30 ms | 1,500 / 15,000 ms | 绝对预算通过 |

数字来自 [`report.json`](report.json) 的 nearest-rank 统计，全部逐样本数据见 [`samples.json`](samples.json)。预热每轮各场景 2 次，按冻结协议排除；S2 每轮 5 个正式样本，其他场景各 15 个。S2 与 S3 之间的不计时沙箱就绪检查单列在报告 `conditioning`；它是协议事前冻结的环境条件，不包含在 S3 的端到端 HTTP 耗时内。故 S3 是已完成预览、批准和绑定之后执行请求的用户可见往返，**不是**从发现到最终结果的完整用户旅程延迟。

相对基线保持未验收：S1/S2 未用同协议独占重测 `9b8c09af`；该基点没有 S3/S4 路由，故 S3/S4 不可比。旧 GLM d06 中间候选也不冒充 main 基点。本报告只证明这个本地 dirty 候选在上述真实网关条件下满足冻结的**绝对预算**，不证明签名发行候选、Windows/macOS、OpenClaw 原生宿主或企业多租户服务的性能。

测量前排他扫描未发现竞争构建/测试进程；46 个既有后台服务已记录在原始报告，并非本批可清理资源。专属沙箱 `siq-v6-f05-171950` 在核对 UUID `8ed255ef-cd9b-4db2-867b-824169c78c43` 后按确切名称删除，随后只读列表确认不再存在；共享网关未重启或更改。驱动的隔离状态根已在 finally 中清理。公开文件检查无用户 home 绝对路径、Bearer 凭据或独立 32 位十六进制令牌；原始私有文件仅存于被忽略的 `*-private/` 目录。候选是未提交工作树，源码绑定不等于可复现的发行源码；测量后报告、文档和资源账本的编辑没有改变所测二进制。

本目录的 `SHA256SUMS` 覆盖公开证据文件；私有日志不在公开清单。协议与原始 JSON 保留不改，后续若需基线对照或完整旅程测量，应另建冻结协议和证据目录。
