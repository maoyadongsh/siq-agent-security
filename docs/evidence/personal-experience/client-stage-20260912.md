# M45：原生签名制品暂存

2026-09-12，当前未提交工作区，规格 §3.11.9。复用 skill-manifest 发行根、签名协议和平台制品字段，不更改旧发布快照或信任根。

`client-stage --manifest FILE --binary FILE` 验签后选当前系统唯一 pin，以签名大小限流复制并校验 SHA-256，再排他发布到状态目录独立版本目录。没有下载或执行候选，也没有停止/切换现有服务。独立子目录 Writer 允许在 daemon 持主锁时准备；签名不可信时不创建状态。重复发布核验内容与文件权限，不覆盖漂移对象，保留清单来源材料。

## 验证

- Go 全量、vet、clientrelease/CLI race 通过；gofmt 无输出；四目标交叉编译通过。
- 正向使用包内固定开发密钥签发测试候选，覆盖暂存、重复、缺失 binary 恢复、主 Writer 活跃时准备与 Unix 0700；候选仅是字节夹具，永不执行。
- 负向覆盖不可信发行根、重复平台、平台不匹配、大小/摘要错误、源 symlink 和已有制品漂移；漂移内容保留。生产 CLI 没有自定义公钥或跳过验签参数。
- Linux 最终二进制负向：unsigned manifest 拒绝，状态目录不存在。没有声称实际发行签名的新制品已完成正向原生升级；发布凭据/正式清单尚未用于本批。

| 制品 | SHA-256 |
| --- | --- |
| siq-darwin-arm64 | `9be5124daaf892a17e4de4227b8cccd5f0d40d72fc0657bc18b3b6d24fef6ae0` |
| siq-linux-amd64 | `c50a9af6c75f33c09f9f634164df9ae0d770e8c5a5ca2e69257dd66271e453ec` |
| siq-linux-arm64 | `53853447d48b620772b4081dab9168aae6a338498f49865d155b2663aa04e84f` |
| siq-windows-amd64 | `fc4f8e1f9428d24dd7c8302f2beb44f02ab0201cc4a95a42e73a0ad0f61aa2e5` |

## 后续

当前只有制品准备，尚未实现状态格式兼容判定、版本切换、自动回退、OS 代码签名、公证与安装包发布。新候选采用独立摘要目录，不复用旧签名或冒称已发布。后续将定义并接入升级停止/恢复事务；其他个人任务及 LAN 仍保持完整目标 active。
