# D05 收口批次独立复核（2026-09-17）

本次是对[已落盘真实腿](../v6-f04-e04e06e11-closure-20260917/report.md)的源码和证据复核，没有重跑网关或沙箱。原目录四文件 SHA256SUMS 全部通过，私有候选副本 SHA256 为 `0b16e5e09e6d807b5be732e390c373e6a4354d096b272eba827bddb84a6c8177`。Go/嵌入 UI 源绑定复算为 `ba8d266ef5ba861282f23a8576be8744d6023118f3f42efd8e1054632ffe06fc`、931 文件，与候选一致；本次只改验证脚本与文档。

## 采纳与修正

原驱动记录保持不变：344 步，337 pass / 6 partial / 1 blocked / 0 fail；原汇总是 11 pass / 2 partial。按任务书原判据复核，**当前采纳汇总为 10 pass / 3 partial（E06、E08、E11）**。[review.json](review.json)记录原矩阵摘要、逐行复核状态及边界，属于复核覆盖层，不冒充新一轮机器实测。

- E04：采纳真实 60 秒 SEC 到期后的决策拒绝，以及 required Authority 缺失时 deny/无 hold。它们证明决策平面；不扩称已运行 task executor 的 SEC 绑定腿。
- E06：加载中拒绝、加载窗口撤权、超时后策略写入及旧 CLI 拒绝各自有效。但 E06u3 实测 v0.0.13 **支持** `--wait`，不能替代“CLI 不支持等待选项时安全拒绝”的原验收项。该缺口保持 not_run，E06 恢复 partial。
- E11k：采纳三个真实文件权限故障场景；运行后结果写盘失败与审计单独失败均返回 503，保留任务实际发生事实；提交前 plan 写入失败不启动任务。
- E11i：原 E11h HTTP 字段已记录顶层 `execution_uncertain=true`，但嵌套 outcome 为 failed / rc=124 / remote_or_cli。不能把它说成“远端真实 failed”或“缺少预留不确定性”。非零 CLI 退出无法区分远端命令退出与 CLI 故障，也不能单凭 124 宣称已经确认远端超时或停止。保留 partial，不更改产品状态来凑通过。
- E08：当前 SIQ 没有可核实的远端单任务停止确认路径，继续 partial；不能仅由当前响应常量推出网关永久不支持。

## 驱动修复

1. E11 汇总要求四个核心检查各有且仅有一条结果；缺失、重复和非 pass 均不允许整行通过。避免 `--only e11_storage` 触发 `all([])` 空集合误报。
2. E06 显式记录不支持等待选项的真实分支未执行；已有旧 CLI 拒绝不能替代它。
3. 等待超时腿只将已观测的 CLI 124 记为该超时条件；普通非零退出或外层进程超时不能计作通过。
4. E11i 同时核对 HTTP 非成功、预留未决、任务执行未知及本地超时边界；普通 CLI 非零返回保持 partial，矛盾的成功/确定状态判 fail。
5. E11k.A 即使在观察启动标记时异常，也先恢复目录权限并等待批属请求线程退出；未退出则停止后续故障注入，避免与审计故障腿重叠。

新增正负向守卫后 **18 项通过**，显式仓库配置 Ruff 通过，见 [checks.json](checks.json)。未改 Go、embed 或候选身份；无需把原二进制已完成腿重建为另一个候选。驱动改动未经新一轮完整真实环境执行，不计新增实机通过项。

## 边界与交付

原 resources.json 自述若干迭代目录已清理；本复核未恢复被清理的失败中间文件，不对失败迭代完整留存作额外保证。公开原证据保持原字节。私有目录权限与候选副本可核对，不因未出现 `<redacted>` 字样就推导“没有敏感内容”；密钥、token 与日志不复制到本复核报告。本次未创建沙箱、启动服务或修改共享网关，未 commit/push/merge/签名/发布。
