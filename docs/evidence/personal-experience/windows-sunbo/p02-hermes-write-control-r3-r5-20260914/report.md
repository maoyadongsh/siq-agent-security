# Hermes Windows 纯文本写入正对照通过

r5 在真实 Windows 上通过了本批 **A 正对照**：公开 Hermes CLI 调用了真实 `write_file`，普通单链接文件的完整 34 字节与固定输入一致，三个写入阶段及进程收尾均符合冻结判据。测试使用固定合成模型和新建私有配置，明确关闭可选 LSP。**SIQ 未调用，B 未运行；完整 P02 与三宿主验收仍未完成。**

| 本机尝试 | 工具成功 | 目标完整内容 | 获准写阶段 | 预期 / 非预期拒绝 | A / B |
| --- | --- | --- | --- | --- | --- |
| r3 | 否 | 目标不存在 | 0 | 10 / 3 | fail / not_run |
| r4 | 是 | 34 字节，完全一致 | 3 | 11 / 4 | fail / not_run |
| r5 | 是 | 34 字节，完全一致 | 3 | 13 / 0 | pass / not_run |

三批使用相同公开 CLI 入口、固定合成 loopback provider 和各自新建的状态目录。来源上下文为干净的 `2a24901ec2f5d3f6c66c2175814e43dedb75a92c`，其相对 main 上下文 `b303c6f92392f3a44c306d81ad7323c6291ef4f2` 只增加 PR #54 的七份旧证据文件；两种身份分别记录。原 r1/r2 与 r3/r4 的失败不回写，不用新结果覆盖历史。

r3 的三个环境失配具有相同记录：缺失 0、变化 0、多出 3。名称指纹对应 `SSL_CERT_FILE`、`_HERMES_GATEWAY`、`HERMES_TURN_LEASE_TIMEOUT`。固定源码存在相应初始化赋值，但普通 chat 的完整实际导入链尚未证明。r4 在新夹具中补齐源码默认值，并为子进程显式指定已校验的安装证书文件；完整环境精确匹配规则未改，环境失配降为 0，真实写入已成立。

r4 仍有四次非预期辅助拒绝：两次只读 ASLR 探测超过原分类上限，另两次 loopback 连接来自 LSP 后台事件循环的 socketpair 初始化。六次 ASLR 调用的源码路径对应 snapshot、首次 PATH 构造、true fallback 和三个写阶段。r5 仍拒绝这六次相同命令，仅将预期拒绝计数上限从 4 调整到 6，第七次仍是非预期；没有允许 PowerShell 执行。r5 另在新私有配置中设置布尔 `lsp.enabled=false`，通过受支持的配置入口阻止可选 LSP 线程启动，网络允许规则完全不变。r5 没有环境失配或 socket 拒绝，13 次辅助命令拒绝均在事先冻结的分类范围内。

这是一项关闭可选 LSP 的纯文本工具基线，不证明默认配置下的 LSP 或完整用户旅程。TLS 校验、固定证书文件检查、完整环境匹配、命令顺序和真实副作用条件保留。未改安装源码、日常配置或凭据，也未安装 SIQ 插件。

三批均有 8 次 provider 请求、2 次有效 chat、0 协议错误。r4/r5 的唯一真实工具成功回执均报告 34 字节且 verified=true；实际文件 SHA256 为 `5a6d41ac0f6ba13b7cfb7d504e2f3de8f262eab57010611ba6b23ce881945ae9`。冻结早期夹具说明中的“33 字节”是文案笔误：代码常量、运行前期望、工具回执与实际文件均为 34 字节，旧说明原件保留并在此勘误。

三批实际 Python 与 launcher 均自然退出 0。Job 关闭前 active=0、terminated=0，total 分别为 3、31、31；总数不等于逐个进程身份核验。持有句柄、Job、provider 监听器、线程与连接均完成收尾，无超时、强制终止或输出收取超时。wrapper 的退出码分别为 1、1、0；CLI 零退出没有覆盖 r3/r4 的整组失败。

归档时复核了 r3/r4/r5 的 19/22/21 个冻结文件及 99/101/103 项安装输入，前后来源与配置记录一致。r4 的新增纯负向用例为 16 个预测器、8 个匹配器、7 个 pin、5 个证书映射；r5 又直接提取实际 reject 函数验证六次仍拒绝、第七次仍非预期，以及 socket 仍拒绝、两处配置严格同步。原 73 个匹配负向用例通过。纯测试与本机实际调用证据分别记录。

Python audit 不覆盖原生 Bash/coreutils 子进程的文件与网络行为；Job 仅管理生命周期。本批不宣称 OS 沙箱、完整依赖闭包或第二台 Windows 独立复测，也不增加完整平台验收 pass 或改变原研究分母。详见 [机器可读摘要](summary.json)、[来源摘要索引](raw-source-index.json)及 [归档校验](verification.json)。
