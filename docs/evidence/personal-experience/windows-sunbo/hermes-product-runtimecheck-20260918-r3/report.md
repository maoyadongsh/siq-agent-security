# Hermes Windows 产品原生自检 r3

本轮 controller、strict v2 离线核验与独立只读复核均通过。独立复核检查既有证据和源码，没有再次运行宿主或密码学核验；签名统计来自 strict v2 已完成结果。此包不修改验收台账。

固定干净候选为 `494464ea9f3cff90be4dc0ceac0dd5980c464617`，Windows 二进制 SHA256 为 `ad7680ba64672c9abbaab341ecebea4946e317e2fc3c6160cb5a0ade7e6d9d16`。产品修复来源为 `b8779b8`（状态快照）与 `494464e`（适配器管理请求期限及取消）；本次仅观察 helper 更新，产品与原有 120 秒自检预算未变。

这是真实已安装 Hermes 的 Windows 原生调用，使用产品提供的合成模型。正式 HTTP 接入、preview/start/Get 自检、卸载与配置恢复均完成。产品自检耗时 63.877 秒，driver 总耗时 126.83 秒，退出码 0。五项检查通过：允许读取、越权写在执行前拒绝、拒绝后合法调用仍允许、原生会话绑定、回执链验证。strict v2 记录核验 16 份签名文档、5 条回执、60 次探针采样、18 条辅助模型路由、1380 个源码文件及 23 个固定文件，无所定义的 evidence gap。它是既有证据的离线核验，不是第二次原生运行。

观察器记录 108 个进程句柄，最终均 signaled，错误为零；另有 2 条 `signaled-before-image-observation`，不计为 image 验证证据。独立复核逐条确认这两条记录的同句柄终态信号证据，并核对 1067 次观察采样未超限。4 个自有 Job 均关闭，服务 Job 统计总进程数为 136；108 个句柄与 2 条记录不能被描述为捕获了所有 136 个进程。正式 HTTP 卸载成功，原配置语义还原，本隔离实例的3个SIQ受管插件文件及自检材料移除。独立复核另核对安装/卸载 preview 与确认 POST 的计划、摘要、操作者、实例和 native 绑定。离散采样不证明文件持续不存在，已删除目录身份也不能事后重新观察。

同一产品候选的 r1、r2 仍为失败。r1 未定位的 WinError 5 留下观察缺口，新的 v2 诊断结果仅为 incomplete；r2 复查记录同时出现未 signaled 的 Wait 与退出码 0。helper 读序存在已确认的跨 API 快照风险，但不能据此证明原 WinError 5 无害；原失败保留。r3 只在同句柄 GetExitCode 后再 Wait(0)，最终仍未 signaled 就保持失败规则；没有忽略 WinError 5。16 个纯 API 用例及 2 个旧行为拒绝断言、3 个自有 Python 短命进程（退出码 0/7/259）的组件检查，仅支撑 helper 修复，不单独算宿主验收。

494 当前 CI 实测为 38 成功、2 跳过、2 失败；测试的是 merge `f6a647a45f68817e882e6e950e572562d283fcdf`。两个失败 Job 均为现有正式 Skill manifest 与当前内容绑定不符，仍是真实失败；没有重签、替换公钥或绕过验签，不能把本批 native pass 写成 CI 全绿。

最多建议 P02-HM-A03-01、P02-HM-A03-02 通过；P02-HM-A03-04、P02-HM-A03-06 仅补证，P02-HM-A03-05 未测。P02-HM-A03-01 覆盖正式管理 API，不包含浏览器交互；P02-HM-A03-04 缺少非空无关 entry/allow/deny 与安装后的用户修改，P02-HM-A03-06 缺少其他实例保留与冲突变体。303 分母和其他旅程状态不变。此结果不证明桌面交互、商业模型、任意用户配置、完整 Python 依赖或操作系统/网络隔离。

公开文件仅含白名单事实与原件摘要；原始签名对象、私有路径、进程和会话标识、公钥摘要及秘密材料不公开。`source-manifest.json` 的逻辑标签对应保留原件，原件未复制；脱敏汇总不冒充可直接验签的原对象。`SHA256SUMS` 校验本包另外三个文件。
