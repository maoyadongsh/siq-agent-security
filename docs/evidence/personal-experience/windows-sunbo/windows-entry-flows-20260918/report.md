# Windows 入口功能实现检查

本批交付三个产品改动：明确 Windows profile 的合法资源输入规范化；已显式开启的后台计数通知与固定本实例待办导航；首次 client-install 在受信发行验证后、暂存前创建身份，并只读复用运行实例的已有身份。

实现提交为 `4449c94eb4707c8b32984635ac84209aad3b4ec8`，资源输入前置提交为 `6d3cd12`。检查在提交前的开发工作树运行，`source_dirty=true`；检查后的提交源码摘要另行记录，不能将这些结果改写为干净最终候选或原生宿主验收。

资源输入定向检查通过。最终通知包 26 个顶层测试通过；安装与接线 16 个顶层测试通过。两个 macOS 专用测试按宿主条件跳过。相关三包 vet、模块 gofmt（无输出）及 windows/amd64、linux/amd64、linux/arm64、darwin/arm64 构建均通过。Go JSON 日志的父测试与子测试不重复统计为独立顶层用例。

独立源码审阅发现并修复：Explorer 首次恢复失败后需继续退避恢复图标；已有有效身份的运行实例重复安装不应重新获取主 Writer。对应正负向包含退避不重播、退出停止、原身份及锁不变、缺钥锁冲突、历史缺钥、损坏密钥、ACL 与未来 writer 拒绝。

保留 r1 的路径夹具失败及 r2/r3 的检查脚本启动失败；最终 r4 使用新建的短私密目录，原测试断言、产品路径上限与 ACL 要求均未放宽。输出仅进行路径脱敏和 JSON 重序列化，原始/脱敏摘要见 verification.json。

本批不启动真实桌面通知、注册系统任务、运行宿主或模型，也不执行重启/注销/睡眠。没有更改 303 项验收台账通过数。真实桌面投递/点击、正式签名制品安装和三个宿主的最终闭环仍待功能集成后验收。
