# OpenClaw 原生会话轮换验证

受测源码：`06745097c0d42523d10c5264e8290c6dc2d5835d`。Linux amd64 制品 SHA256：`6df47cb59a90229f950a49329090e8e1b7ede66fb69bcc6de51f2889a3376f75`。Windows 编排、WSL2/Linux 内真实 OpenClaw CLI、POSIX 资源解释；不代表 Windows 原生授权、桌面安装或三宿主全部验收。

## 原始候选的三个旅程

- 允许读取授权文件，拒绝未授权写入；目标不存在、对照文件未变，服务端签名回执关联真实工具调用。
- 同一路由 session key 真实空闲超过 61 秒后，宿主 UUID 发生轮换；没有新绑定时拒绝调用，不复用旧 Intent 或授权。
- 受管实例发生相同轮换后，新会话登记产生不同的 Intent、Binding 和任务范围；合法读取允许，超出 Grant 的写入拒绝。

## 缺 UUID 的三模式故障注入

仅测试插件副本在前置调用中移除原生 UUID，原候选保持不变。`block`、`warn`、`audit_only` 均在调用前拒绝；每项零 `/v1/decide` 和 `/v1/runtime-sessions` 请求、零服务端回执，目标不存在、对照文件未变。宿主后置事件仍带 UUID，会尝试 `/v1/observe`；这不是工具效果已经发生的证据。

首次 missing-block 夹具错误要求决策端口完全零请求，实际后置观察请求使该断言失败。原失败保留为 `epoch067-r1`，只修正断言为前置裁决/会话登记零请求，并在独立 `epoch067-r2` 重跑三个注入场景；没有改候选代码，也没有把原失败覆盖为通过。

## 证据和复核

`summary.json` 逐项记录制品、插件、原始记录和计划摘要，真实空闲、签名回执、副作用、合成 provider 调用数以及清理结果。六项通过使用 24 次本地合成 provider 请求；含保留的失败共 28 次，云端模型请求为零。自有运行进程清理均确认，未重启、注销或使 Windows 睡眠。

本地私有记录索引保留于 `.tmp/windows-openclaw-epoch-native-20260918/epoch067-final-private-evidence-index.json`，不提交个人绝对路径。独立只读复核使用同目录 `collect-results.py --verify-only`，重新核对冻结包摘要、Plan/Run、六项结果与效果、清理及原候选/故障插件身份，退出 0；该复核没有启动宿主、WSL 或模型，也不是第二次独立原生重跑。

上述结果属于固定历史候选。后续候选须评估变更影响并完成必需回归，不能把本报告的 SHA 替换为新 SHA。验收台账不得据此把未覆盖的安装、审批、Skill 归属等旅程记为通过。

公开 JSON 副本仅将 CRLF 转为 LF，解析后的所有字段不变；原本地摘要 SHA256 为 `c0859750c580cf32b972b01d2b3068ac9500cdf300d1818790be80e3cb419769`，公开副本 SHA256 为 `3d34aa53dee031fcd7337c64a30595a90dae6e767ab83161d7867175c71eaf3d`。原始记录及其摘要未改写。
