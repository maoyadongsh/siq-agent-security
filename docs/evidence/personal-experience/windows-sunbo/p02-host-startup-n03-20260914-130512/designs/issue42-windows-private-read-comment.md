已基于原生 WIN-ACL-001 证据补充只读源码设计；尚未实现或复验修复。

现有证据已证明宽 DACL 下状态初始化成功，以及公开固定测试 seed 被实际加载；没有证明日常密钥泄漏，也没有实测另一登录身份读取或宽 DACL 下随机秘密首次生成。

新增静态发现：`state.Token()` 当前忽略所有读错误，随后 `O_EXCL` 创建遇到 `EEXIST` 会递归调用 `Token()`。若只接入 ACL 拒读而不改错误分支，既存宽权限 token 可能无限重试。应仅在明确 `NotExist` 时进入创建，其他错误直接返回；`signing.Load` 也需要相同的非 `NotExist` 错误传播。该递归风险本轮未执行，不能当作已复现崩溃。

建议维护者确认一个共享状态主修 owner，先统一 Windows 私密主体/ACE 合同、错误语义与显式私密读取接口，再同步 seed/token 共享调用点。避免把通用 `statefs.Open/ReadFile` 全部改成私密文件检查。Windows 侧可承担约定接口下的 Win32 handle/DACL 后端和原生负向测试。

第一批拟仅拒绝不符合合同的既有私密文件读取；安全创建、备份、适配器直接读取/缓存及真实第二登录身份验证仍需后续覆盖，不能因此关闭整个 #42。本草稿不请求全局权限，不安排修改日常状态或 ACL；待共享归属和接口确认后按隔离 fixture 验证。
