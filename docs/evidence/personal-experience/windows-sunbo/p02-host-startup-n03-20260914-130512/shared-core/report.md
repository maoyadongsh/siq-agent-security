# Windows 共享核心定向复验证据

本组固定受测源码为 `9cc18630be584692905e1ce612b91a9ba5aee610`，各次运行前后均 clean。PR #45 已于 `2026-09-14T03:19:44Z` 合并为 `4464dfbc8e66c8ec1fb2590b286351595fcd9667`，早于测试；这些结果来自固定 PR 源提交，不是对 merge commit 的重新测试。

| 范围 | 结果 | 结论边界 |
| --- | --- | --- |
| 安装诊断定向测试 | 2 个顶层 pass、1 个 fail，退出码 1 | Windows 不生成 Hermes shell wrapper；新增测试读取 wrapper 失败，后续权限与调用顺序断言未执行。两个诊断测试通过不代表宿主安装入口完成。 |
| N03 更新检查复验 | 3 个顶层 pass，其中 10 个子例 pass；0 fail、0 skip，退出码 0 | 覆盖迟到结果不覆盖用户配置、不兼容更新源拒写，以及发现新版本仍需确认。顶层与子例属于不同统计层级，不相加。 |
| N03 首轮冷缓存 | 180 秒外层超时、0 个测试事件、三项未执行 | 保留准备阶段超时；退出码 1 不计作测试语义失败。后续只复用自有编译缓存，使用新私有运行根和 `-count=1` 重新执行。 |

详细命令、时间、原始日志摘要、公开副本摘要及限制分别见 [安装诊断报告](stage-review/report.md) 和 [N03 报告](n03-review/report.md)。N03 首轮清理命令有一条未成功的后代终止记录及后续范围内进程数为零的快照，原报告保留这些限制，不宣称每个后代均逐个终止成功。

这是 Windows 11 amd64、Go 1.27.1 下的组件测试。没有启动真实宿主、执行 SIQ CLI 或调用真实模型；没有构建新的 SIQ CLI 发行二进制，也没有替旧二进制更换源码身份。实际执行的是 Go 临时编译的测试程序。本组不提升三宿主原生验收矩阵，不关闭 Issue #39/#42/#43，不补记完整安装更新、审批、登录重启或状态迁移通过。宿主启动诊断另组交付。

安装组已经过独立只读原始与派生证据比对；N03 由另一位协作者在同机独立审阅报告、汇总及运行脚本，重新解析 56 个事件并核对清单中 10 个文件的长度和 SHA256，结果一致。另有 Gitleaks 8.24.2 对 N03 目录扫描退出码 0。这些属于同机证据审阅，不是独立重跑、独立机器验收、外部签核或对全部底层 fixture 断言的再次审查。

两个子目录分别保留 4、11 个文件的原始字节；其中 N03 自带清单列出 10 个文件并排除它自身。新增 [verification.json](verification.json) 逐一记录 15 个子文件的长度、SHA256、复制一致性、链接和隐私核对，以及上述审阅范围。文件内容中不归档个人绝对路径、SID、凭据或日常宿主配置；本机原始日志仍在私有测试根。

本目录仅为交付准备，尚未写入 tracked docs、提交、推送或发送远端。
