# v6 续作复核修复

本批修复 R07 失败报告直接序列化 `str(exc)[:240]` 的敏感信息风险。截断并非脱敏，现仅输出错误类型、脚本检查 ID/状态及固定等待标记；回归使用包含凭据、原文和私人路径的异常证明其不进入报告，同时覆盖初始化前失败。

同步进度、阶段报告和任务书：归因关系补强已完成，不再作为当前待办；旧 phase-c 超时原因仍未知，新关联断言的 API View 字段误用属于另一事件。新增失败轮 attempt1 原始 JSON 曾误删，保留这项证据缺口。

验证：21 项离线守卫测试通过；原四目录 18 项清单哈希全部匹配；diff 检查通过。完整 Ruff 检查发现 R07 既有 21 处长行，排除 E501 后其余规则通过，不声明全量 lint 通过。本次未重跑宿主旅程，不将离线测试计作实机验收。

产品 Go/embed 未因本批修复改变。驱动摘要因修复变化，历史报告保留原摘要与结果，不重写历史证据。未来重建产品候选后须补对应候选的性能测试，旧证据仍保留其原适用范围。

尚未关闭：R06 已装服务联合旅程、原生 hold 消费、合法授权下超范围拒绝、任务专属导出与真实保留期清理、外部协议及实机条件。未提交、推送或发布。
