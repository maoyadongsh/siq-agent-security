# 文献原字节归位记录

本批把旧根目录的 35 PDF 放入 `research/literature/papers/`，1 历史 XLSX 放入 `research/literature/`，落实根级研究资产组织。基线 `ac585c896b525deef6618eb582b7957c1a556dcf`；[映射](manifest.json)固定原路径、现路径、源 Git blob、SHA-256 和大小，共 36 项。核对源 blob 与迁后字节，不能只更新期望摘要自证。

消费者为 README、研究总览、文献目录、仓库地图与架构方案；许可例外和第三方说明同步。旧 Linux 阶段任务书保留历史语境；冻结证据没有引用这批旧全文路径，不改写。旧目录保留迁移说明及固定基线入口，单文件旧 main URL 不会自动重定向。

正文/文件名/权属状态不改，未知来源、作者及再分发依据继续待核。无新增阅读或研究结果；软件、签名包、冻结证据、服务状态和旧工作树不迁移。回退本批实现提交恢复旧路径及导航，不撤销 Release 或修改用户状态。

验证范围：逐文件源 blob、SHA-256 与大小；路径/目录/冻结守卫，研究元数据和许可范围，仓库工具测试；干净含空格及 Unicode 路径的检出与回退恢复。具体运行结果在本目录另增固定候选记录，CI 与最终 main 身份由对应 PR 保存。
