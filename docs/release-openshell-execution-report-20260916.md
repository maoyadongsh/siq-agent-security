# 发行/OpenShell 批次执行报告（复核后）

本报告替代原“7/8 通过、B2/B3 全通过”结论。原稿保存在
[evidence 原报告](evidence/personal-experience/release-openshell-20260916-142706/original-execution-report.md)，
原始测量、失败样本与 SHA256SUMS 均保留；最终修复与验证见
[复核修复报告](release-openshell-review-fixes-20260916.md)。

## 1. 已完成

原批次产出 rc.1 四目标构建、真实策略读回、控制面 apply/CAS/回滚观察。
复核发现限制字段重写丢失风险，现已实现写前拒绝与负向回归；
验收工具收紧结构化校验，修复绝对预算漏判、跳过/no-op 冒充覆盖、
证据源绑定、失败现场保留；签名指引已纠正。

## 2. 证据级别

- doctor 证明指定策略可读，不证明会话/调用身份、撤销或执行证据链。
- 真实控制面回滚驱动只验证策略操作，不能代替候选二进制 B3。
- 旧 B2 数值保留观察用途，缺少逐样本状态证明，不作新候选验收。
- b01-lifecycle 是旧/新测试信任构建的生命周期观察，不是 rc.1/rc.2 正式发行验收。
- 新候选构建/测试证据与旧证据分开，未签名、未发布。

## 3. 未关闭项

O05 会话绑定、真实撤销/失效、required 失联执行拒绝和执行结果关联仍需完整链验证。
沙箱环回可达是负面事实；`client.go` 的 `Interceptor:false` 为静态兼容字段，
不能证明后端关闭拦截器，不能承诺“启用 interceptor 即解决”。
须分析网络路径/命名空间/策略适用范围，并完成真实允许与越权负向双腿。
B2 v2 正式复测、候选二进制 B3、正式种子签名及 Windows/macOS 实机验收仍待。

## 4. 验证

修复批实际命令、退出码与新构建身份见复核修复报告。
原批次声称的测试成功不替代本次独立复核。

## 5. 证据与版本

基线 `052c81617438d1eabf94a5e0166338312366c491`，同一 GLM 工作树。
rc.1 与旧证据冻结保留；rc.2 用于本次安全修复。
本轮未 commit/push/merge/publish。

## 6. 环境

修复批不修改共享网关或既有沙箱，不读取/生成正式签名种子。
原批创建沙箱已删除，原始私有日志继续保留；不复用消失目标伪造新验收。
