# ADR-038：个人 Skill 安装预览入口

状态：管理 API 与个人界面已实现并通过 Linux 隔离验证；承接 ADR-037，UX-009 仍在进行。

## 管理接口

管理会话或管理凭据可调用 `POST /v1/skill-installations/plans`，严格接收 ADR-037 的 stage-create/v1 七个必需字段；未知字段、重复键、null 和客户端绝对目标路径均拒绝。实际目标通过现有 Hermes 实例解析器查找，必须 detected，使用对应 profile 的 skills 子目录。只生成私有暂存和签名计划，不调用 Grant approve/deploy，不写平台目录。

响应为 `local-skill-install-plan-created/v1`：plan（ADR-037 合同）、reused。首次 201，原请求成功复验 200。管理 `GET /v1/skill-installations/plans/{plan_id}` 返回通过完整 Load 复验的 plan/v1，不提供未经复验的“可安装”状态。与导入共享 daemon 单并发工作锁，竞争返回 429/Retry-After；处理上下文 60 秒、响应写期限 65 秒、浏览器预算 70 秒。响应 no-store；未授权 401、决策凭据 403。

固定错误：invalid 400、changed/conflict/expired 409、not_found 404、limit 413、busy 429、interrupted 408、unavailable 503，均为 skill_install_ 前缀。不得将原路径、文件内容或凭据加入错误。

## 个人界面

沿用签发页，绑定导入来源的 Hermes 实例授权在 approved 时显示安装预览入口。来源卡片读取已签名 admission 的 Skill 名称供目录名预填；用户可改名，目录规则由前后端共同校验。目标实例由授权绑定，不能在预览中更换主体。

用户点击“生成安装预览”才写私有暂存。展示目标位置、文件数/大小、候选与权限摘要、计划期限，并明确尚未安装。操作中禁止重复提交；失败保留原请求供显式重试，重新准备才产生新请求 ID。成功后位置栏只保存 Grant/plan 的不透明 ID，刷新通过 GET 重新验证。目录、操作者或客户端摘要不作为读取权限依据。

切换授权版本、离开组件或退出时取消请求并忽略迟到响应；读取/重试失败撤下旧完整预览。到期显示失效，用户可重新准备；前端时间只影响提示，服务期限仍为权威。刷新或恢复不会自动创建暂存。此阶段不展示可用的“安装完成/已保护”状态，不以预览替代后续明确安装确认。

## 验证与后续

HTTP 正负向覆盖真实导入→人工批准→暂存→复验、未批准/错误版本/错误主体、管理分权、严格输入、竞争、撤销与内容篡改，确认目标目录和运行权限未变化。浏览器以隔离真实 daemon 验证预填、重复点击/响应丢失原请求重试、刷新复验、失败撤下预览、移动端及未写入目标。后续继续明确安装确认、目标归属、写入、恢复与读回，以及真实平台识别/运行证据；没有缩减完整任务书范围。

## 本批验证记录

[验证清单](../evidence/personal-experience/skill-install-preview-20260911/verification.json)绑定本批源码、前端制品、四目标二进制和日志。新增预览浏览器流程 7 项、既有权限准备/批准浏览器回归 8 项均使用同一 Linux 候选。前者准备授权使用隔离 API 人工操作者夹具，后者继续走界面人工批准；两者均未运行宿主或安装 Skill。移动端等待侧栏收起完成后检查主内容无横向溢出，分别保存预览内容和操作区截图。

前端校验不把合法授权限定为最初的 grt-si 前缀，重新起草的 grt-d 授权按实际编号、版本、签名与来源匹配；目录名结尾换行仍拒绝。后端始终独立验证授权与源副本，前端显示不构成运行权限。
