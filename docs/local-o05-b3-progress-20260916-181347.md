# local-o05-b3 执行进度（批次 20260916-181347）

> **2026-09-16 策略加载修复后更新**：见 [修复与复测报告](openshell-policy-load-wait-repair-20260916.md)。L01 旧 `arrivals=1` 失败已定位到未等待沙箱加载；应用及回滚增加有界加载确认，3 轮真实复测均为 HTTP 403 + 零到达，rc.6 策略 HTTP 旅程 57/57。Python 同类路径同步修复并完成组件回归。旧失败证据保留；L02 完整任务/UI、新候选性能及外部实机仍未完成。此更新优先于下方历史状态。


> **2026-09-16 收尾复核更新**：以 [最新复核报告](local-o05-closure-review-20260916.md) 为准。rc.5 身份保护原生验证 4/4、策略 HTTP 旅程 57/57；L01 严格实测发现违规请求到达 1 次，保持未通过。L02 非完整任务执行；L04 的旧 0.9967 只对应 doctor_readback，B3 未测，新候选性能未测；L05 部分完成。旧绿色勾选仅保留为历史声明。


> **2026-09-16 复核修正（优先于下方历史勾选）**：L01 原 PASS 的判定允许未知失败被当作阻断，且成功路径未实际恢复策略，不能用作修复后验收。已修复分类、双目标正控、回执恢复与漂移拒绝，真实独占目标复测仍待。L02 已修复批准参数与单个签名 Grant 的绑定、写前撤权复核、并发重放及证据失败状态；它仍只是 `policy_apply`，不执行沙箱任务，不能记为 O05/B3 完成。原日志保留；复核证据：[`local-o05-review`](evidence/personal-experience/local-o05-review-20260916-204031/)。正式 rc.2 冻结包未覆盖本轮源码，不能作为本轮验收候选。


- 批次：L00–L07（提示词 `docs/glm-local-o05-b3-next-execution-prompt-20260916-175248.md`）
- 基线：review 修复批之后的工作树（source-files.json 1137/1137 digest 零漂移，rc.2 四工件 SHA256 全部匹配）
- 快照：`/tmp/siq-l00-input-20260916-181347/`（0700，dirty+untracked 输入私有快照）
- 本轮约束：不 commit/push/merge/sign/release；仅落盘
- 状态图例：✅ 完成 / 🔶 进行中 / ⛔ 外部阻塞 / ⬜ 未开始

## L00 基线保护与差距映射 — 🔶

- [x] review 基线核对：零漂移（见会话记录；source-files.json 与 rc.2 SHA256SUMS 均验证通过）
- [x] 私有快照落盘
- [x] 生产调用链七环节映射 — ✅ 映射代理报告已返回（file:line 引用齐备）
- [x] 进度文档（本文档）随批更新
- [x] 调用链七环节 implemented/test-double/not-wired/missing 定性表（映射代理核实，路径相对 `apps/agentshield/`）：

| # | 环节 | 定性 | 关键证据 | 缺口 |
|---|------|------|----------|------|
| 1 | 用户确认 | implemented（决策面，未连到 O05） | `internal/receipt/engine.go:1073,766`（hold 创建）；`confirmations.go:160-176` ResolveConfirmation 绑定 ReceiptID+Hash+ParamsDigest、拒重放/并发双批；路由 `server.go:241-242` | 批准只落在签名决策上，没有任何 OpenShell 操作绑定确认 ID |
| 2 | 管理员授权 | implemented（身份分离）+ not-wired（对 O05 操作） | `server/authz.go:32-105` 配对→管理员会话；`authz.go:19-23` capAdmin/capDecision/capEffectObserve 分离；`confirmations.go:130-156` per-op authority 复核 | `/v1/openshell/apply` 仅要求有效管理员 bearer，无逐操作关联（`openshell_http.go:101-154`） |
| 3 | 执行身份 | implemented（agent/决策面）+ not-wired（OpenShell 侧） | `runtimeidentity/store.go:197,246` ri-* 凭据绑定 grant digest；`runtime_identity_auth.go:63-114` 作用域凭据强制 | OpenShell 侧"身份"仅是 CLI 输出指纹（`identity.go:28-62`），无 ri-identity↔沙箱 target 绑定 |
| 4 | 当前授权 | implemented（决策面）+ **missing**（openshell apply 路径） | `receipt/hold_execution.go:125-180` ReserveHoldExecution 消费前复核 authority+deadline | `openshell_http.go:101-154` 除 bearer 外零复核：无 grant 状态/过期/撤销/确认 |
| 5 | 后端执行 | test-double/partial：真实 CLI 执行存在但无 确认→沙箱执行 入口 | 真实：`openshell/policy.go:402-489` ApplyNetwork（CAS）、`policy.go:598` ApplyAndVerify；HTTP `server.go:272`、CLI `cmd/agentshield/openshell.go:50-98` —— 均直连、与 hold/grant 无关联 | `sandbox exec` 仅存在于 opt-in live 测试 helper；无 handler 消费 hold 预留并在沙箱执行 |
| 6 | 回执 | implemented（决策面）+ not-wired（O05） | `receipt/chain.go:65-131` 哈希链+Ed25519+fsync；预留/对账回执均走 Chain.Append | OpenShell DeploymentReceipt 仅经 `PutEvidence` 散存（`openshell_http.go:148`，写错误被 `_ =` 丢弃），不入哈希链 |
| 7 | 结果追踪 | implemented（工具效果面）+ partial（OpenShell 手动无 UI 闭环） | `effectevidence/correlation.go:31-70`；`hold_reconciliation.go:59` uncertain→confirmed_occurred/not_occurred；drift-check `ledger_http.go:310-369` 出签名 FindingRecord | drift-check 是手动 POST 非执行后自动验证；`grantNetwork`（`ledger_http.go:373`）无 apply 调用方消费；sandbox exec 结果不出测试；无 UI 关联 operation_id↔确认/grant |

**(a) 端到端路径现状：不存在** —— 两个完整但互不连接的面：决策面（确认→hold 预留→重执行→回执，全部入签名链）批准的是宿主上的 agent 工具调用；OpenShell 面（`/v1/openshell/apply`/CLI）是真实后端但不受任何确认/hold/身份/grant 约束的独立管理工具。
**(b) 既有入口**：确认 `POST /v1/confirmations/{action_id}/resolve`；预留 `POST /v1/hold-executions/reserve|status|reconcile`；OpenShell 执行 `POST /v1/openshell/apply`、CLI `openshell apply`；只读 `probe|doctor|drift-check`。
**(c) L02 最小闭环必须新增**（映射代理建议，逐条对应 prompt L02 七步）：① 会话执行端点绑定 action_id+decision_receipt_id+预留回执，经既有 `ResolveConfirmation`→`ReserveHoldExecution`→`holdAuthorityCurrent` 校验后才调 `ApplyAndVerify`；② target→grant/subject 映射 + 用 `grantNetwork` 编译规则体（不采信请求原文）；③ DeploymentReceipt 入签名链（绑 operation_id↔预留回执）；④ 执行后自动 readback+drift finding + UI 结果视图；⑤ 记录触发执行者（ri- identity）进回执。

## L01 阻断问题定位 — ✅（受控实测完成，根因已定位）

只读预检已完成（私有证据 `docs/evidence/personal-experience/local-o05-b3-20260916-181347/l01-private/`）：

- [x] 网关只读确认：siq-openshell-dev v0.0.83 已连接（仅 canary 沙箱，本批未触碰）
- [x] CLI 命令面确认：`policy` 为顶层命令（set/update/get/list/delete/prove）；`sandbox create --name`；`sandbox get NAME --policy-only`
- [x] canary 策略读回结构确认：Version 2 / Hash / Status Effective / Source sandbox；endpoints 带 `protocol: rest, enforcement: enforce, rules: [...]`
- [x] 关键差异定位：`apps/agentshield/internal/openshell/policy.go` `networkRulesToGateway` 产出的 `siq_as_rule_N` 只含 `{name, endpoints:[{host,port}], binaries:[{path}]}`，**缺 `protocol` 与 `enforcement` 字段** —— 与 canary 显式 `enforcement: enforce` 形成对照，阻断失效的第一嫌疑
- [x] 历史负证据重审：`release-openshell-20260916-142706/openshell-live/o05/exec-behavior-abc.log` 中接收端自身返回 "HTTP Error 404: No permission to list directory"，证明未放行端口 8098 的请求**确实到达**了目标（沙箱内 loopback）——即策略未默认拒绝
- [x] 历史失败沙箱已清理（siq-l01-o05-181742 已 delete，删除记录在 create2.log）
- [x] 沙箱镜像阻塞定位：上游 `ghcr.io/nvidia/openshell/sandbox:0.0.83` registry 返回 manifest unknown（404）；本地缓存无此镜像；canary 实际使用本地镜像 `siq/hermes-openshell-siq-analysis:f470321d2fb2b2cda14b7c3b`
- [x] 专用临时沙箱 `siq-l01-o05-181930` 重建成功（Ready，rev1 默认策略无 network 段）
- [x] 受控放行流量 + 受控违规流量实测（先证明靶标可达，再谈阻断）— 8 项对照矩阵全部完成
- [x] loopback 与跨边界流量分开记账
- [x] 受控接收端带 correlation ID（沙箱内 recv.py ×2 + 宿主 hostrecv.py ×2）
- [x] enforcement 语义确认（实测而非文档）：**loopback 不经 egress 代理、完全不被 network policy 拦截**（加 `enforcement: enforce` 也不阻断）；**跨边界默认拒绝（403）**，agentshield 形状 allow 规则可放行，违规目标 403 且请求不达
- [x] 定性结论：**不是 agentshield 策略编译缺陷**——旧 O05 失败根因是测试靶标放在策略边界外（loopback）。完整报告与对照矩阵：`l01-root-cause.md`（证据目录内）
- [x] **修正后行为级 live 测试落盘并实跑通过**：`apps/agentshield/internal/openshell/o05_enforcement_live_test.go`（opt-in 默认跳过；loopback 零声明、correlation-ID 双接收端、基线默认拒绝前置证明、CAS 应用+readback 验证、放行 settle 窗口、违规 0 到达、策略恢复双路）。实测 PASS：baseline_deny=ok，rev13→14，放行到达、违规 0 到达（`l01-private/o05-enforcement-live-test.log`）
- [x] **新发现：执行平面传播滞后**：gateway readback 确认新规则后，沙箱 egress 执行平面滞后 ~2-4s 才生效（+1ms→403，+2.3s→200，带时间戳实测）。enforcement_verified 级测试必须在放行路径带 bounded settle 窗口——新测试已内置（30s 上限并记录传播耗时）
- [x] 单包 gofmt/vet/test/race 全部通过（race 7.5s ok）；live 测试在无 opt-in 时按 skip 路径通过

## L02 O05 会话执行最小闭环 — 🔶（HTTP 合同级完成：实现+10 项负向测试全绿；原生宿主真实执行未覆盖）

**能力标级（按 prompt 要求分级，不混称）**：实现与全部测试为 **HTTP 单测 / 模拟 runner 级**（httptest 服务器 + 注入 `openshell.Options.Runner` 的有状态假网关，含 CAS 版本/摘要回读语义）；**未做**：原生宿主真实执行、真实 OpenShell 网关联测（共享网关本批只读，不写 canary）、UI 入口（见⑦）。

**新增文件**（均未提交）：`internal/server/openshell_session_exec.go`（~580 行）、`internal/server/openshell_session_exec_test.go`（6 个测试全绿）；`internal/openshell/policy.go` 新增 `RollbackAuthorized`（fail-closed 回滚：未知/已消费操作拒绝、回滚前漂移检查、no_op 消费、无授权器拒绝）。

**新增路由**（`server.go:280-282`，全部经 `s.auth`，不绕过既有授权）：
- `POST /v1/openshell/session-executions`（capDecision，decision 路径）——受控执行
- `POST /v1/openshell/session-executions/preview`（capAdmin）——只读预览
- `POST /v1/openshell/session-executions/rollback`（capAdmin）——回滚

**七步闭环对照**：
- ① 预览：`/preview` 只读返回 target/grant_ids/authorized_endpoints（从 grant facts 编译，不采信请求原文）；`execution_constraints_verified:false` + note 声明未验证实际执行限制（仅配置读回时）
- ② 确认与绑定：绑定既有 hold 链——`action_id`+`decision_receipt_id`+`original_tool_call_id`+params 身份复核后 `ReserveHoldExecution`（`receipt/hold_execution.go`），授权解析全部服务端可信状态；绑定事实（reservation hash、target、endpoints、expected_revision）入 `openshell_session_execution_binding` 证据
- ③ 执行前复核：L3 门（未配置→503、probe 失败→503，required 不回落）；target↔grant 映射复核（跨 target 403、endpoint 不在 allow 403）；`expected_revision` CAS 复核（不符→409 `revision_conflict`，零写入，自动 reconcile not_occurred→cancelled）
- ④ 受控执行：仅调既有 `ApplyAndVerify`（写后 readback 摘要验证）；无 required 降级路径
- ⑤ 停止与恢复：不自动重放——set 失败→502 `execution_uncertain`（预留保持 uncertain，重试→409）；写后漂移→409 `execution_uncertain`（带 backend_revision）；不确定均记 `openshell_session_execution_uncertain` 证据并自动 reconcile 到 uncertain 语义
- ⑥ 结果追溯：执行成功→observation 回执入签名链（`receipt.Verify` 通过），链上含 decision/hold_reservation(reason=hold_execution_reserved)/observation；绑定/不确定证据入 store（0600）；`effective_readback`（backend/revision/verified_at/evidence_id）随响应返回
- ⑦ 界面：**未新增 UI 入口**；preview/rollback 走既有本地管理 HTTP 面（capAdmin），执行走 capDecision。UI 部分记为未完成项

**10 项必要负向测试（实际合同状态码）**：无凭据 401；bootAdmin 上 decision 路径 401（身份分离保持）；decision token 上 admin 路径 403；跨 target 403；endpoint 不在 grant 403；`expected_revision` 非 64hex 400；相对路径 binary 400；未配置/probe 失败 503（授权保持完好）；执行不确定 502/409 且预留不落 completed、重放 409；revision 冲突 409 + 自动对账 cancelled + 零写入（setCalls=0）。正向：happy path 200（backend_revision=2、observation 入链、绑定证据落盘）→ 错误 action 回滚 403 → 正确回滚 200（网关恢复 rev3 无 endpoint）→ 二次回滚 409 fail-closed。

**验证记录**：`gofmt -l` 零输出、`go vet ./...` 干净、`go test ./...` 全绿、`go test ./internal/server/ -race` ok（45.1s）。

**诚实边界（不得标 done 的部分）**：无真实网关执行证据；无沙箱内命令真实执行（本闭环只覆盖网络策略面 apply/rollback，沙箱 `sandbox exec` 不存在执行接口，未编造）；无 UI；错误分支 409/502 的分类依据是 `DeploymentReceipt.BackendRevision` 是否被填充（写失败于 ApplyNetwork 内部→空→502 未知结果；写成功但 verify 不过→非空→409），该分类有测试覆盖。
## L03 候选产品旅程（真二进制+真 UI/HTTP/CLI 入口）— ✅（57/57 通过，见 `l03-journey.json`）

rc.3 linux-arm64 冻结二进制（SHA256 `4866f307cf03…`，与 builds-rc3.json 一致）实跑 57 项旅程断言全过：init→serve→配对→授权→策略→回执→导出→更新→撤销全链经真实 HTTP 入口。私材料（配对码/凭据/serve 日志）仅存 `l03-private/`。

## L04 冻结协议性能复测（对比 rc.2，独占测量）— ✅（全预算通过，总体比率 0.9967）

独占测量窗口内完成（无并行负载）；协议、样本、逐项结果见 `b2b3-perf.protocol.json` / `b2b3-perf.samples.jsonl` / `b2b3-perf.json` / `b2b3-perf.claim`。全部预算阈值通过；B07 diagnose_unconfigured p95 沿用按失败记账的结论不变。
## L05 Linux 剩余验收 — ✅（B03 服务级补充完成；B04 原生采集腿保持外部阻塞；B05 记录为外部阻塞）

最新台账差距盘点（来源 `docs/personal-v5-execution-final-report-20260916.md` + `docs/release-openshell-next-execution-plan-20260916-142706.md` §2）：

- **B03**（partial）：双 CLI 合成未来/损坏状态拒写 44/44 已过；缺真实安装 daemon 的独立管理员配对、loopback HTTP 并发与签名 409 实机覆盖（另有 Go 回归但不得混称实机）
- **B04**（partial）：真实墙钟到期 19/19、双独立 HTTP export 复核各 192/192 已过；缺宿主原生采集腿（HTTP 写入合成记录不冒充）；opt-in/到期/撤销/export 归属互斥需分开验证
- **B05**（partial）：当前候选 r3 16/16 已过；通知视觉与其他 OS/宿主保持 external_manual；服务失联需副作用级验证（不以端点不通替代）
- **B07**（沿用记账）：diagnose_unconfigured p95 超阈值 15.79% 按失败记账未调阈值；如无性能相关源改动则记录沿用，不虚耗重跑——但本批 L02 若触及性能相关源码，L04 复测将覆盖
- 本批新增影响：L01 新增 `o05_enforcement_live_test.go`（非生产源，不影响 B07 沿用结论的判定边界）

**L05 执行结果（2026-09-16，全部真实 daemon 入口，非 httptest）**：

- **B03 服务级补充 — ✅ 完成**。新驱动 `scripts/personal-experience/closure-b03-service-concurrency.py`（独立 HOME+state 目录的真实 rc.3 daemon，实际 HTTP 入口 + 真实管理员配对，非 httptest），四条腿全过（`b03-service-concurrency.json` overall=pass）：
  - `up07-refusals`：状态兼容边界拒绝（未来版本/损坏 marker/不可识别非空态）三例全部 rc=1 拒绝且状态逐字节未动；另含两例**观察腿**（非失败项）：完整骨架无 marker → 按 `checkUnmarkedState` 设计接受为 legacy 且 marker 不回写；marker+keys 缺失但 config.json 可解码 → 同样接受，且 serve 静默重建了新 `keys/signing.seed`（原身份已删）。**行为事实记录**：rc.3 `init` 不写 signing.seed（首次 serve 才生成）；`ErrMissingMarker` 的真实 fail-closed 边界是"无 marker 且无可读 config.json 的非空状态"。
  - `up05-same-plan`：3 个独立管理员会话并发提交同一 update → 429 `skill_install_busy`（Retry-After）重试后恰好 1 个 200，其余 409 合同错误；durable 视图/移除视图/撤销 Grant 冻结/update-operations 工件集合逐项验证；陈旧 candidate revision 的合法形状 stage 探针 409 合同错误（实测 `skill_install_removal_pending`）；篡改 plan_signature 的 commit 400/409 拒绝。
  - `up05-update-removal`：update 与 removal 双飞行并发 → 合同内串行化，durable 单值且稳定，撤销恰好一次（revision 两次读一致）。
  - `up05-diff-plan`：两个不同候选并发 commit → 恰 ≤1 个 200，败者 409 合同错误（实测 `skill_install_removal_pending`），胜者 durable 视图签名一致。
  - 私材料（serve 日志含配对码）仅存 `b03-private/b03-service/`。
- **B04 原生采集腿 — ⛔ 保持外部阻塞**：需要真实宿主原生采集环境，本机不具备；HTTP 写入合成记录不冒充原生采集的区分验证在本批无靶标。opt-in/到期/撤销/export 归属互斥的既有 HTTP 级验证结论沿用。
- **B05 — ⛔ 外部阻塞（详见 `b05-r4-blocked.md`）**：openclaw 已被外部降级 2026.5.12→2026.3.11，零回执，smoke.py:263 `run_cli` 失败与 rc.2 完全一致；非本批回归。通知视觉无可见桌面 → 保持 external_manual。
- 生命周期/更新相关 R06/R04/R07 腿：本批改动（3 个新 session-execution 端点 + `RollbackAuthorized`）不触及安装生命周期状态机；`up05-*` 腿已按新候选实跑覆盖更新/移除/恢复合同，无需重复 R06/R04/R07。

## L06 条件性真实源安装（B06）— ⛔（只读预检完成：环境不具备，转条件记账）

只读预检（2026-09-16，有限次数探测，未改任何全局 DNS/代理/路由配置）：

- [x] 生产来源端点确认：`apps/agentshield/internal/skillimport/gitsource.go:35` 固定 `hostedGitHost = "github.com"`，走 `api.github.com`（commit 查询）与 `codeload.github.com`（zip 归档）；`download.go:24` 的 excludedIPv4 明确排除 `198.18.0.0/15`，且 `download.go:106` 注明 **无任何环境标志可放宽 DNS/TLS 策略**
- [x] 当前 DNS 条件（不能沿用历史观察，本次实测）：`github.com`→198.18.0.8、`api.github.com`→198.18.0.29、`codeload.github.com`→198.18.0.6 —— **仍处于 fake-IP 段（198.18.0.0/15）**，路由经本机 Clash/Meta TUN（`ip route get` table 2022 dev Meta）透明代理
- [x] TLS 连通性：`api.github.com` HTTPS 200、证书为真 GitHub 证书（CN=*.github.com，Sectigo 签发，2026-08-30→2026-11-27 有效，verify 结果 0）——代理为 SNI 透传非 MITM；`codeload.github.com` 301（正常根路径跳转）
- [x] **结论：B06 生产来源安装在本机当前网络条件下 blocked（按设计 fail-closed）**——Go 受控抓取路径自行解析 DNS 得到 198.18.0.0/15 地址，会命中 excludedIPv4 拒绝连接；这是 `download.go` 的设计行为（无开关可放宽），解锁条件为真实 DNS（非 fake-IP）环境。不在本批修改全局 DNS/代理，不降级 SSRF 边界
- [x] 抓取类负向合同不受影响：`gitsource.go:25` 拒绝所有重定向、固定 commit + 内容摘要合同照旧；本批不执行导入的 Skill 内容，生产来源入口开关保持关闭
## L07 文档/任务簿/矩阵回写（不提交，给提交拆分建议）— ✅

- 本进度文档已回写（L03/L04/L05 状态与本节）。批次收尾交付物已落盘至证据目录：`report.md`（含提交拆分建议）、`baseline.json`、`checks.json`、`resources.json`。
- **N09 矩阵**：不改动，维持 `personal-acceptance-baseline/v2`（本批无门槛调整；N09 未放松）。
- **宿主架构注意**：本机为 aarch64，仅 linux-arm64 产物可执行；amd64 产物未在本机实跑（构建产物哈希见 `builds-rc3.json`）。
- **O05 发现（exec→hold 机制）**：会话执行通过既有 hold 链授权——进程事实（hold 预留）经 `ocReq["exec"]` 绑定到执行请求，`ReserveHoldExecution` 消费预留；未知/已消费预留 fail-closed 拒绝，不自动重放。详见 L02 ②⑤。
- **状态兼容边界（B03 实测发现，行为事实非缺陷）**：rc.3 的 `checkUnmarkedState` 三层语义——(1) 完整核心骨架无 marker → 设计上接受为 legacy（marker 不回写）；(2) 骨架不完整但 config.json 可解码（或 keys/signing.seed 为合法 32 字节种子）→ 仍接受为 legacy；(3) 仅"非空且不可识别"（无 marker、无可读 config.json）→ `ErrMissingMarker` 拒绝。且 legacy 接受的 serve 会**静默重建缺失的 `keys/signing.seed`**（新身份）。`EnforceStateCompatibility` 不变更不迁移；`publishInitialStateFormat` 仅在 init 时写 marker。若维护者认为 (2)+(种子重建) 过宽，可作为后续收紧项（不在本批改动）。
- **给 sunbo/Luke 的重测清单**（涉及本批改动的公共 API）：
  1. 新增 3 个端点（`POST /v1/openshell/session-executions`、`/preview`、`/rollback`）：在 Windows/macOS 实机各跑一次 10 项负向 + happy path（脚本可从 `internal/server/openshell_session_exec_test.go` 的合同表直译）。
  2. `openshell.Options.Runner` 注入点与 `policy.RollbackAuthorized`：确认与 Luke 的 runner 实现兼容（签名未变，仅新增授权函数）。
  3. `Server` 结构新增字段：跨平台构建无标签冲突即可，无需行为重测。
  4. B03 `up05-*` 并发合同在 amd64 实机复跑 `closure-b03-service-concurrency.py --binary <amd64 产物>`（本机仅 arm64 可执行）。
- **提交拆分建议**（供维护者审阅，本批不执行任何 git 操作）：
  1. `feat(agentshield): O05 session-execution closed loop` — `internal/server/openshell_session_exec.go` + `_test.go` + `server.go` 路由注册。
  2. `feat(agentshield): fail-closed rollback authorization` — `internal/openshell/policy.go` `RollbackAuthorized` 及其测试。
  3. `test(agentshield): O05 enforcement live test (opt-in)` — `internal/openshell/o05_enforcement_live_test.go`。
  4. `chore(scripts): B03 service-level concurrency driver` — `scripts/personal-experience/closure-b03-service-concurrency.py`。
  5. `docs: local-o05-b3 batch evidence + progress` — `docs/local-o05-b3-progress-…md` + `docs/evidence/personal-experience/local-o05-b3-20260916-181347/`（注意其中 `*-private/` 与 serve 日志是否入库需维护者按保密策略裁决，建议排除）。

## 外部阻塞记录

| 时间(UTC) | 事项 | 根因 | 处置 |
|---|---|---|---|
| 2026-09-16 10:19 | 沙箱创建失败 `ImagePullFailed: manifest unknown` | 上游 registry 无 ghcr.io/nvidia/openshell/sandbox:0.0.83 标签，本地无缓存 | 已清理 Error 沙箱；改用本地 canary 同源镜像重建（bypxt1f1b 进行中） |
