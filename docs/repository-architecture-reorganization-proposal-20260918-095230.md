# SIQ Agent Security 仓库架构调整方案

> 版本：讨论方案 v1.0
>
> 生成时间：2026-09-18 09:52:30（Asia/Shanghai）
>
> 状态：仅供评审，尚未实施；本次仅新增本文件。
>
> 设计原则：产品真实运行优先，研究过程可解释，实验可复现，平台能力有边界，历史证据可追溯。
>
> 核查基线：`main@bb364015f745316497f0d654ee9746b5287035c0`，已合入 PR #75；本次 GitHub main 只读查询与本地一致。

## 1. 结论与本轮边界

推荐保留现有单仓多应用结构，采用“入口先统一、研究资产逐步归位、平台交付单独导航、测评结果独立呈现、运行代码稳定不动”的分阶段调整。

本方案回答四个核心问题：

1. **文献研究应成为根级研究资产。** 根目录建设 `research/`，研究问题、文献、方法、实验设计和结论可直接发现；不再把全部研究主线埋在 `docs/`。
2. **操作系统应有各自的交付入口。** 根目录建设 `platforms/linux/`、`platforms/macos/`、`platforms/windows/`，组织安装、生命周期、诊断、验证及限制。OS 专属源码仍位于其所属模块，不复制三套产品。
3. **测试结果应有独立入口。** 根目录建设 `evaluations/`，区分工程验证、研究测评、外部复现与已知缺口。单元测试留在代码旁，基准执行器保留于现有 `benchmarks/`。
4. **第三方测评应明确可见且可核验。** 先采用 `evaluations/external/`，在根 README 显式链接；不同时新增含义重叠的 `third-party/`、`results/`、`reports/`。真正独立的机构测评与合作开发者自测分开记账。

本次没有搬迁目录、修改已有文件、执行研究实验、运行产品服务、修改仓库设置或清理工作树；没有提交、推送、合并或发布。本方案中的目录、命令和任务均是**后续建议**，不是已落地能力。

默认决策：产品与可复现研究并重，不把当前仓库直接改造成纯论文代码仓；研究方法和脱敏公开材料优先开放，私人状态、客户材料、凭据与未审查原始输出独立受控保存。

## 2. 核查方法与真实基线

### 2.1 范围

本次只读检查了 Git 跟踪清单、工作树登记、README、研究与许可入口、当前任务与平台规格、代表性构建/CI/验证脚本，以及远端开放 PR 元数据。未逐份审读论文正文，未重跑历史实验，未重新审计全部代码，也未据此判断所有工作树能否删除。

实际用于核查的 main 工作树为本机 `siq/worktrees/siq-main-delivery-20260913`。本机常用项目目录 `siq/siq-agent-security` 当前登记在另一开发分支，**目录名称不等于分支身份**。后续执行必须先确认 `git rev-parse --show-toplevel`、当前分支、HEAD 与工作树状态。

### 2.2 跟踪文件统计

以下为新增本方案前、上述固定基线的跟踪文件统计，不含 Git 对象库、未跟踪构建物和其他工作树副本；大小为工作区文件字节总和，不代表克隆下载大小。

| 项目 | 本次观察 | 对方案的影响 |
| --- | --- | --- |
| 全仓跟踪路径 | 5,651 项 | 先建立分类清单，避免人工按名字批量删除 |
| `docs/` | 3,392 项，114,803,467 字节 | 混有规格、计划、证据与历史材料，不能整体搬走 |
| `docs/evidence/` | 2,556 项；是上述 docs 的子集 | 迁移风险主要来自证据引用和校验绑定 |
| `docs/` 顶层 Markdown | 120 份 | 优先解决导航与当前状态歧义 |
| 根级 `Frontier References on Agent Security/` | 36 项，131,406,251 字节；35 份 PDF、1 份 XLSX | 文献已经在根目录；需索引、权属与研究关联，不是从零创建 |
| `docs/research/` | 81 项，4,061,058 字节 | 已有研究问题、协议、主张—证据映射与复现规范，应复用 |
| `docs/hackathon/` | 558 项，53,915,090 字节 | 包含历史冻结材料，不能视作过期垃圾 |
| `benchmarks/` | 66 项 | 已有执行和验证工具，不另造一套测试框架 |
| `scripts/` | 187 项，其中 personal-experience 94 项 | 需区分通用工具、批次复现与临时诊断 |
| Go 模块 | 13 个 | 存在相对 replace 和 CI 模块矩阵，首轮保持路径 |
| Git 登记工作树 | 29 个，含临时及 detached 条目 | 是本机管理问题，不是远端项目目录布局；未逐树验收清理条件 |

### 2.3 已有模块与职责

| 现有路径 | 真实职责 | 建议 |
| --- | --- | --- |
| `apps/agentshield/` | 本机 Go 服务、CLI、授权/状态/签名、平台生命周期、OpenShell 接入、内嵌 UI | 保持模块名和目录 |
| `apps/control-api/` | 企业 Python 控制面、合同校验及业务测试 | 保持应用目录和锁定依赖 |
| `apps/secure-agent/` | 现有 Python 应用层：模型路由、固定任务与 SkillRunner，通过现有 SIQ API 执行；研究/比赛演示仍在调用 | 保留真实应用和复现依赖，不因名称含研究就移入纯文档目录 |
| `apps/web/` | 企业与个人前端，分别构建企业页面与 Go embed 本地界面 | 保持单一源码，不拆出重复个人 UI |
| `edge/agent/` | 企业 Edge 与共享 Connector 协议 | 保持与本机运行时的职责区别 |
| `connectors/` | 11 个独立 Go Connector 模块，复用 Edge 协议 | 不按 OS 重复 Connector；保持模块相对路径 |
| `adapters/runtime/` | 宿主钩子和本机裁决服务间的协议转换 | 按宿主维护，不承担授权决策 |
| `packages/contracts/` | 跨组件及离线 QA 合同 | 保留版本与解释语义 |
| `skills/` | 分发与使用入口、清单及相关资产 | Skill 不替代真实运行时 |
| `deploy/compose/`、`deploy/dgx-spark/` | 部署拓扑与 DGX Spark 环境脚本 | 保留部署权威入口 |
| `patches/` | 上游宿主补丁 | 保留上游版本、适用条件和许可信息 |
| `site/`、`demo/`、`testdata/` | 公开站点、演示与共享测试资产 | 各司其职，不混入运行时私有状态 |

### 2.4 先于搬迁的已知信息问题

- 根 [README](../README.md) 仍以 `7b4186f` 描述部分状态，并称 v6 真实任务执行接口未合入；本次基线已包含 #75。应优先更正现行概述，同时保留旧候选验收范围。
- [personal-experience-current.md](personal-experience-current.md) 顶部仍指向 v4，并累计多个历史“当前入口”；不应继续无限追加当前声明。
- [v6 集成进度](personal-v6-integration-progress-20260917.md) 开头保留当时“未提交/未合并”的快照。应通过当前索引解释历史时点，不能把原实测记录全部改成现在已通过。
- [根 CITATION](../CITATION.cff) 指向研究源码版本 `research-v0.1.0-rc.1`。main 前进不意味着该研究版本自动升级，不为整目录而修改发布身份。
- 当前 [平台材料规格](personal-platform-validation-spec-v1.md) 已说明原 18 项库存与当前产品范围的区别。不能删除旧合同里的行来制造平台全绿。
- 当前范围已由 `internal/adapterinstall/platform_scope.go` 的新接入限制及 N09 v2 表达；`personal-acceptance-baseline.v2.schema.json` 和 `n09-baseline-check.py` 支持 Linux/WorkBuddy 的 `out_of_scope/product_scope_excluded`。复用这些现有机制，不为目录方案另造并行范围算法；已知宿主列表与新接入排除条件仍需同时检查。
- `AGENTS.md` 存在历史冻结要求、后续授权周期及 CodeBuddy 旧示例。后续可按作用域整理说明，不能把删除旧示例当成删除安全约束的理由。

## 3. 架构决策

### 3.1 产品与研究共享仓库，依赖方向清晰

产品代码依赖所属模块与版本化合同；研究工具可以调用已构建产品，产品构建不能反向依赖论文、实验报告或外部测评记录。

```text
文献与研究问题
    ↓
威胁模型、方法与可检验假设
    ↓
产品实现 / 合同 / 平台接入
    ↓
基准与集成验证工具
    ↓
测评记录、证据校验与人工复核
    ↓
研究结论、能力说明与新的问题
```

这个链路是索引与工程反馈关系，不应被实现成“研究报告自动授予权限”或“测评通过自动宣布 supported”。

### 3.2 三个根级入口，一套职责划分

- `research/`：为什么研究、依据是什么、提出什么方法、结论能支持什么。
- `platforms/`：各 OS/架构/运行形态怎样部署、使用、排障和验证。
- `evaluations/`：谁在何时以什么候选测了什么，结果与局限是什么。

`docs/` 继续承担产品文档、架构规范、开发协作和运维手册；不是所有 Markdown 的兜底目录。

### 3.3 保持现有信任边界

不把本机 Grant 与企业权限混成同一个新模块；不因去重让适配器拥有签名私钥或自主判定权限；不改变 fail-closed、来源绑定、审批唯一消费、未知结果保持不确定等不变量。

目录调整不得同时更改授权算法、状态协议、签名规范、运行端口、默认采集开关、托管源 SSRF 规则或产品支持范围。

### 3.4 一份正文、多处索引

每类规范指定唯一维护位置。其他入口链接它，必要时提供简短摘要；不手工同步多份完整副本。

旧证据被多个入口引用是允许的，但必须明确其候选和范围；重复引用不增加独立实测次数。

## 4. 目标目录蓝图

下面是**目标逻辑结构**，未标“现有”的内容不代表已经存在；不要求一次性创建所有空目录。

```text
siq-agent-security/
├── README.md / README.en.md          产品与研究的双语总入口（现有）
├── AGENTSHIELD.md                    本机操作稳定入口（现有，先保留）
├── REPRODUCIBILITY.md                复现总入口（现有）
├── CITATION.cff                      软件引用与已发布版本身份（现有）
├── LICENSE / LICENSES/               许可、例外和材料范围（现有）
├── SECURITY.md / GOVERNANCE.md / CONTRIBUTING.md / AGENTS.md
│
├── apps/                            现有四应用，含 secure-agent；路径保持
├── edge/agent/                      现有企业 Edge
├── adapters/runtime/                现有宿主运行时钩子
├── connectors/                      现有 Connector 模块
├── packages/contracts/              现有版本化合同
├── skills/                          现有 Skill 分发资产
├── patches/                         现有上游补丁
│
├── research/                        新根级研究入口
│   ├── README.md
│   ├── literature/                  文献目录、来源、笔记、综述
│   │   ├── README.md
│   │   ├── catalog.json             提议：可核查的文献目录
│   │   ├── references.bib           提议：由已核实引用信息生成
│   │   ├── notes/
│   │   ├── reviews/
│   │   └── papers/                  后续仅迁入权属确认且适合分发的全文
│   ├── questions/                   研究问题与假设
│   ├── methods/                     威胁模型关联、方法、评测协议
│   ├── experiments/                 实验设计和复现入口
│   ├── findings/                    技术报告、主张—证据映射、局限
│   └── governance/                  引用、作者贡献、研究发布边界
│
├── benchmarks/                      现有基准执行器、语料与验证器
│   ├── hackathon/                   历史基准继续可复现
│   └── runtime-security/            现有运行时安全套件
│
├── evaluations/                     新根级结果与验收入口
│   ├── README.md
│   ├── catalog.json                 提议：所有可公开测评的索引
│   ├── internal/                    工程验证、研究实验的分层摘要
│   ├── external/                    外部复现、独立机构、客户公开摘要
│   ├── gaps.md                      当前缺口及解除条件，链接现行台账
│   └── templates/                   记录模板及字段说明
│
├── platforms/                       新 OS 交付和验收导航
│   ├── README.md
│   ├── support-matrix.md
│   ├── linux/                       OpenClaw / Hermes
│   ├── macos/                       OpenClaw / Hermes / WorkBuddy
│   └── windows/                     OpenClaw / Hermes / WorkBuddy；native/WSL2 分列
│
├── deploy/                          现有部署实现
│   ├── compose/
│   └── dgx-spark/                   保持硬件/环境入口
├── scripts/                         现有构建、验证和批次工具
├── testdata/                        现有共享夹具，不存私有输出
├── site/ / demo/                    现有站点和演示
│
└── docs/
    ├── README.md                    新产品/开发文档导航
    ├── architecture/                后续迁入非冻结架构说明
    ├── adr/                         现有 ADR，编号和历史保留
    ├── development/
    │   ├── current.md               唯一当前开发导航
    │   ├── plans/                   新任务书
    │   └── handoffs/                新交接和执行提示词
    ├── user-guides/                  个人使用、企业使用
    ├── operations/                  部署运维与故障恢复
    ├── evidence/                    现有历史证据，首轮原地保留
    ├── hackathon/                   现有比赛冻结材料，原地保留
    ├── research/evidence/            现有研究证据，原地保留
    └── archive/                     历史计划与迁移索引
```

过渡期根 `Frontier References on Agent Security/` 保留。等逐项完成路径、权属与引用核查后，全文可分批迁到 `research/literature/papers/`，原目录保留 README 指向新路径。不采用 Windows 支持不一致的目录软链接作为兼容方式。

## 5. 研究与文献资产设计

### 5.1 复用现有研究体系

已有 [研究总览](research/README.md)、[研究问题](research/research-questions.md)、[主张与证据](research/claims-evidence.md)、[评测协议](research/evaluation-protocol.md) 和 [外部复现模板](research/external-reproduction-template.md)。目标是迁移与补全这些材料，而不是重新编写另一套互不一致的研究结论。

根文献目录目前没有配套 README、机器目录或 BibTeX 索引。本次仅清点文件，不对论文内容、真实性、链接和全文再分发权利作已核验声明。

### 5.2 文献目录最小字段

建议 `catalog.json` 每条包含：

| 字段组 | 内容 |
| --- | --- |
| 身份 | 稳定文献 ID、标题、作者、版本、年份；未知值明确待核 |
| 来源 | DOI/arXiv/出版页面等原始链接及核查日期 |
| 本地文件 | 原路径、当前路径、文件 SHA-256；不只依赖易变文件名 |
| 权利 | 原许可证、来源说明、全文分发依据或待核状态 |
| 阅读 | 未读、已阅读、已综述、已复现，附维护者与日期 |
| 项目关联 | 关联研究问题、ADR、实现模块、实验与局限 |
| 贡献 | 引入者、笔记者、复现者，保留原提交贡献 |

现有 XLSX 先作为历史清单保留；核对后再明确 JSON 为维护源、BibTeX 为生成产物，避免三份人工清单漂移。标注项目专用字段，不把导入的 BibTeX 元数据当论文内容已验证。

### 5.3 研究主张追溯

每项“创新”应能链接到研究问题、前沿参考、项目实现、对照设计和证据。采用“已实现”“受控实验观察”“原生复现”“待验证”区分成熟度。

PR #71 的五案例报告保留为贡献者阶段观察：5/5 与 2/5 仅适用于其构造案例，不能写为总体攻破率降低结论、独立认证或新平台验收通过；候选绑定和原始证据的缺口继续可见。

技术价值可围绕权限独立、来源约束、审批到执行一致性、结果证据和生命周期恢复呈现；商业价值以个人安全管理、组织治理和集成场景说明，不由目录调整生成未经验证的首创、收入或性能主张。

还应区分“本项目复现外部论文/开源基准”与“外部团队复现 SIQ”。前者归研究实验设计，保留外部协议、上游版本、偏差与许可；后者归外部测评记录。本次未发现已独立登记的外部论文基准执行模块，不能将现有两套自有 benchmark 改名后宣称已完成外部基准复现。

### 5.4 许可与引用

现有 [许可范围](../LICENSES/scope.json) 对若干 `docs/research/` 路径逐项指定许可，并排除了第三方出版物。迁移这些路径时必须同步许可清单和相关校验，不能把项目自有文档许可自动覆盖到 PDF 全文。

将“第三方软件依赖清单”“第三方文献目录”“第三方测评记录”分别管理。当前 `third-party-source-inventory.json` 不是文献授权清单，`THIRD_PARTY_NOTICES.md` 也不是第三方测评背书。

## 6. 平台交付架构

### 6.1 一个核心，两个正交维度

操作系统负责路径、ACL/权限、进程、服务注册、通知和安装包差异；宿主负责配置、钩子、版本探测、SEC 归属、工具映射和受控启动。用矩阵组合它们，不建设八个互相复制的产品目录。

| OS | 当前产品范围 | 验证要求 |
| --- | --- | --- |
| Linux | OpenClaw、Hermes | amd64/arm64 分别记录；本机 ARM64 成功不覆盖其他架构 |
| macOS | OpenClaw、Hermes、WorkBuddy | Luke 的阶段成果与最终同候选验收分开；GUI 与 CLI 分开 |
| Windows | OpenClaw、Hermes、WorkBuddy | sunbo 的实测和在研分支继续；native 与 WSL2、guest 版本分别记录 |

Linux/WorkBuddy 不作为当前交付任务。CodeBuddy 不再新增适配和授权路径；历史配置查看、拒绝、撤销、卸载及旧证据仍按现有安全规则保留。目录清理不删除兼容恢复代码。

Windows native/WSL2 分列仅适用于实际存在且属于产品范围的运行形态，不代表 WorkBuddy 提供 WSL2 版本，也不把 WSL2 中宿主成功当作 Windows 原生成功。

### 6.2 每个 OS 入口包含什么

初期各目录只需 `README.md` 与必要的运行/验证说明，不提前创建空壳框架。包含：

- 实测环境、架构、最低版本已知/待确认状态。
- 安装、升级、后台生命周期与保留数据退出说明。
- 各宿主接入方式、已保护路径、限制和诊断步骤。
- 到实际生产代码、现有验证脚本及证据的相对链接。
- 原生桌面验收、离线验证、构建验证各自入口。
- 维护者职责与当前在研任务链接。

未来如提供 OS 专属包装脚本，只做参数和环境适配，调用原实现；不复制签名、迁移或策略判定。

### 6.3 不迁移的 OS 实现

`apps/agentshield/cmd/agentshield/` 中 systemd、launch-agent、Windows Task Scheduler 逻辑和相邻 PowerShell 资源先保留。`internal/state/`、`statefs/`、`stateformat/`、`adapterinstall/` 的平台文件留在同包，遵循 Go 文件后缀与构建约束。

不能将所有 `*_windows.go` 机械移动到 `platforms/windows/src/`：这会改变 Go 包、内部可见性、测试上下文和嵌入资源路径。

### 6.4 DGX Spark 与 OpenShell

保留 `deploy/dgx-spark/` 作为硬件环境部署入口，Linux 平台页链接它；保留 `internal/openshell/` 和现有兼容脚本作为后端实现，不将 OpenShell 建成第四种 OS。

OpenShell 专题导航同时覆盖本机 Go 的 `apps/agentshield/internal/openshell/`、企业 Python 的 `apps/control-api/app/adapters/openshell/` 与 `app/openshell_sync.py`，关联共享合同和各自验证；不为目录统一而合并两条控制面实现。

记录至少区分：SIQ 候选、宿主版本、OpenShell CLI/网关版本、GPU/CPU 与实际使用模式、策略读回、加载确认、行为验证、远端停止能力、性能协议。本机 DGX 上的 CPU fixture 不升级成 GPU 推理或完整网关验收。

## 7. 测试、评测与第三方记录

### 7.1 三层分工

| 层 | 位置 | 规则 |
| --- | --- | --- |
| 模块测试 | 现有 `*_test.go`、`apps/control-api/app/tests/`、Web 测试 | 邻近被测代码，不集中搬到根 tests |
| 验证方法和工具 | `benchmarks/`、`scripts/personal-experience/`、`scripts/research/` | 基准与产品验收分开，冻结批次执行器保留 |
| 结果与审阅入口 | 新 `evaluations/` | 索引真实证据，说明候选与方法；不运行时授权 |

不把生命周期验收脚本都塞进 benchmarks。研究基准强调语料、分母、对照和复现；工程验收强调产品旅程、系统副作用、故障恢复和交付条件。

### 7.2 统一测评索引

先建立小规模人工可审查目录；字段确定后再做轻量校验器，不先造完整测评平台。

建议字段：记录 ID、类型、作者/组织及关系声明、source SHA、dirty 状态、制品/语料/验证器/脚本摘要、OS/架构/运行形态、宿主及后端版本、方法级别、预期与实际结果、精确分母、失败/中断/重跑记录、限制、公开证据路径及摘要、复核状态。

**这是仓库索引的候选格式，不是新的运行时合同。** 不擅自改写现有 QA schema；需要互通时先明确版本与映射。

对历史记录无法取得的字段使用 unknown/not_recorded，并记录原因，不补造候选、时间或独立性。

### 7.3 外部测评分层

| 记录类别 | 说明 |
| --- | --- |
| 协作者平台验证 | sunbo/Luke 的项目协作贡献；不同设备不等于独立第三方 |
| 社区外部复现 | 公开方法下独立执行，说明与项目的关系及任何资助 |
| 专业机构测评 | 核对机构、方法、报告许可、对象版本和签署范围 |
| 客户试点验收 | 可有保密限制，仅公开获准摘要；不将客户评价当安全认证 |

外部提交采用现有复现模板的身份、失败保留、材料许可和署名原则。没有记录就明确“待开展”，不填示例成功数字。

Bugbot、SonarCloud 或 CI 扫描属于自动化分析/工程检查，不归类为独立人工测评或认证；工具未运行、额度受限、跳过分别记录。

### 7.4 结果展示

至少分开显示实现、构建/单测、真实宿主、桌面人工、发行五种状态。CI 通过不能提升平台支持，历史 18 项 QA 库存不能作为当前全部产品验收分母。

“完整通过”必须限定候选、协议、环境和覆盖范围。矩阵结构/摘要校验通过不等于真实执行通过；无记录不等于零副作用；新修复后的源码不能直接继承旧候选性能结论。

## 8. 文档导航与当前任务管理

建议形成四个稳定导航入口，各有唯一职责：

1. 根 README：个人用户、企业用户、研究者、平台贡献者四条路线；保留已有内容，修正过时状态，链接详细材料。
2. `docs/README.md`：产品架构、操作、运维、开发协作与历史文档地图。
3. `docs/development/current.md`：唯一当前开发导航，记录所依据的 main SHA、更新时间、有效任务书及各平台子任务、阻塞与下一步。
4. `research/README.md` 和 `evaluations/README.md`：分别解释研究链路和结果证据，不复制开发台账。

现有 `personal-experience-current.md` 转为兼容入口或历史快照，通过短说明链接新 current。实施前检查是否被证据清单冻结；若已冻结，仅在外部索引标为被替代，保持其字节。

时间戳用于批次计划、报告和交接；稳定文件名用于导航与现行规范。不要在一个“current”文件不断叠加互相冲突的当前指令。新 current 应引用所有仍有效子任务，不能只指向“时间最新”的一个文件而遗漏 Windows/macOS。

AI 开发提示词应给仓库相对路径、基线识别方法、允许修改范围、执行门禁和证据要求；个人机器绝对 worktree 路径只能作为本机附注，不作为协作者执行条件。

## 9. 路径调整清单

| 现有资产 | 目标或处理 | 时机与约束 |
| --- | --- | --- |
| 根 README / README.en.md | 保留，增加四类入口并同步实际基线 | 首批；中文与英文逐项核对，原产品描述不大幅删除 |
| 根 `Frontier References on Agent Security/` | 先原地索引；后续全文择项迁入 `research/literature/papers/` | 权属、SHA、外部链接和路径映射齐备后 |
| `docs/research/research-questions.md` | `research/questions/README.md` | 内容先保持，更新所有入链和许可索引 |
| `docs/research/evaluation-protocol.md` | `research/methods/evaluation-protocol.md` | 保留“历史回顾协议而非预注册”的原说明 |
| `docs/research/technical-report.md` | `research/findings/technical-report.md` | 许可与引用核查后，旧链接提供入口 |
| `docs/research/claims-evidence.{md,json}` | `research/findings/` 下保持成对维护 | 对外引用与摘要检查通过；不重标旧候选 |
| `docs/research/external-reproduction-template.md` | `evaluations/templates/external-reproduction.md` | 一份模板正文，research 链接它 |
| `docs/research/` 的发布、作者、引用和权利说明 | 分别归研究治理或当前发布说明，逐份判定 | 不整体移动，避免权属清单/历史报告混迁 |
| `docs/hermes-openshell-siq-security-experiment-20260917.md` | 首轮原地保留，加入测评索引 | 标为阶段观察，不归类为独立认证 |
| `docs/evidence/**` | 原位冻结，`evaluations/` 建索引 | 首轮不迁移、不重算旧清单 |
| `docs/research/evidence/**` | 原位冻结，研究与评测导航共同引用 | 不因 research 正文迁移而一并搬走 |
| `docs/hackathon/**` | 保留原比赛与发布身份 | 不混入当前发行完成状态 |
| 现有平台任务书与验收规格 | `platforms/` 先链接；后续非冻结指南归类 | 不重写历史分母 |
| 时间戳任务书、提示词、交接 | 新增材料进入 development 子目录；旧件先索引 | 逐份识别有效范围，再决定归档 |
| ADR、主规格、合同 | 保留现位和编号 | 影响面明确后才单独提议迁移 |
| 单元测试与 fixture | 保留所属包/应用及既有 testdata | 不为统一视觉破坏测试寻址 |
| `scripts/personal-experience/` | 先分类注册，稳定工具与批次驱动区分 | 当前活跃驱动不改路径 |
| `deploy/dgx-spark/`、`deploy/compose/` | 保留 | 新入口只链接，不复制启动逻辑 |
| `LICENSES/`、`NOTICE`、`CITATION.cff` | 保留；迁移相关列表需同步 | 不修改第三方授权含义或发布身份 |

“旧链接兼容”只适用于允许修改的导航文档；被哈希冻结的文档不能插入重定向正文。对固定 commit URL，保持历史内容并记录其对应版本；不通过重写 Git 历史修复旧 URL。

## 10. 真实路径耦合与迁移风险

| 已发现的耦合 | 可能故障 | 后续检查 |
| --- | --- | --- |
| Connector `go.mod` 的 `replace ... => ../../edge/agent` | 移目录后模块无法构建 | 13 模块路径与 CI matrix 保持，或同 PR 更新并全验证 |
| `apps/agentshield/internal/ui/ui.go` 的 `//go:embed all:embedded` | 源码能编译却缺 UI，或嵌入旧产物 | 企业/本地构建分别检查，核对 embed 产物身份 |
| `cmd/agentshield/windows_task_*.go` 相邻 `.ps1` embed | Windows 任务注册/查询失效 | 保留同包资源，交叉构建加 Windows 实测 |
| `scripts/build_ui.sh` 及 Vite 本地构建配置 | 错 cwd、写错 embedded 目录 | 使用任意受支持检出路径验证，保留安装命令 |
| `scripts/hackathon/manage.py` 通过 PYTHONPATH 加载 `apps/secure-agent` | 研究演示和真实模型应用启动失败 | 保留应用层和自身单元测试；按现有启动器验证 |
| `deploy/compose/compose.yaml` 引用 Control API 相对构建上下文 | 企业服务镜像构建失败 | 保持 build context；涉及它的调整需 Compose 配置和镜像验证 |
| `scripts/research/check_metadata.py` 固定 `docs/research` | 新 research 目录未检查、旧链接失败 | 新旧入口过渡期纳入校验，避免静默漏扫 |
| `LICENSES/scope.json` 列举路径 | 搬迁后许可标注丢失或测试失败 | 同步明示许可对象，保留第三方排除规则 |
| `.github/workflows/research.yml` 的旧 evidence 输入 | 研究 CI 无法读取历史控制报告 | 旧证据原地保存，执行器迁移需兼容 |
| 源绑定脚本遍历 Go 文件、go.mod、embedded | 改路径会改变候选源摘要 | 记录新身份及变更范围，旧证据仍只对旧候选有效 |
| Python 脚本使用 `Path(__file__).parents[n]` | 移动深度后定位错误仓库根 | 逐脚本审查，不用全局字符串替换 |
| `.gitignore` 对 `docs/evidence/**/*-private/` 与数据库的规则 | 新 evaluations 路径下泄露私有输出 | 新路径开放写入前补保护与负向校验 |
| 根目录文献的空格、Unicode 文件名 | shell、Markdown、打包路径处理失效 | 保留原名映射，兼容路径包含空格的检出环境 |
| 规则包/适配器有嵌入副本 | 删除“重复文件”使发布制品与源码脱节 | 区分生成副本与重复维护，保留现有一致性校验 |

代表性约束还包括 `adapterinstall/install_test.go` 对 OpenClaw/Hermes 源文件与嵌入安装资产的字节对等检查，以及 Go 规则包/威胁测试对 Python 规则和语料的相对引用。产品测试语料保持代码附近，不迁到研究目录造成反向依赖。现有研究源码打包工具还通过 `git archive` 获取跟踪树，移动文献并不会自动使论文从分发包中排除，分发范围必须显式核查。

本方案不提议修改对外 API、Go module 名、产品二进制名、状态目录格式或签名清单格式。若后续确需改变，另立 ADR 和兼容迁移计划。

## 11. 历史证据、大文件与私有材料

### 11.1 冻结与更正

先枚举被 SHA256SUMS、manifest、签名及报告相对路径引用的文件，再判定能否移动。默认将历史 evidence 目录视作冻结集合。

索引更正、审查结论和“被后续结果补充”的说明放在新记录中，附所更正对象的原摘要；不就地美化失败记录，不用重新计算旧清单掩盖变动。

当前产品改动会要求新的候选验证，但不会抹去旧候选证据的历史效力。

本次只读复核 `docs/research/claims-evidence.json` 的 10 条证据 SHA-256 引用，10/10 与当前文件一致。其范围包括 `docs/hackathon/final-submission-state.md`、`benchmark-report.md` 和 `evidence/INDEX.md` 等看似普通说明文：这些文件同样不能直接追加迁移提示。本次未重新验证仓外归档、视频或完整历史验收。

### 11.2 大文件策略

文献全文约 131 MB、docs 跟踪内容约 115 MB，足以值得做体积盘点，但本次没有测量 Git 历史膨胀或 clone 成本。

先确定许可、下载频率、离线复现需求和长期托管方式，再选择普通 Git、按需外部获取或 Git LFS。首轮不进行历史重写、不批量引入 LFS、不删除 PDF/截图/日志。

若未来转外部存储，必须保留稳定获取说明、内容摘要、版本、许可和不可用时的明确状态；发布二进制可沿用 Release 资产分发，但是否发布另行授权。

### 11.3 私有边界

公开测评目录不保存 daemon 完整状态、signing.seed、配对码、token、模型配置、真实客户原文或未审查截图。新运行默认输出到隔离私有目录，审查后只发布必要材料。

目录 ignore 是防误提交机制，不是权限隔离。Linux/macOS 私有权限和 Windows ACL 分别验证，不能用 POSIX 0600 推断 Windows 私密性。

已有签名原文含敏感信息时，不能直接改正文再声称原签名有效；应隔离原始材料，生成有明确派生关系的公开摘要，并注明哪些验签能力无法公开提供。

## 12. 分支、worktree 与协作安排

### 12.1 当前只读快照

本次 main 已合并 #75、#48、#49、#52 等阶段成果；这些是代码/材料纳入主线，不等于 Windows、macOS 或发行全部验收通过。

远端开放关系观察到：

```text
main
└── #80 Windows integration（draft）
    └── #81 migration recovery（draft）
        └── #82 private ACL（draft）
```

另有 #56/#57 及 #73/#74/#76/#78/#44 等平台分支。此表为本次观察时点，后续执行前必须重新查询，不能将草稿提交视作已完成成果。

**#68 `cursor/setup-dev-environment-fdee` 按项目负责人明确要求单独保留：不合并、不关闭、不删除，也不作为启动整理工作的等待条件。**

### 12.2 并行边界

现在可推进导航、资产索引、迁移清单和非侵入链接修正。Windows 当前组合批次完成评审并形成 main 基线后，再调整它涉及的脚本、状态/文件系统模块路径。

macOS 同样以 Luke 的实际活跃分支和未完成任务为准，不能因历史阶段已合入就判断其所有工作停止。公共核心与宿主验证工具的迁移由主线维护者协调。

每批功能开发与架构整理使用独立 PR；单个共享 worktree 不安排多个写入代理同时改同组文件。多代理只读检查可以并行。

### 12.3 本机清理独立执行

未来工作树登记表至少包含：路径、分支/HEAD、用途、负责人、独有提交、未提交/忽略文件、运行进程/服务依赖、证据是否引用该路径、建议处置。

删除条件必须逐树满足；`prunable` 标记和“分支已合并”均不足以证明无有价值材料。不得执行全局清理、强制 reset 或 `git clean -fdx` 代替检查。

本次未清理任何工作树。未来若规范本机布局，优先指定一个清晰 main 检出入口与独立 worktrees 根，不通过重命名整个目录打断已有服务、IDE 和临时证据引用。

## 13. 分阶段实施计划

任务编号 RA-00～RA-07 是本方案后续执行单位，不覆盖既有 F/D/N/R/B/M 任务编号。

| 阶段 | 目标与允许改动 | 前置 | 交付与退出条件 |
| --- | --- | --- | --- |
| RA-00 资产与依赖清单 | 列出现有路径、冻结范围、消费者、许可、负责人 | 选定新 main 基线；确认活跃分支 | 完整路径分类及迁移映射；无批量改名 |
| RA-01 当前入口收口 | 修正 README 状态，新增 `docs/development/current.md`、研究/平台/评测导航 | RA-00；逐项核对实际合并与证据 | 唯一当前导航；四类读者均能找到入口；旧任务不丢 |
| RA-02 文献与研究归位 | 文献目录、阅读关系；非冻结研究正文逐份迁移 | 链接、许可、源清单与旧入口处理就绪 | 元数据可核查，正文路径唯一，研究 CI 通过 |
| RA-03 测评登记 | 工程/研究/外部记录索引与模板；缺口展示 | 证据级别和候选字段规则确定 | 旧摘要不变；不借用跨候选/跨平台结果 |
| RA-04 平台交付入口 | 三 OS 文档、运行与验收链接、DGX/OpenShell 映射 | 对应平台维护者确认入口 | OS/宿主/架构/运行形态清楚，无源码复制 |
| RA-05 工具与路径整理 | 对非冻结稳定脚本小批量归类；必要时薄兼容入口 | 活跃批次合入；路径消费者枚举完成 | 新旧入口按声明工作，构建/验证产物一致或差异有解释 |
| RA-06 历史文档与本机清理 | 历史计划归档、过时入口标记、单独清理许可已明确的工作树 | 无独有成果、无资源依赖，备份可恢复 | 历史可查、当前入口简洁、无误删 |
| RA-07 收口与演练 | 干净检出、路径兼容、产品/研究复现、平台回归 | 前述适用阶段完成 | 发布候选与历史证据边界明确，记录未测项和恢复步骤 |

RA-02、RA-03 可在导航约定完成后按不重叠文件并行；RA-05 中涉及 Windows 活跃模块的部分等待组合批次，其他区域不必无期限等待。无依据的源码拆包不属于默认交付，需另外证明收益。

不提供固定工时承诺：先以 RA-00 的实际依赖数、第三方权属待核量和原生环境条件决定批次规模。

## 14. 迁移执行与回滚协议

每个迁移 PR 必须包含：

1. 基线 SHA、明确路径集合、迁移前后映射、保留理由和排除范围。
2. 入链与消费者清单：文档、构建、CI、脚本、许可、工具、历史外部引用。
3. 冻结证据识别结果与迁移前摘要；冻结对象不改写。
4. 先使用 Git 可追踪移动，避免同一 PR 大段重写正文，方便区分移动与语义变化。
5. 保持启动命令、状态格式与默认行为；若无法保持，拆出独立兼容变更。
6. 迁移后校验、失败/跳过项、恢复方案和维护者复核结论。

若导航失败，回退该导航提交；若生产路径变更导致运行失败，撤回该批变更并恢复旧入口。不要通过恢复旧版本覆盖用户运行状态，也不要把 `git revert` 当作状态格式自动降级工具。

任何跨平台文件名应避免仅大小写不同、Windows 保留名、尾点/尾空格等冲突；检查路径长度和实际支持的特殊字符。脚本引用全部采用正确引用/转义，不将绝对私有 HOME 路径写成通用入口。

## 15. 验收门禁

以下是后续实施需要执行的检查，本次方案生成未重跑它们。

| 变更类型 | 必须验证 |
| --- | --- |
| 导航/普通文档 | 相对链接及锚点、双语入口、当前基线与许可范围一致；敏感信息扫描 |
| 研究正文/文献元数据 | research 元数据校验、许可列表、原文件摘要、引用来源核对；现有 A/B 复现入口仍可用 |
| 测评索引/模板 | 正负向字段和路径校验；缺候选/缺证据/错误哈希/未运行不能显示通过 |
| Go 模块或打包路径 | gofmt、vet、相关测试，影响扩大时模块全量；四目标构建 |
| Python 工具/路径 | 对应测试、Ruff、实际 Python import/仓库根定位；合同产消同步 |
| Web/embed | npm test、企业 build、本地 build；内嵌产物检查与本机页面烟测 |
| 平台服务/权限/安装入口 | 相关 OS 原生生命周期与负向；Linux 或 CI 交叉编译不替代 Windows/macOS |
| 证据/资源引用 | 旧清单校验与公开索引完整；首次失败、重跑、partial/blocked 保留 |
| 目录或工具搬迁 | 从干净检出和含空格路径运行；旧命令兼容声明可验证 |
| 全批收口 | 最新 main 集成、相关 CI、秘密扫描、恢复演练及遗留任务清单 |

沿用仓库已有 `go vet ./...`、`go test ./...`、`uv run pytest`、`uv run ruff check app`、`npm test`、`npm run build`、`npm run build:local` 和现有验证器，不为一次文档迁移无目的重复全部高成本实验。

CI 的工作流名、required check 名称和路径过滤变更要单独评估；不随目录整理自动减少门禁，也不修改代码所有者/管理员规则来让批次“更容易合并”。

最终验收应证明：

- 新开发者能从 README 找到产品使用、当前任务、研究复现和外部测评入口。
- 普通用户不需要下载/执行文献或研究实验才能安装运行产品。
- 产品源码、测试、平台运行依赖没有因路径变化隐式丢失。
- 所有迁移项有消费者核查和恢复路径；未迁移的冻结材料仍可定位验证。
- 当前能力与历史观察不混淆；仓库看起来更清晰不等于平台验收数字增加。
- 孙博、Luke 与本机后续执行使用仓库相对路径，不依赖某个开发者的 worktree 名称。

## 16. 风险与选择

| 风险 | 应对决策 |
| --- | --- |
| 目录更整齐但安装命令失效 | 运行入口优先稳定，迁移命令单列验收 |
| 多个“current”继续互相冲突 | 一个当前导航，旧件仅兼容或历史 |
| 实验报告被当成生产能力 | 类型、候选、方法和限制随结果展示 |
| 大文件移动导致克隆问题未改善 | 先测 Git 历史和需求，不以工作区大小直接决定 LFS |
| 平台目录演变成三份核心代码 | OS 入口与核心实现分离，模块职责不变 |
| 新结果目录漏掉秘密忽略规则 | 先补防误提交规则与负向，再开放写入 |
| 清理旧 CodeBuddy 时破坏安全退出 | 保留历史查看、撤销、卸载及兼容拒绝能力 |
| 未完成 Windows 栈被结构调整打断 | 源码路径变更等待相关栈形成稳定 main 基线 |
| 新研究命名覆盖原贡献与许可 | 保留作者、原始来源、授权范围和 Git 历史 |

暂不采用：拆为多个独立产品仓；统一重写所有测试框架；按 OS 全量拆包；创建并行权威合同；批量清除历史日志；强制重写 Git 历史；把一切“测试通过”折算成总体完成百分比。

## 17. 后续执行提示约束

未来交给 Codex/GLM/Kimi 时，应附以下约束，而不是仅说“按新目录整理全部文件”：

> 从已确认的最新 main 与干净独立工作树开始，先核对本方案基线差异及远端活跃依赖。一次只执行一个 RA 阶段或明确的路径子集。先列消费者和冻结证据，再实施迁移。保留旧启动入口、历史候选、许可与失败材料。不得修改运行时授权语义、状态协议、发行身份或默认隐私设置；不得改写历史签名/摘要材料。#68 明确排除。每批说明修改路径、运行影响、实际验证、未测条件、恢复方法以及 Git 状态。未经新的执行指令，本方案只供评审。

## 18. 评审结论

可以现在开始的后续工作是资产清单、当前入口收口、文献索引、平台导航与测评登记设计；实际执行仍由后续指令开启。

源码路径调整及 Windows 活跃模块的工具归位，应在相关开发批次合入稳定 main 后实施。无需等待 #68 或所有历史分支消失。

最终目标是形成一个能够真实使用、按证据陈述能力、支持研究复现和外部评价的仓库。评估收益以入门清晰度、运行入口稳定性、证据可追溯性与维护成本衡量，不以移动文件数量衡量。
