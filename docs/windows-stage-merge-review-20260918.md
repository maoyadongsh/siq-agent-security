# Windows 阶段审查与集成记录（2026-09-18）

## 范围

按维护者授权，优先处理非草稿、小范围且可独立验收的 Windows 提交。审查环境为 Linux；Windows 实机结论来自协作者固定候选证据，本次不冒充重新执行 Windows 实机测试。

| PR | 范围 | 审查结果 |
| --- | --- | --- |
| #60 | Schema 测试入口延迟导入应用，保留 client fixture 行为 | 全量 Python pytest 与 Ruff 通过，已通过远端检查并合并 |
| #58 | 历史无标记状态到 v2 的迁移证据 | 7 项清单文件长度/哈希匹配，候选及限制明确，已通过远端检查并合并 |
| #61 | Windows 文件占用后的更新恢复证据 | 8 项长度/哈希匹配，两张截图已查看，已通过最终远端检查并合并 |

#60 另有 4 项证据文件长度/哈希核对，合计 19 项。三分支在隔离工作树按当前 main 试合并无冲突，提交范围 gitleaks 无泄露。保留协作者原提交、失败轮与候选身份，不扩大 N01/P04/Windows 三宿主验收范围。

## 保留的开发分支

#44/#73/#74 仍为草稿，不代协作者宣布完成。#49 修复基于旧安装协议，main 已迁移到 protocolVersion/sourcePath 等字段，不能靠覆盖式冲突解决恢复旧测试。#57 存在冲突，需单独审查。其他 Windows 证据及修复留待后续批次；#72 在上一批已合并。

## PR #75 附带修复

远端 agentshield 与 runtime-security-toolchain 均定位到同一无后端绑定用例：缓存指纹对错误/无配置也非空，不能代表明确 endpoint。提交 d0e58eb 对任务执行、加载验证与实例读回增加显式 env_pair 绑定要求，五种负向情形在后端 I/O 前拒绝。Go 全量 vet/test、openshell/server race、四平台 CGO=0 构建通过；该提交的远端检查亦已完成且无失败。

修复改变产品源码；旧 0b16e5e0 候选和 ba8d266e 源绑定证据保持原适用范围，不自动代表修复后的版本。后续新候选需相应实机与性能验收。PR #75 仍受代码所有者审核要求约束，不绕过保护规则。
