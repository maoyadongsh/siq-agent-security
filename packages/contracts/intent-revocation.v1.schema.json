{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Immutable signed global Intent revocation v1",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "schema_version",
    "intent_id",
    "intent_digest",
    "revoked_at",
    "reason_code",
    "signing_schema",
    "signature"
  ],
  "properties": {
    "schema_version": {
      "const": "intent-revocation/v1"
    },
    "intent_id": {
      "type": "string",
      "pattern": "^[A-Za-z0-9][A-Za-z0-9._-]{0,127}$"
    },
    "intent_digest": {
      "type": "string",
      "pattern": "^[0-9a-f]{64}$"
    },
    "revoked_at": {
      "type": "string",
      "format": "date-time"
    },
    "reason_code": {
      "const": "intent_revoked"
    },
    "signing_schema": {
      "const": "local_canonical/v1"
    },
    "signature": {
      "type": "string",
      "pattern": "^[0-9a-f]{128}$"
    }
  }
}
