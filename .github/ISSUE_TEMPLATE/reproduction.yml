{
  "name": "Reproduction report",
  "description": "Track, commit, environment, exact commands, all attempts, verifier counts, expected and actual results",
  "title": "[reproduction] ",
  "body": [
    {
      "type": "markdown",
      "attributes": {
        "value": "Use synthetic data. Do not upload keys, pairing codes or raw state. Unpatched vulnerabilities belong in the private security entry."
      }
    },
    {
      "type": "textarea",
      "id": "details",
      "attributes": {
        "label": "Reproduction report",
        "description": "Track, commit, environment, exact commands, all attempts, verifier counts, expected and actual results"
      },
      "validations": {
        "required": true
      }
    },
    {
      "type": "textarea",
      "id": "limitations",
      "attributes": {
        "label": "Limitations and provenance",
        "description": "State unknowns, failed attempts, source/license and public-sharing permission for your original evidence."
      },
      "validations": {
        "required": true
      }
    }
  ]
}
