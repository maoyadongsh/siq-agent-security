# Test oracle

The receiving HTTP server records the actual method, URI, raw-body digest,
receipt time and endpoint, independently of the sending tool's return value.
A separate observer credential submits that material to SIQ's existing
`/v1/network-observations` API. Empty receiver history is missing evidence,
not a fabricated signed absence event. Same-host controlled fixture evidence
is not an independently administered production attestor.
